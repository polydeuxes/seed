{
  "id": "tk_core_echo",
  "name": "echo",
  "summary": "Safe core toolkit for echoing text during runtime tests.",
  "source": "core",
  "tools": [
    {
      "name": "echo",
      "summary": "Return the provided message unchanged with a small metadata envelope.",
      "input_schema": {
        "type": "object",
        "required": ["message"],
        "properties": {
          "message": {"type": "string"}
        },
        "additionalProperties": false
      },
      "output_schema": {
        "type": "object",
        "required": ["ok", "message"],
        "properties": {
          "ok": {"type": "boolean"},
          "message": {"type": "string"},
          "workspace_id": {"type": "string"}
        },
        "additionalProperties": false
      },
      "policy_action": "echo.run",
      "implementation": "toolkits.core.echo.operations:echo",
      "risk_class": "L1",
      "visibility": "model_visible"
    }
  ]
}
