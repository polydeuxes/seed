{
  "id": "tk_generated_ssh_access",
  "name": "ssh_access",
  "summary": "Safe generated-style SSH access toolkit with read-only/stub verification and plan-only install guidance.",
  "source": "generated",
  "tools": [
    {
      "name": "verify_ssh_access",
      "summary": "Return a safe, non-network SSH access verification stub for a host.",
      "input_schema": {
        "type": "object",
        "required": ["host"],
        "properties": {
          "host": {"type": "string"}
        },
        "additionalProperties": false
      },
      "output_schema": {
        "type": "object",
        "required": ["ok", "host", "access_status", "method", "summary"],
        "properties": {
          "ok": {"type": "boolean"},
          "host": {"type": "string"},
          "access_status": {"type": "string", "enum": ["not_checked"]},
          "method": {"type": "string", "enum": ["stub_no_network"]},
          "summary": {"type": "string"}
        },
        "additionalProperties": false
      },
      "policy_action": "ssh_access.verify",
      "implementation": "toolkits.generated.ssh_access.operations:verify_ssh_access",
      "risk_class": "L1",
      "visibility": "model_visible"
    },
    {
      "name": "plan_ssh_install",
      "summary": "Return a non-mutating plan for installing or enabling SSH on a host.",
      "input_schema": {
        "type": "object",
        "required": ["host"],
        "properties": {
          "host": {"type": "string"}
        },
        "additionalProperties": false
      },
      "output_schema": {
        "type": "object",
        "required": ["ok", "host", "plan_only", "mutation_allowed", "steps", "summary"],
        "properties": {
          "ok": {"type": "boolean"},
          "host": {"type": "string"},
          "plan_only": {"type": "boolean"},
          "mutation_allowed": {"type": "boolean"},
          "steps": {
            "type": "array",
            "items": {"type": "string"}
          },
          "summary": {"type": "string"}
        },
        "additionalProperties": false
      },
      "policy_action": "ssh_access.plan_install",
      "implementation": "toolkits.generated.ssh_access.operations:plan_ssh_install",
      "risk_class": "L1",
      "visibility": "model_visible"
    }
  ]
}
