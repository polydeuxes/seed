{
  "id": "tk_generated_host_notes",
  "name": "host_notes",
  "summary": "Harmless generated-style toolkit for recording and listing notes about hosts in Seed state.",
  "source": "generated",
  "tools": [
    {
      "name": "add_host_note",
      "summary": "Record a note about a host in the Seed event ledger without touching the host.",
      "input_schema": {
        "type": "object",
        "required": ["host", "note"],
        "properties": {
          "host": {"type": "string"},
          "note": {"type": "string"}
        },
        "additionalProperties": false
      },
      "output_schema": {
        "type": "object",
        "required": ["ok", "host", "note_id", "summary"],
        "properties": {
          "ok": {"type": "boolean"},
          "host": {"type": "string"},
          "note_id": {"type": "string"},
          "summary": {"type": "string"}
        },
        "additionalProperties": false
      },
      "policy_action": "host_notes.add",
      "implementation": "toolkits.generated.host_notes.operations:add_host_note",
      "risk_class": "L1",
      "visibility": "model_visible"
    },
    {
      "name": "list_host_notes",
      "summary": "List notes previously recorded for a host in the Seed event ledger.",
      "input_schema": {
        "type": "object",
        "required": ["host"],
        "properties": {
          "host": {"type": "string"}
        },
        "additionalProperties": false
      },
      "output_schema": {
        "type": "object",
        "required": ["ok", "host", "notes"],
        "properties": {
          "ok": {"type": "boolean"},
          "host": {"type": "string"},
          "notes": {
            "type": "array",
            "items": {
              "type": "object",
              "required": ["id", "note"],
              "properties": {
                "id": {"type": "string"},
                "note": {"type": "string"}
              },
              "additionalProperties": false
            }
          }
        },
        "additionalProperties": false
      },
      "policy_action": "host_notes.list",
      "implementation": "toolkits.generated.host_notes.operations:list_host_notes",
      "risk_class": "L1",
      "visibility": "model_visible"
    }
  ]
}
