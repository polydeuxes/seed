# Post-Locality active lifecycle residue census 001

## Question

After freezing `06.Locality.B`, `06.Locality.C`, and `06.Locality.D`, which
console-reachable family gives the narrowest independent pressure specimen for:

```text
prospective lifecycle identities
separate binding occurrences
copied coordinates
broad material reads used for exact identity questions
```

This census does not generalize the Locality result to another family.

## Active boundary

The inward operator road currently reaches:

```text
operator and Witness Source
Locality relations
occurrence-position Measurement
registered Measurement families
byte and byte-pair Measurement
recorded-pair Compare
shared-position Measurement and Compare
Compare-Distinction Measurement
```

Callable authored controls and retired lifecycle constructors are not evidence
that their occurrence shapes remain active.

## Remaining specimens

### Source and Locality

The active Source and Locality roads have already lost their prospective result
and Act-occurrence identities. Locality B/C/D have also independently lost
their binding occurrences and copied Act/result coordinates. They remain
positive controls and are frozen.

### Occurrence-position and byte Measurement families

These active families still contain combinations of:

```text
separate binding occurrences
opaque exact Act identities
prospective Act-occurrence identities
prospective result identities
copied binding references and result coordinates
```

They are substantial future pressure targets, but their binding and bounded
current-coordinate readers make several distinctions move at once.

### Compare-Distinction Measurement

This family has the smaller active shape:

```text
exact current Compare result C
        ↓
Measurement Act A
        ↓
Measurement result R
```

There is no separate binding occurrence and no Applicability lifecycle. `A`
already addresses exact `C`.

Nevertheless `A` mints and carries:

```text
opaque exact Act identity
prospective Act-occurrence identity
prospective Measurement-result identity
```

`R` then copies those identities plus the Act name, subject, source-result
occurrence, source Locality, and finding coordinates. The actual Ledger
occurrences `A.identity` and `R.identity` remain separate exact addresses.

## Why this family is next

The family satisfies four independent controls:

```text
active through the operator console                 yes
subject exact before the Act                        yes
separate binding occurrence                         absent
positive result-to-later-Act composition consumer  yes
```

Its results later participate in the exact read-only joint of two Distinction
Measurement occurrences. Equal findings must therefore retain separate result
occurrence identities while copied lifecycle aliases are pressured.

## Pressure order

```text
1. prospective Measurement-result identity
2. prospective Act-occurrence identity
3. opaque exact Act identity
4. copied result coordinates
5. broad reads, only where an exact narrower question is found
```

The actual Act and result Ledger occurrence identities, exact subject `C`,
Measurement Act, Locality, complete finding structure, replay, restart, and
downstream occurrence references must remain.

## Disposition

```text
Locality B/C/D                           frozen
global lifecycle subtraction            refused
Compare-Distinction Measurement         next independent specimen
occurrence-position and byte families   retained for later pressure
```

No runtime road, Book clause, occurrence, Act, result, relation, or coordinate
is added or removed by this census.
