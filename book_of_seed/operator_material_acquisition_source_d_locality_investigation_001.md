# Operator-material acquisition / 01.Source.D Locality investigation 001

## Question

Does the exact operator-material occurrence produced when material crosses the
console boundary already establish the Locality relation required by
`01.Source.D`?

This report uses **O1**, **O2**, **contact occurrence**, and **console ingress**
only as investigation orientation:

```text
O1 = one exact nonempty operator-boundary occurrence
O2 = one later exact nonempty operator-boundary occurrence
```

Those names establish no chronology beyond the exact recorded occurrence
order, no response relation, no movement, and no Standing by identity.

The first subject of the required Locality relation remains independently
unresolved:

```text
operator-material acquisition result I
--carries--> exact material M

I != M

required Locality first subject X
?= I
?= M
?= another exact bounded subject
```

## Active material inspected

- `01.Source.D` and `01.Source.G` in
  `book_of_seed/chapters/01_source_coordinates_and_grammar.md`
- `06.Locality.A` through `06.Locality.D` in
  `book_of_seed/chapters/12_locality_relations.md`
- `01.Source.G` in `book_of_seed/witness_grammar.json`
- `seed_runtime/operator_console.py`
- `seed_runtime/operator_material_acquisition.py`
- `seed_runtime/operator_ingest.py`
- `seed_runtime/material_ingest.py`
- `tests/test_operator_material_acquisition.py`
- `tests/test_operator_ingest.py`

No Book clause, Witness Grammar coordinate, or runtime occurrence is added by
this report.

## 1. Ordinary console material first enters the recorded acquisition road

For each nonempty ordinary console read, `run_persistent_operator_console()`
records this exact family:

```text
01.Source.G Responsibility assignment occurrence
↓
02.Acts.A responsible Act Evidence occurrence
↓
operator boundary supplies exact material
↓
Evidence-of-Yield relation
↓
01.Source.G exact operator-material acquisition result I
```

The result event carries the exact supplied bytes as `exact_material`.

`run_operator_ingest()` is a separate road. In the persistent console it is
currently entered only inside the external operator-invocation branch, after a
host prefix predicate. Ordinary material such as `Hello\n` does not need that
later `material.ingest.occurred` event to become one exact
`01.Source.G` acquisition result.

Therefore the current contact-point question is first about the
operator-material acquisition occurrence, not a generic Ingest occurrence.

## 2. O1 already carries substantial exact physiology

The `01.Source.G` assignment carries:

```text
assignment identity
assignment subject identity
Book clause identity
responsible boundary = this Seed
exact acquisition Act identity
Act occurrence identity
result boundary identity
addressed Representation occurrence reference
current bounded Locality replay boundary reference
runtime locality identity
Scope
Evidence occurrence reference
Authority
limits
Unknown
```

The distinct Act Evidence occurrence carries the assignment reference, exact
Act and occurrence identities, Scope, Authority, evidence boundary, and
responsible boundary.

The result preserves those references and adds:

```text
exact source-boundary material
exact material M
known loss
Evidence-of-Yield relation reference
preserved Standing material
represented_relation Unknown
source_relation Unknown
```

The result reader reconstructs those coordinates, validates occurrence order,
and requires the exact Evidence-of-Yield relation.

This is not a bare ledger label. O1 is already an exact recorded boundary
physiology.

## 3. No second Locality lifecycle is required by identity

Active `06.Locality.A` requires an exact evidenced relation between two exact
bounded subjects. It does not state that every Locality relation must be
recorded by a standalone generic Locality Act after the occurrence that
produced its subjects.

One responsible occurrence carries both its primary result and another
relation only where the exact relation subjects, occurrence, Evidence,
Authority, Scope, limits, and Unknown are established for that same bounded
occurrence.

Therefore:

```text
whether O1 is the occurrence carrying the required Locality relation:
unresolved
```

is a live investigation orientation.

It does not establish:

```text
O1 Act occurrence
= Locality relation occurrence
```

or require another event by default. Exact identity and coordinates decide the
question.

## 4. The current O1 record does not carry the required relation

The exact acquisition record has:

```text
responsible_boundary = this Seed
runtime locality_identity = L
source Standing reference in L
Act occurrence identity
Act Evidence
Scope
Authority
limits
Unknown
```

It does not carry one structured Locality Assertion containing:

```text
first exact bounded subject X
relation = locality
second exact bounded subject = this_Seed
participants and roles
Locality relation occurrence identity
Locality Evidence identity
```

The available coordinates do not fill those positions by identity:

```text
responsible boundary = this Seed
!= this Seed occupies the second Locality-subject role

Act occurrence identity
!= Locality relation occurrence identity

Act Evidence occurrence
!= Locality Evidence

runtime locality label L
!= Locality relation

I carries M
!= I or M occupies the first Locality-subject role
```

Whether the O1 record is the intended carrier remains unresolved. The required
relation is not currently recorded there.

## 5. The bare `Locality` coordinate in `01.Source.G` does not complete it

Active `01.Source.G` says its assignment preserves `Locality`. Witness Grammar
lists `Locality` among that Responsibility's coordinates.

The coordinate list does not independently name:

```text
Locality first subject
Locality second subject
participants and roles
relation occurrence
Locality Evidence
```

Active `06.Locality.A` explicitly refuses subject identity, co-presence,
chronology, and shared label as the Locality relation.

Therefore:

```text
01.Source.G coordinate list contains Locality
!= exact 01.Source.D Locality relation established
```

The bare coordinate records unresolved pressure, not completion of the
relation physiology. Its presence does not establish which exact Locality
relation its author intended.

## 6. The addressed Representation does not silently supply the relation

Before each operator read, the acquisition assignment validates one addressed
Representation carried by the current bounded Locality replay. That
Representation has its own exact Locality Evidence.

The acquisition source reference retains only:

```text
runtime locality identity
bounded replay occurrence reference
addressed Representation occurrence reference
```

It does not retain the Representation's Locality Evidence as the Locality
Evidence of `I`, `M`, or another first subject to this Seed.

Even if that evidence reference were copied, the subjects of the
Representation Locality relation would remain distinct. Reference availability
would not transfer the relation.

## 7. O2 is another exact contact occurrence

For two nonempty reads, existing tests establish:

```text
O1 -> one exact assignment, Act Evidence, Yield, result I1, material M1
O2 -> another exact assignment, Act Evidence, Yield, result I2, material M2
```

The occurrences, Acts, results, Scopes, and material remain distinct even when
the exact bytes are equal.

If the required Locality relation is recovered as part of this acquisition
road, each operator occurrence requires its own exact relation coordinates. O1
cannot lend its relation occurrence to O2 by shared console, locality label, or
chronology.

## 8. Effect on the spring investigation

The earlier Locality investigation was right to refuse:

```text
same locality label
-> exact Locality relation
```

It was too early to orient toward a separate new relation-producing lifecycle.
The existing `01.Source.G` occurrence is the first exact road to pressure.

The spring topology can now remain:

```text
exact operator-boundary occurrence O1
↓
exact acquisition result I carrying material M
↓
[required Locality relation carried by O1: unestablished]
↓
this Seed bears the conditional Measurement Responsibility
↓
subject-specific assignment
↓
Applicability / Participation / Act
```

This refinement does not repair the separate assignment-Standing gap. It also
does not make arbitrary ledger mutation a constitutional occurrence.

## 9. Elimination matrix

| Candidate coordinate in O1 | Exact current finding |
|---|---|
| `responsible_boundary = this Seed` | exact acquisition Responsibility boundary, not Locality role by identity |
| acquisition assignment subject | exact acquisition-assignment subject, not required Locality first subject by identity |
| acquisition Act occurrence | exact Act occurrence, not relation occurrence by identity |
| acquisition Act Evidence | exact Act Evidence, not Locality Evidence by identity |
| result `I` | exact result carrying material, first Locality role unresolved |
| material `M` | exact carried material, first Locality role unresolved |
| runtime locality identity | exact replay address, not relation |
| current bounded Locality replay reference | exact source boundary reference, not relation |
| addressed Representation Locality Evidence | exact different relation family |
| `01.Source.G` coordinate named `Locality` | required structure not instantiated |

## 10. Current disposition

```text
ordinary console material enters 01.Source.G acquisition    established
O1 assignment / Act Evidence / Yield / result               recorded/replayable
O1 carries exact material M                                 recorded/replayable
O1 responsible boundary = this Seed                         recorded/replayable

separate Locality event required by identity                not established
each additional relation carried by O1                      separately established coordinates required

required Locality first subject X                           unresolved
X --locality--> this_Seed                                   unestablished
Locality relation occurrence                                unestablished
Locality Evidence                                           unestablished
```

## Next bounded question

Do the active history and exact `01.Source.G` Responsibility establish that O1
was intended to carry the missing Locality relation? If so, recover its exact
first subject, second-subject role, relation occurrence, Evidence, Authority,
Scope, limits, and Unknown inside that same bounded road.

Do not add a second Locality lifecycle merely because the current O1 record is
missing those coordinates. Do not add fields to O1 until the exact relation
subjects and responsible physiology are recovered.
