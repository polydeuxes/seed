# Byte-pair input Applicability subtraction report 001

## Result

The active inward byte-pair road now records:

```text
exact byte Measurement result occurrence
+ exact result-local source position
        ↓
byte-pair Measurement Act occurrence
        ↓
byte-pair Measurement result occurrence
```

The former Applicability binding, Measurement binding, Applicability Act, and
forced-positive Applicability result occurrences are absent from this road.
No replacement Candidate, population, binding event, verdict, or wrapper was
introduced.

## Exact subject before the removed stages

The direct Measurement Act addresses one already recorded
`exact_source_material_set` result position. Its coordinates are:

```text
parent byte Measurement result occurrence
result-local position
source material occurrence references
completeness boundary
source Localities
result-position movement occurrence when movement occurred
destination Locality
```

These coordinates existed before the removed lifecycle. The old
Applicability producer returned `applicable` for every accepted instance and
had no negative producer. It neither narrowed these coordinates nor changed
the bounded material.

## Direct Act and result

The byte-pair Measurement Act records its exact subject position, declared
Act identity, Act occurrence identity, future result identity, Locality, and
prior through-occurrence boundary. The result directly addresses that Act
occurrence and records exact pair-position content:

```text
ordered byte-pair content
total pair occurrence count
number of source occurrences containing the pair
recurrence when the total count exceeds one
exact source result-position references
```

The result reader validates the recorded result positions and their local
references. Reading does not perform Measurement again.

## Preserved physiology

The subtraction preserves:

```text
source position before Measurement Act
Measurement Act before result
Act present with result absent
exactly one result for one Act
same-Locality and moved-source coordinates
pair counts and recurrence
current-coordinate replay
SQLite reopen
changed source and changed Act refusal
intervening append and mutation refusal
recorded-pair Compare source provenance
source-position recurrence and Compare
ordered-position and Distinction results
```

Consumers now resolve the pair result's source occurrence sequence through
its exact source byte-result position. They no longer obtain that provenance
from the retired Measurement binding event.

## Negative control

The older callable byte-pair lifecycle remains as a laboratory control. It
can still instantiate its independently callable binding and Applicability
surfaces, including authored malformed and negative cases. The active inward
operation does not call that lifecycle.

Ordinary recurrent shared-position Applicability also remains untouched. Its
callable result can vary between `applicable` and `inapplicable`, and its
occurrence-production boundary remains frozen.

## Finding

```text
exact subject before prospective stages       established
independently variable Applicability verdict  absent on active byte-pair road
forced positive ceremony                      failed subtraction
separate binding occurrences                  failed on active byte-pair road
Measurement Act/result distinction            preserved
callable variable Applicability control       retained
```

Applicability is not a mandatory stage before Measurement. This result is
bounded to the active byte-pair road whose exact subject was already present
and whose Applicability result could not vary.

## Validation

```text
focused and dependent runtime / grammar tests  258 passed
exact-material reading controls                5 passed
broader suite before long combinatorial stop   404 passed, 8 skipped
full-suite collection                          1,133 tests
```

The five exact-material controls include the isolated ordered-structure case.
The broader run was stopped during an existing long shared-position expansion
after it had produced no failure.
