# Open-world internal-variation observation 001

## Boundary

This is a findings-only continuation from the source-selected Unicode-scalar
road at `05927819` and the outer-context stopping boundary at `baf7c9fc`.

Discovery read only:

```text
the frozen scalar-aperture artifact for exact source-window addresses
the 17 exact addressed source windows
```

It did not read the Book, witness grammar, Rosetta material, a dictionary,
expected words, language categories, or a requested coordinate count.  It made
no Book, machine-grammar, runtime, or test amendment.

The question was not:

```text
find a structure with two changing positions
```

It was:

```text
for every source-derived recurrent extent
measure how many internal coordinate classes separately vary
```

## Exact operation

Every adjacent Unicode-scalar extent begins independently.  Each extent grows
by its next exact source-order scalar only while its complete internal
same-content/difference surface recurs.

For one recurrent surface, exact source positions are partitioned into the
coordinate classes already established by that surface.  Every class is then
tested identically:

```text
hold every other exact class material fixed
↓
enumerate the exact materials the source carries at this class
↓
retain a substitution frame only when at least two distinct complete
productions each recur
```

No class is selected before the operation.  One recurrent surface therefore
reports exactly:

```text
0 / 1 / 2 / ... / n
source-addressed varying coordinate classes
```

This is stronger than noticing that several coordinates vary somewhere in a
population.  For each reported class, the source contains exact recurrent
productions that differ there while every other coordinate-class material in
that local frame remains fixed.

It does not establish that two such frames have the same support population,
that their consequences are equal, or that either coordinate governs another.

## Frozen findings

The first complete monolithic observer artifact is retained beneath `/tmp` as:

```text
/tmp/seed_open_world_internal_variation_blind.json
bytes:            31,519,801
findings SHA-256: 3c58e671553a50372d4125d2b879506ea9c7ffa7887464b794e5a9803533e921
known loss:       none
wall time:        147.750 seconds
```

The artifact was then projected without changing any source finding into 17
independently bounded source artifacts plus this manifest:

```text
/tmp/seed_open_world_internal_variation_manifest.json
bytes:            11,840
findings SHA-256: 2cfce8391612463c2c8854b6e548c733ac3e097364584162c6f9c63feb94fcdb
known loss:       none
```

The exact pressure projection is:

```text
/tmp/seed_open_world_internal_variation_pressure.json
bytes:            43,626
findings SHA-256: 57e922ca4df98c8b3684b64fc55ff0429ed971b1de5478d104728174eb0f85be
```

The sharding verifier establishes:

```text
each shard source finding
==
the corresponding complete monolithic source finding
```

The shard boundary is representational, not a change to the observation.

## Population

Across all 17 independently bounded windows, recurrent same/different surfaces
carry this exact varying-coordinate population:

```text
source-addressed varying coordinate classes    recurrent surfaces
0                                                         210,550
1                                                           2,370
2                                                             690
3                                                             363
4                                                             224
5                                                              43
6                                                              27
7                                                              11
8                                                               1
                                                          -------
                                                          214,279
```

Thus 1,359 recurrent surfaces expose at least two separately varying internal
coordinate classes without the observer asking for two.

Their exact extent distribution is:

```text
coordinate count    multi-position surfaces
2                                         17
3                                         65
4                                        121
5                                        196
6                                        271
7                                        284
8                                        204
9                                         97
10                                        42
11                                        29
12                                        18
13                                         8
14                                         4
15                                         2
16                                         1
```

Every source population contributes multi-position surfaces.  The maximum
number of separately varying classes observed in one recurrent surface is:

```text
Russian arithmetic window    8

Austen, Roget, Kittredge,
Grimm, Spanish, Latin,
Greek, Russian               7

many other windows           6
```

These numbers do not name the materials or assign ordinary-language roles.

## What the strongest surfaces are

The strongest numerical surfaces are not secretly recovered sentences or
grammar roles.  The unique eight-position finding is the complete all-different
surface over eight scalars in the Russian window.  Its recurrent exact
productions include unrelated line-leading and textual material.

The seven-position population similarly contains prose, grammar testimony,
layout, line starts, and several external languages.

That is a useful refusal:

```text
many separately varying positions
!=
language-bearing organization
```

The measurement has found the missing coordinate population, not a head.

## Performance compression

The first implementation treated every substitution frame as a separately
copied population.  By Franklin extent six it had copied 8,055 exact
productions into 4,685 frame objects and emitted 9.3 MiB before reaching the
later extents.

The correction preserves one indexed recurrent production population per
surface:

```text
one exact surface support population
├── exact recurrent productions
└── each varying coordinate carries only the production-index groups
    produced while all other coordinate classes remain fixed
```

Complete addressability is unchanged.  Franklin's complete artifact fell from
10.8 MiB during the interrupted first shape to 2.28 MiB, and repeat runs yield
the same exact source artifact digest.

The second compression preserves the 17 source windows as 17 source artifacts.
Passing the complete 31.5 MiB population through a parent process and
serializing it again took about 90 seconds after parallel work had finished.
Direct source shards remove that serial reconstruction and preserve the source
boundaries more honestly.

The one-time monolith-to-shard proving conversion remains slow because it must
read and rewrite the retired monolithic representation.  Future observations
write the shards directly.

A separate mechanics correction then refused reconstruction for any recurrent
surface that carried fewer than two recurrent exact productions.  Such a
surface can only contribute to the measured zero-position count; rebuilding
its exact starts and coordinate classes cannot change that result.

The correction preserves every source artifact byte-for-byte.  On Franklin:

```text
before mechanics: 47.764 seconds
after mechanics:  33.948 seconds
source SHA-256:    305215850fb61dae0c7e82be20e768f2d2828df28e8658a5aefd25fa100e3638
```

Starting the largest exact source windows first further removes parallel tail
latency without changing manifest order or findings.  The complete 17-source,
14-process run then recorded:

```text
wall time:   66.556 seconds
known loss:  none
```

All 17 source artifact digests equal the frozen complete population.  The
aggregate observation remains 6.556 seconds above the desired one-minute
mechanics boundary, but no individual source mechanics exceeded its 55-second
exact bound.  The earlier 147.750-second monolithic road is no longer the live
observer shape.

## Why direction still does not follow

The prior observation stopped because every recovered frame had one varying
internal position.  That missing population now exists:

```text
one recurrent larger surface
├── separately varying coordinate X
├── separately varying coordinate Y
└── sometimes further separately varying coordinates
```

But the current finding establishes only exact substitution addressability:

```text
change X while its local other coordinates remain exact

change Y while its local other coordinates remain exact
```

It has not yet measured:

```text
X substitution
→ exact later/outward consequence population

Y substitution
→ exact later/outward consequence population

X consequence
Compare
Y consequence
```

Therefore none of these follows:

```text
more varying positions
!= organization

more support
!= organization

greater extent
!= organization

one coordinate occurring earlier
!= organization
```

## Exact stopping boundary

The open-world road now reaches:

```text
source-selected Unicode-scalar material
↓
incremental complete same/different surfaces
↓
exact recurrence
↓
source-addressed 0 / 1 / n internal variation classes
↓
1,359 exact multi-position surfaces
↓
STOP
```

The next exact subject is no longer a developer-authored two-slot template.  It
is one of the source-produced multi-position surfaces and the exact recurrent
substitution frames it already carries.

The missing finding is a comparison of independently measured consequence
populations for two or more of those coordinates under that same exact surface
boundary.

Even if such consequence asymmetry appears, it will establish only that exact
asymmetry.  Organizer-, dependent-, head-, and root-like relations remain
unestablished until exact directional relations warrant them.

## Finding

The source naturally supplies recurrent larger structures with several
separately varying internal coordinates.  The requested aperture therefore
exists without selecting a slot count, material value, language, or role.

This is a real advance over `baf7c9fc`: the prior missing population has been
recovered.  It is not yet hierarchy.  It is the first unbiased boundary at
which directional consequence can now be measured.
