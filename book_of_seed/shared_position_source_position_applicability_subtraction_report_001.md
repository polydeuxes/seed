# Shared-position source-position Applicability subtraction report 001

## Result

The active inward source-position material Compare road now records, for each
exact member of `{(0, 1), (0, 2), (1, 2)}`:

```text
one exact shared-position Measurement result
+ one exact pair of its three ordered source-position coordinates
        ↓
Compare binding occurrence
        ↓
Compare Act occurrence
        ↓
Compare result occurrence
```

The Applicability binding, Applicability Act, and Applicability result are
absent. No replacement verdict, wrapper, or producer occurrence was added.

## Why Applicability failed

The shared-position Measurement result establishes exactly three ordered
source-position coordinates. Host mechanics supplies each of the three
possible two-position members. Before the removed lifecycle begins, the road
refuses any subject that lacks:

```text
one exact current shared-position Measurement result
one of the three exact ordered source-position pairs
one shared source material result occurrence
one completeness boundary
one Locality
```

After those refusals, Applicability could record only `applicable`. No negative
producer or varying applicability coordinate existed.

The later Compare result retains the independently varying fact:

```text
same-content
difference
```

That finding comes from the exact material at the two addressed source
positions. Removing Applicability loses none of its coordinates.

## Preserved physiology

The subtraction preserves:

```text
all three exact source-position pair subjects
source-position order and exact material
shared-position Measurement result reference
completeness and Locality coordinates
Compare binding before Compare Act
Compare Act before result
one result for one Compare Act
same-content / difference findings
serial delivery of one pair before its later siblings
current-coordinate replay
SQLite restart
source mutation refusal
```

The Compare Act and result contain no Applicability result reference. Current
coordinates contain no Applicability binding or result occurrence from this
active road.

## Amended binding boundary

The separate Compare binding occurrence subsequently failed its own
subtraction. See
`shared_position_source_position_compare_binding_occurrence_subtraction_report_001.md`.

The three-member construction remains separate from both removed ceremonies.
This report does not infer that the fixed host walk is a Seed occurrence.

## Finding

```text
exact source-position pair subjects          established
variable Applicability result                absent
forced positive Applicability lifecycle      failed subtraction
variable Compare finding                     preserved
separate active Compare binding occurrence   failed subtraction
```

Applicability is not a mandatory stage between one exact source-position pair
binding and its Compare Act.
