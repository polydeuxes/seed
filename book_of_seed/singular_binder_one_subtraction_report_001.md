# Singular-binder `one` subtraction report 001

## Question

When a clause states “the coordinates of one exact relation” or begins “One
exact occurrence,” does `one` count a bounded extent or introduce the exact
instance whose coordinate law follows?

## Falsifier

Replace only law-scoping singular binders with `a` or `an`.

Preserve all uses that distinguish zero, one, and multiple coordinates, bind
one exact Act to one result, select one exact alternative, or identify several
named surfaces as one occurrence.

## Result

The changed clauses still address exact instances of:

```text
Act occurrence
source boundary
relation
subject-to-Act binding
Applicability result occurrence
through-occurrence boundary
count finding
```

Repeated instances remain separately lawful and exactly addressable. The
clauses never claimed a global singleton.

## Positive controls

The pass retains:

```text
one Applicability Act for an exact binding
one result for one exact Act occurrence
one added exact occurrence
one of applicable or inapplicable
one exact source, destination, Act, and Locality coordinate where counted
one Emission occurrence and one result for several named surfaces
```

## Finding

Singular-binder `one` is ordinary scoping prose in this slice. Exact
cardinality `one` remains admitted and load-bearing.
