# Bounded Locality availability Measurement-consumer investigation 001

## Question

The bounded replay and Standing investigation recovered:

```text
material-acquisition result available through B
!= positive Standing for that result
```

This report traces every active Measurement crossing that consumes the
available acquisition-result population as though the containing runtime
object supplied its constitutional warrant.

It asks:

```text
Can the first crossing be corrected by one source filter?

Or has acquisition availability become assignment authority in several
independent consumers?
```

This investigation changes no runtime physiology.  It does not add Witness
Gate A or Gate B, does not make O1 universal, and does not remove the current
Witness campaigns.  Its purpose is to locate the complete cut before one
partial refusal makes the remaining paths harder to see.

## Sources inspected

- `seed_runtime/operator_locality_standing.py`
- `seed_runtime/standing_measurement_declarations.py`
- position-coordinate Measurement
- exact-byte Measurement
- recurrent-pair-position Measurement
- recorded-pair Measurement Compare
- operator-console pair-premise recovery
- active Witness tests that call the declaration runner directly

## 1. One positive relation surface already exists for O1

Operator material acquisition O1 records:

```text
exact material coordinate M
--Locality-->
this Seed

relation occurrence = O1 result occurrence
Locality Evidence    = O1 result occurrence under its complete O1 physiology
```

Bounded replay retains that exact source-specific relation separately in:

```text
operator_material_locality_relation_occurrences
```

The same replay also retains every exact material-acquisition result in:

```text
material_acquisition_result_occurrences
```

For current O1:

```text
result occurrence available                           yes
exact material-to-this-Seed Locality relation         yes
source-specific relation occurrence and Evidence      yes
```

For current Witness O2:

```text
result occurrence available                           yes
exact material-to-this-Seed Locality relation         no
source-specific relation occurrence and Evidence      no
```

Therefore the existing runtime can already distinguish the two surfaces
without inspecting bytes, source significance, or a host-supplied display
coordinate.

That does not complete positive assignment Standing for O1.  It does prove
that raw result availability is weaker than the Locality condition required
by `01.Source.D`.

## 2. The position-coordinate assignment is the first direct crossing

The position-coordinate source reader consumes:

```text
bounded_replay.material_acquisition_result_occurrences
```

It validates the acquisition Act/Yield/result physiology and returns every
unassigned exact result.  It explicitly establishes no Locality relation,
assignment, Applicability, Act, or Standing.

The assignment recorder then performs these checks:

```text
caller-supplied runtime object equals fresh bounded reconstruction
source identity occurs in material_acquisition_result_occurrences
source identity occurs in the unassigned-result read
```

It then appends the `01.Source.D` assignment.  Its own comment says the
membership establishes neither the missing Locality relation nor the required
subject and coordinates.

Thus the first exact laundering path is:

```text
available acquisition result
↓ renamed by containing object
current Locality Standing carries source
↓
Measurement assignment
```

No separate positive coordinate intervenes.

## 3. Exact-byte Measurement has an independent bypass

The exact-byte declaration discovery reads every identity in:

```text
material_acquisition_result_occurrences
```

and calls that ordered population its exact subject.

But the public exact-byte assignment recorder has a second path.  Its source
preparation reads every exact material-acquisition result in each declared
source Locality through the append boundary:

```text
declared Locality
↓
every exact acquisition result through B
↓
exact-byte Measurement source set
```

It does not require the acquisition result to occur in a positive Locality
relation to this Seed.  It also does not require a source-specific Standing
subject before including the result.

Therefore changing only the position-coordinate source reader would leave:

```text
Witness O2
↓ exact source-Locality scan
exact-byte Measurement assignment
```

The declaration runner would also continue admitting O2 through its own
unfiltered source-set construction unless both paths were corrected.

## 4. Later Measurements repeat the same equation

The recurrent-pair-position Measurement requires:

```text
earlier exact pair Assertion
+ later exact material-acquisition result
```

Its current assignment checks that both occur in the dictionary named current
Locality Standing.  For the later material, the decisive check is again only:

```text
result identity occurs in material_acquisition_result_occurrences
```

Its assignment reader repeats the same check when reconstructing prior
Standing.  It even derives one exact acquisition-result availability shape and
requires the replay entry to equal that shape, which proves exact
availability—not positive Standing.

Thus:

```text
exact availability coordinates
!= Standing coordinates
```

but the current error and assignment language call the former exact prior
Standing.

## 5. Compare preserves a Witness-specific provenance shortcut

The console no longer branches from supplied Witness provenance directly into
pair Measurement or Compare.  That host choreography was correctly removed.

The lower recorded-pair Compare implementation still contains a
source-specific branch:

```text
source role = this Witness
+ exact cited prior occurrence
↓
input relation = this Witness supplied occurrence provenance
```

For operator material, the neighboring branch reconstructs O1 source
coordinates and checks a prior bounded read.  For Witness material, provenance
alone names the input relation.

Preserving exact Witness provenance is lawful testimony.  It does not by
identity establish:

```text
Applicability
Participation
Compare input relation
Standing for that relation
```

Removing the console shortcut therefore did not remove every active
provenance-to-Compare crossing.

## 6. Operator pair-premise recovery also begins from availability

The console helper for existing material begins:

```text
if no material_acquisition_result_occurrences:
    no pair premise
```

It then constructs the current source population from every available result
identity.  Existing exact-byte Measurements are matched against that complete
population, or a new pair premise is recorded for it.

This is another place where:

```text
available material in Locality
```

acts as the entrance condition for later Measurement physiology.  It must not
remain as an alternate path after the declaration reader is corrected.

## 7. Active Witness campaigns depend on the compressed crossing

Several active tests intentionally record raw Witness material and then call:

```text
record_declared_measurements_from_current_standing
```

They use the resulting Measurements to build later positional, recurrence,
Compare, and Candidate testimony.  Examples include:

```text
calculator relation-path testimony
external JSON-output material testimony
carried Locality reconstruction testimony
```

Those tests contain useful external material and exact downstream mechanics.
But their current first constitutional crossing is:

```text
raw Witness acquisition availability
→ declared Measurement assignment
```

Changing one runtime predicate without reclassifying those tests would either
break them or encourage a test-only alternate physiology.  Preserving their
current expectations would preserve the very crossing under investigation.

Therefore the lawful cleanup boundary includes the active Witness tests.  They
must either stop at the now-exposed gate or later resume from a genuinely
recovered Witness acquisition and Locality physiology.  They cannot be kept
alive by manufacturing an operator O1 surrogate or a test-only Locality
relation.

## 8. The complete cut

A consistent correction must address at least:

```text
position-coordinate source discovery
position-coordinate assignment validation
exact-byte declaration subject discovery
exact-byte public source-set construction
recurrent-pair-position assignment validation
recorded-pair Compare Witness-provenance branch
operator pair-premise recovery
Witness tests that expect raw O2 to produce downstream Measurements
```

The first positive discriminator already available is narrow:

```text
O1 exact material Locality relation occurrence + Evidence
```

Using it can lawfully refuse current Witness O2 from `01.Source.D` pressure.
It cannot by itself establish positive assignment Standing or prove the entire
O1 Measurement lifecycle constitutional.

The exact immediate correction would therefore be a refusal, not a completed
bridge:

```text
acquisition result available
+ no exact material-to-this-Seed Locality relation/Evidence
↓
no 01.Source.D Measurement assignment
```

For O2, that refusal remains until both gates are recovered:

```text
Gate A
Witness acquisition Responsibility assignment + Standing
↓
lawful O2

Gate B
M --Locality--> this Seed + occurrence + Evidence
↓
exact 01.Source.D subject and coordinates in Standing
↓
Measurement assignment
```

## 9. Current disposition

```text
bounded replay exposes exact acquisition results             yes
O1 exact material Locality relation separately readable      yes
O2 exact material Locality relation                           no
position Measurement currently admits availability           yes
exact-byte Measurement independently admits availability      yes
later recurrent Measurement repeats the equation              yes
Witness provenance remains a lower Compare shortcut           yes
active Witness campaigns depend on the shortcut               yes
```

Therefore one small filter would be incomplete.  The next implementation must
remove acquisition-availability-as-Standing consistently across the complete
consumer surface, preserve raw Witness material as available testimony, and
let the affected Witness campaigns stop truthfully at Gate A or Gate B until
the missing physiology exists.
