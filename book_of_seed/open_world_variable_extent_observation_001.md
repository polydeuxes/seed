# Open-world variable-extent observation 001

## Boundary

This is a findings-only observation over the frozen Unicode-scalar aperture
population from `open_world_grammar_recovery_observation_001.md`.

It did not read the Book, machine grammar, Rosetta material, dictionaries,
language labels, grammar categories, or expected words while producing the
variable extents.  It made no Book, machine-grammar, runtime, or test amendment.

The distinction governing this report is:

```text
current Seed cannot name distinction X
!=
X has earned constitutional grammar
```

`absent from current Seed grammar` below means only that the current machine
surface has no coordinate for an observed difference.  It does not grant that
difference constitutional status.

## Bookkeeping correction

The detailed scalar report established `the _ of` in Franklin, Brown, and
Kittredge before the expanded scalar population.  The atlas later reports four
independent windows.  The fourth is real: it is the independently bounded
`corpus/latin_vulgate.txt` window, whose measured content is English and which
carries:

```text
the _ of
├── God       × 11
├── children  × 6
├── king      × 3
├── land      × 2
└── midst     × 2
```

It is not a byte/scalar view of one of the first three windows.  The atlas count
remains four; its provenance sentence was clarified.

## Frozen inputs and outputs

The scalar-aperture input has findings SHA-256:

```text
9f67758c8e2b036090aefeb3c32882eb38006f3a2c01dd369c0970027e1940b5
```

The variable-extent artifact is:

```text
/tmp/seed_open_world_variable_extents_blind.json
bytes:            3,132,471
findings SHA-256: 306b29b72fc258c36dd9a6e41e9243f52e903ab82ffbf28a727a95f3212977aa
known loss:       none
wall time:        18.548 seconds
```

The independently rendered recurrence/containment artifact is:

```text
/tmp/seed_open_world_variable_extent_relations.json
bytes:            8,281,916
findings SHA-256: 54b9cb0fe32550f005a14c08c1a396769f7049edc3fc26be30f3458bb0e91557
known loss:       none
wall time:        51.711 seconds
```

Wall timings are printed mechanics testimony and are excluded from both
canonical findings artifacts.

## Exact operation

Each recurrent scalar frame already carried exact source occurrences of:

```text
stable left material
separator scalar
varying occupant material
separator scalar
stable right material
```

For each exact occupant branch, the observer attempted one-scalar extension on
both source sides.  An enlarged surface survived only if at least two exact
source occurrences carried the same enlarged material.  Lost occurrences were
preserved as population deltas.  Branches split only when the support
population split.

No extension was chosen because its rendering looked meaningful.

## The first bottleneck exposed a compression

Literal left/right enumeration reached this boundary in Grimm:

```text
15,530 materialized extents
49.561 seconds
unfinished frontier
```

The source contains duplicated layout regions.  If one support population
shares `L` further scalars to the left and `R` further scalars to the right,
literal enumeration constructs every cell of the `L × R` extent rectangle.

That confuses:

```text
complete addressability of every contained extent
!=
every contained extent is an independently discovered branch
```

The corrected observer records one support-preserving extent family carrying:

```text
minimum / maximum left reach
minimum / maximum right reach
exact minimal and maximal material
exact source ranges
exact support occurrence identities
exact parent / child support-changing edges
```

It branches only when another scalar changes the exact support population.
Every contained extent remains addressable from those coordinates.

The compressed coordinate union was checked against literal enumeration:

```text
Franklin: 44 / 44 exact extent coordinates
Hume:    702 / 702 exact extent coordinates
```

Grimm then completed:

```text
33,388 exact addressable extents
1,156 support-changing extent families
10.624 seconds for the measured source phase
no unfinished frontier
```

The bottleneck therefore exposed a representation compression rather than a
need to cap source evidence.

## Population

The 22 recurrent scalar frames carried 51 exact occupant branches.  Every one
of those branches admitted at least one recurrent outward extension; none died
at the initial frame surface.

```text
source                                      branches  families  extents  longest
prose_franklin_autobiog.txt                        3         5       44       24
prose_hume_enquiry.txt                             8        33      702       37
grammar_goold_brown.txt                            2         2       14       21
grammar_kittredge.txt                              6        17      116       38
german_grimm_maerchen.txt                         10     1,156   33,388       73
spanish_don_quijote.txt                            2         6       58       29
latin_vulgate.txt                                 10        32    1,351       60
russian_mental_arithmetic.txt                     10        12      102       25
                                                ----     -----   ------
                                                  51     1,263   35,775
```

`longest` is a Unicode-scalar count, not a word, token, phrase, or grammar
category.

## Exact variable extents

Source evidence enlarged both layout and non-layout surfaces.  Representative
terminal material is rendered only after the populations were frozen:

```text
Franklin
    " for the defense of the "
    " the loss of "
    " the news of "

Brown / Kittredge
    " the beginning of the"
    " the death of "
    " the form of a "
    " the glory of "

German
    " Da ließ der "
    "hen. Da sprach der "
    ", und als er "

Spanish
    "pues, don Quijote que "
    " —respondió don Quijote— que "

Russian
    " по 23 руб. "
    " руб. 20 коп. "
    ". Сколько получает он за"
    " купил 28 аршин плиса и "
```

These are exact recurrent materials.  The observation establishes no ordinary
language role for any part of them.

The longest populations are duplicated layout/license surfaces, including
Project Gutenberg material and illustration spacing.  Source pressure does not
privilege the surfaces that resemble human language units.

## Cross-source pressure

Only ten exact extended materials recur across distinct source windows.  Every
one is a CR/LF layout surface shared by Grimm and Kittredge.  Their scalar
lengths are five through nine.

No non-layout extended material in this 17-window population survives exact
cross-source equality.

This does not erase the independently established `the _ of` frame.  It says
that exact whole-extent equality is a stronger operation than equality of the
frame aperture and its stable positions.

## Containment and composition

Exact frame-position occupancy occurs only in the Grimm window:

```text
empty-center space frame
↓ recurrent outward extension
layout surface
↓
exactly occupies the illustration-material position of another frame
```

After duplicate derivations are removed, 100 exact occupancy coordinates are
preserved.  They connect frame 3 branch 1 to the illustration occupants of
frame 1 branch 2 and frame 2 branch 2.

The artifact also preserves 13,500 exact extent-family ranges having at least
one strict containment inside those same illustration positions.  Extent
families can overlap, so `13,500` is a family-range count, not a unique
contained-extent population.

No language-bearing recovered frame exactly occupies another recovered varying
position in this source population.

Therefore:

```text
exact containment appeared
exact composition appeared
but the only established composition is layout physiology
```

Calling this phrase, clause, modifier, nesting, or syntax would add an
unmeasured role.

## Continuation audit

The observer finding carries exact source boundaries, decoded-scalar ranges,
stable material, varying material, support populations, and extent-family
coordinates.  It is not a durable Seed event.  It carries no responsible Act
occurrence, Yield, Locality relation, or current-Standing occurrence.

After the blind artifacts were frozen, the current runtime was checked by its
exact accepted subject coordinates:

```text
declared Measurement registry
├── one acquired-material subject for direct position Measurement
└── one exact acquired-material population for exact-byte Measurement
```

The variable-extent proof is reachable only through an explicit call carrying:

```text
direct byte-position Measurement result event identity
caller-supplied extension count
```

Its corresponding-coordinate consumer requires that proof road's exact
recurrence-result event and a carried Locality Standing value.

None of these boundaries accepts an observer scalar-frame finding.  Manually
constructing a runtime event or routing the artifact to Compare would invent
the missing responsible occurrence.

The exact stop is:

```text
frozen observer frame finding
├── exact source/scalar/support coordinates established by observer
├── no Seed Act occurrence
├── no Yield
├── no Locality / current-Standing carriage
└── no declared Responsibility addresses this subject shape
    ↓
STOP
```

This establishes an uptake vacancy in the current runtime.  It does not by
itself establish a missing constitutional distinction.

## Distinctions the current machine surface cannot presently name

The measured populations expose at least these currently unaddressed shapes:

```text
transport-byte position
!=
decoded-scalar position

one exact recurring aperture
carrying stable and varying material populations

one support-preserving bidirectional extent family
carrying an exactly addressable rectangle of contained extents

one outward extension
retaining / losing exact source occurrence identities

one exact recovered extent
occupying or being contained by another frame position
```

These are inventory entries, not proposed grammar keys.

## Finding

Source evidence does enlarge one-scalar apertures into exact asymmetric
variable extents.  It also produces exact containment and composition without
being told to seek hierarchy.  In this population, the strongest composition
belongs to duplicated layout, while the language-like extents remain separate
recurrent surfaces.

The next boundary is not vocabulary naming.  It is the absence of a responsible
Seed occurrence that can Yield one such source-derived frame so that ordinary
Applicability can decide its later uptake without a developer supplying a role.
