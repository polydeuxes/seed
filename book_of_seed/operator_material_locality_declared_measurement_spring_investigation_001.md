# Operator-material Locality / declared Measurement spring investigation 001

## Question

After one ordinary operator material occurrence O1 records:

```text
exact material M
-- locality -->
this Seed
```

what current path makes M available to the declared `01.Source.D`
Measurements?

This report does not authorize a generic Ingest surrogate, add a central
scheduler, or establish positive assignment or result Standing.

## 1. Ordinary material already discharges the declarations

The initial orientation was wrong. The ordinary console path does not stop
after O1 and its Representation.

For ordinary material `Hello\n`, the complete current runtime records:

```text
01.Source.G assignment
↓
O1 Act Evidence / Yield / exact result carrying M
↓
M --locality--> this Seed
↓
Representation
↓
generic Ingest Act Evidence / Yield / result I carrying the same bytes
↓
declared position-coordinate Measurement
↓
declared exact-byte Measurement
```

The focused witness establishes:

```text
O1 exact material occurrence                         present
M --locality--> this Seed                            present in bounded replay
generic Ingest occurrence I                          present
I identity != O1 identity                            established
I exact bytes == O1 exact bytes                      established
I provenance occurrence reference == O1              established
position-coordinate Measurement source == I          established
exact-byte Measurement source == I                    established
```

The spring fires. It fires through a second material occurrence.

## 2. The fallback console path creates the second occurrence

After the specialized `!` handling declines or finishes, the ordinary console
path calls `run_operator_ingest` with:

```text
boundary material                         = the material supplied to O1
operator material occurrence reference   = O1
```

`run_operator_ingest` rereads and validates O1, then calls `ingest_material`.
That call records a distinct Ingest Act occurrence and result. The Ingest
result retains O1 only as an exact provenance occurrence reference.

The console then advances bounded replay over I and invokes the declared
Measurement discharge. Both currently declared families therefore receive I,
not O1, as their exact source.

This road predates the O1 Locality recovery. The present work did not
reintroduce it.

## 3. O1's exact relation is now independently readable

Bounded Locality replay now carries a family-specific surface:

```text
operator_material_locality_relation_occurrences
└── O1 result occurrence
    ├── exact first-subject reference to O1.exact_material
    ├── relation: locality
    ├── second subject: this Seed
    ├── relation occurrence: O1
    └── Evidence: O1 under its exact acquisition physiology
```

This removes the mechanical discovery gap that existed when replay validated
O1 and discarded its relation. A later family can address M and its evidenced
Locality without inferring either from event kind, chronology, or a shared
locality label.

Availability in this surface establishes no Measurement Responsibility,
assignment-subject relation, Applicability, Participation, Act, or Standing.

## 4. The current declarations explain why I remains load-bearing

Active `01.Source.D` states that a declared byte Measurement counts exact byte
occurrences in exact Ingest material occurrences.

Witness Grammar names the two currently discharged subjects exactly:

```text
measurement_of_position_coordinates_of_byte_pair_occurrences
    subject = exact_Ingest_result

measurement_of_exact_byte_occurrences
    subject = exact_Ingest_source_set
```

The runtime preserves the same boundary:

```text
direct position-coordinate discovery
    reads bounded replay ingest_occurrences
    calls read_exact_ingest_result

exact-byte discovery
    reads the replayed Ingest source set
```

O1 is an exact `01.Source.G` material result. It is not an exact Ingest result
by identity. The broader `01.Source.D` conditional over exact material does
not erase the exact subject boundary of either present declaration.

Consequently, deleting I or simply passing O1 to these implementations would
break their active subject grammar:

```text
O1 material M
!= exact_Ingest_result

O1 material M
!= member of exact_Ingest_source_set
```

## 5. What the O1 → I road does and does not establish

The current road is stronger than byte equality alone:

```text
I carries exact provenance occurrence reference to O1
run_operator_ingest validates O1 before recording I
I and O1 carry equal exact bytes and source boundary
```

It remains two exact occurrences under two different Acts and results:

```text
O1 acquisition Act occurrence != I Ingest Act occurrence
O1 result identity             != I result identity
O1 Locality relation occurrence != I occurrence
```

Calling I a surrogate is Rosetta orientation. Active runtime records a real
second constitutional-looking physiology. Whether that physiology is required
or is an older compression cannot be decided from duplicated bytes alone.

## 6. Refused shortcuts

```text
O1 Locality availability → either Ingest-specific subject     refused
O1 event kind → host-selected Measurement family              refused
equal O1/I bytes → shared occurrence                           refused
O1 provenance → O1 is the Measurement subject                 refused
delete I and pass O1 into read_exact_ingest_result             refused
expand a general declaration registry for O1                   refused
result or relation occurrence → positive Standing              refused
```

## 7. Current topology

```text
operator boundary
↓
O1 exact result occurrence
├── exact material M
└── M --locality--> this Seed                    recorded/replayable
        ↓
bounded family-readable relation surface         recorded/replayable

O1 + same exact boundary material
↓ run_operator_ingest
distinct generic Ingest result I                 recorded/replayable
↓
current exact Ingest-specific declarations
↓
two declared Measurement lifecycles              recorded/replayable

────────────────────────────────────────────────────────────

O1 M as either declared Measurement subject      not established
need for the second Ingest occurrence             unresolved
positive assignment Standing                     not established
```

## 8. Next bounded question

Does active Book physiology require the second Ingest occurrence between O1
and the two current declared Measurements, or is it an older implementation
bridge from before O1 carried its exact Locality relation?

That question must be answered before changing the spring:

- If I is required, O1 Locality and I Ingest are distinct lawful stages and
  the current discharge is already connected.
- If O1 material itself is intended to be a declared Measurement subject, its
  exact subject grammar and family Responsibility must be recovered before the
  Ingest-specific implementations can change.

The current evidence warrants neither deleting I nor adding another
declaration. The smallest completed advance is narrower: O1's exact Locality
relation now remains available in bounded replay, and the pre-existing
O1-to-Ingest crossing is exposed as the next constitutional question.
