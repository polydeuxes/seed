# Support relation census 001

## Boundary

This investigation begins at `b1ef1ace`, after every live
`assertion_scope` copy was removed and the exact parent result occurrence plus
Assertion position continued to supply the affected boundary coordinates. It
changes no Book clause, Witness Grammar coordinate, runtime occurrence,
reader, or test.

The census asks separately:

```text
Are exact earlier Assertion and result references real?

Does one separately exact constitutional Support relation occur?

Does that relation address an input-to-Act position as the Book declares?
```

The DNA/RNA adversarial control is an exact dependency between physical
conditions. A later interaction can require an earlier molecular condition
without producing another object called Support. If Support is independently
real, one exact directed relation must be addressable beyond the existence of
its endpoint coordinates.

## Active Book declaration

Active Book grammar says:

```text
exact subject
Support relation
exact input-to-Act position
+ exact relation occurrence
+ exact Act
+ Scope
+ Locality
+ Unknown
```

Subject-to-Act binding also lists required support relations among its exact
coordinates.

## Runtime census

Five live runtime modules produce `input_support` dictionaries:

```text
plain-byte and byte-pair Measurement
occurrence-position Measurement
pair-position Measurement
recurrent pair-position Measurement
shared-position Measurement
```

Source-position recurrence separately carries exact
`support_result_references` and lists of supporting result occurrences.

Repository search finds no live:

```text
support identity
Support relation occurrence identity
Support event kind
Support Act
Support Yield
Support result
record Support operation
read Support operation
```

## Exact references are live

The stored references are not mere prose. They include:

```text
parent result occurrence + Assertion position
local Assertion position within one parent result
source occurrence reference
supporting recurrence-result reference
```

Readers use them to require exact earlier coordinates. Examples include:

```text
byte recurrence Assertion
    → exact count Assertion position in the same result

byte-pair count Assertion
    → exact source-material Assertion address

byte-pair recurrence Assertion
    → exact count Assertion position in the same result

recurrent pair-position Measurement
    → exact recurrent pair Assertion and its count Assertion

source-position recurrent material
    → every exact supporting result and source-position coordinate
```

Changing, omitting, reordering, or crossing those references can change or
refuse the later reading. The dependency coordinates therefore survive this
census.

## The wrapper is not an occurrence

`input_support` groups heterogeneous reference lists such as:

```text
assertion_references
local_assertion_references
occurrence_references
```

The parent Assertion occurrence supplies the first endpoint; each carried
reference supplies the addressed earlier endpoint. No additional Support
identity or event occurs between them.

The source-position road likewise carries exact supporting result references
inside another finding, binding, Act, or result. A later reader addresses the
supporting result occurrences directly.

Thus current runtime evidence establishes:

```text
exact directed dependency references    live
separate Support object                 unestablished
separate Support occurrence             unestablished
```

## Input-to-Act mismatch

The Book declares the second Support subject as one exact input-to-Act
position. The inspected runtime references instead address:

```text
earlier Assertion
earlier occurrence
earlier Measurement or Compare result
```

Some of those referenced coordinates later participate in a subject-to-Act
binding, but the Support material itself does not carry the addressed Act
identity or an input-to-Act position. Co-presence in a later road cannot supply
that missing relation.

The current Witness therefore does not establish the exact relation shape
declared by `08.Support.A`.

## Scope dependency

The Book requires Scope for each Support relation occurrence. The runtime
support references survived removal of every nested `assertion_scope` copy.
Their exact addresses depend on parent result occurrence, Assertion position,
source occurrence, Locality, and completeness boundaries—not on an
independently addressed Scope.

This does not by itself withdraw Scope or Support. It establishes that the
current Support references do not witness Scope as another relation
coordinate.

## DNA/RNA comparison

Biological consequences often depend on earlier exact conditions: a charged
tRNA, a bound factor, a processed RNA, or a prior conformational change. The
dependency is carried by the changed material or relation and by the later
interaction that requires it. Biology does not add a second certificate named
Support.

Seed's current exact references resemble those directed dependencies. They
remain useful even if the wrapper or constitutional noun later disappears.

## Smallest exact finding

```text
exact earlier-coordinate references       established
later readers requiring those references  established
Support as an additional object            unestablished
Support as a separately recorded occurrence unestablished
Support to exact input-to-Act position      no live Witness
```

The Book's current Support relation is stronger than the runtime evidence.
The runtime currently witnesses directed Assertion/result dependency
references.

## Falsifiable runtime frontier

Start with one plain-byte recurrence Assertion:

```text
parent byte-Measurement result occurrence
+ recurrence Assertion position
+ exact referenced count Assertion position
```

Pressure-test whether the `input_support` wrapper adds any discriminator beyond
that directed local reference. Preserve the count Assertion address and its
requirement by the recurrence reader. Do not erase the dependency merely
because the wrapper is suspect.

Possible exact outcomes are:

```text
the directed reference itself is the relation occurrence

or

another exact relation coordinate is missing
```

Do not replace `input_support` with a Support identity, certificate, grouping
object, or prose field. Stop if removal of the wrapper makes the direction or
endpoint role ambiguous.

