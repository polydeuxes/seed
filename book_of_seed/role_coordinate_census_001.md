# Role coordinate census 001

## Boundary

This investigation begins after `ef4e2142`, when the profiled current-coordinate
roads complete below one second without hidden replay state. It asks whether
role-shaped strings establish independently exact Seed coordinates or narrate
positions already established by subjects, Acts, occurrences, and boundaries.

The active Book declares no first-class Role object, Role occurrence, Role Act,
Role Yield, or Role result. Live role-shaped material is embedded in other
occurrences.

DNA/RNA supplies the adversarial test:

```text
remove the role word

preserve:
    exact subject
    exact structural position
    exact Act and Act occurrence
    exact Locality and boundary

ask:
    what later occurrence can no longer address or distinguish?
```

No replacement Role object or position wrapper is presumed.

## Byte-pair input role subtraction

The byte-pair Measurement repeated one constant through its Applicability
binding, Applicability Act, Applicability result, Measurement Act, and
Measurement result:

```text
input_role = exact bounded source material for position-byte Measurement
```

The exact input was already addressed by:

```text
input Assertion reference
+ Assertion Locality-movement occurrence
+ subject-to-Act binding reference
+ addressed Measurement Act identity
+ Locality
```

Removing `input_role` lost no reader, refusal, replay, Act, Yield, result, or
finding. All 92 focused byte-Measurement tests pass. The string described the
only structural input position already established by the binding.

## Earlier and later input role subtraction

Recorded-pair Compare repeated two prose values:

```text
earlier recorded pair Measurement result
later recorded pair Measurement result
```

Its Applicability binding already addresses distinct coordinates:

```text
earlier_input:
    exact subject
    addressed Compare Act

later_input:
    exact subject
    addressed Compare Act
```

The Compare binding additionally carries exact
`earlier_measurement_reference` and `later_measurement_reference` coordinates.
Removing only the two `input_role` strings preserves the complete focused
Compare Witness: 20 tests pass.

Thus these specimens establish:

```text
structural position                    exact
role prose repeating that position     not independently established
```

This finding does not establish a constitutional Position object. The named
coordinates already distinguish the two positions.

## Source role is the harder specimen

Material-source roads still record:

```text
source_role = this operator
source_role = this Witness
```

Unlike the two subtracted specimens, current readers branch on these values.
The values distinguish behavior today, but the distinction is already present
in independently exact source physiology:

```text
operator source:
    operator material source occurrence kind
    operator source Act
    operator source binding
    exact operator result occurrence

Witness source:
    Witness material source occurrence kind
    Witness source Act
    Witness source binding
    exact Witness result occurrence
```

The source result occurrence kinds are disjoint. Every inspected downstream
reader that consumes `source_role` also possesses, or can resolve from an exact
reference, the source result occurrence:

```text
operator invocation Locality reader
supplied Witness source reader
current-coordinate material-result read
position-coordinate Measurement read
recurrent-position availability read
recorded-pair Compare input read
```

The current-coordinate projection nevertheless copies `source_role` beside
the exact result occurrence identity, and later roads copy it again. Recorded-
pair Compare then branches on the copied string even though it already resolves
the exact added material-result occurrence.

This is strong evidence that source route is real while `source_role` may be a
compiled narration of the route. It is not yet a completed subtraction: active
readers must first discriminate the exact source occurrence kind directly.

## DNA/RNA testimony

A molecular participant need not carry a certificate naming its role when its
exact position in an interaction already distinguishes it. An entering tRNA,
bound substrate, or source molecule is distinguished by its exact structure,
location, and interaction coordinates.

This testimony does not erase functional distinction. Operator and Witness
source routes remain different exact physiologies. It only refuses the inference:

```text
different source routes
→ independently real source_role string
```

## Smallest exact finding

```text
byte-pair input role prose                 subtracted
earlier/later input role prose             subtracted
exact structural positions                preserved
operator/Witness source distinction        established
independent source_role coordinate         not established
source_role-dependent runtime branches     still live
```

## Next falsifiable frontier

Proceed one source consumer at a time:

```text
exact source result occurrence identity
→ resolve exact occurrence
→ discriminate operator or Witness source occurrence kind
```

First remove copied `source_role` from current-coordinate projections and
downstream reads where the exact source result occurrence is already present.
Then remove `source_role` from operator and Witness binding, Act, and result
material only after every consumer follows the exact source occurrence.

Preserve the distinct operator and Witness source Acts, bindings, occurrences,
boundaries, Localities, exact material, loss, Unknown, and source-occurrence
references. Do not merge the two source roads and do not introduce Role,
Position, source classification, or another bucket.
