# `each` and `both` subtraction report 001

## Question

Do `each` and `both` establish exact Book coordinates beyond the exact
subjects, references, and boundaries named by their clauses?

## `each`

The active uses were replaced as follows:

```text
Each exact subject of one Act
→ the exact subjects of one Act

each Measurement result reference
→ the Measurement result references

each pair-position result reference
→ the pair-position result references

one boundary coordinate of each exact position
→ one boundary is a coordinate of every exact position

one count finding preserves each occurrence reference
→ one count finding preserves its exact occurrence references
```

The exact plural coordinates preserve the complete addressed references. No
member-walk Act, distribution occurrence, or additional result is lost.

## `both`

The two uses followed two exact subjects named immediately beforehand:

```text
both results
→ the earlier and later exact results

both
→ the same-position result position and recorded pair-finding Compare result
```

The replacement names the exact coordinates rather than pointing backward
through a generic two-item word.

## Finding

```text
exact subjects                         survive
exact result references                survive
common through-occurrence coordinate   survives
complete finding references            survive
per-member addressability              survives
`each` coordinate                      absent
`both` coordinate                      absent
```

`each` and `both` were connective English. They leave active Book admission.
No `Distribution`, `Collection`, `Group`, or two-subject wrapper is added.
