# Plain-byte source Assertion subject investigation 001

## Boundary

This investigation begins at `45fe9fa0`, after copied Rule prose was removed
from three adjacent Measurement families. It changes no Book clause, Witness
Grammar coordinate, runtime occurrence, reader, or test.

The investigation asks what exact subject exists before plain-byte
Measurement and whether `BYTE_MEASUREMENT_RULE` currently masks that subject
inside the produced `exact_source_material_set` Assertion.

The DNA/RNA adversarial control is:

```text
description of an operation
!=
material on which the operation acts
```

The test keeps separate the exact material-result subjects, completeness
boundary, Scope, Locality, and produced finding.

## Exact subject before the Act

The plain-byte subject-to-Act binding already carries this subject:

```text
subject_reference:
    source_occurrence_references:
        exact material-result occurrence references
```

Those references are recovered from exact material results in the declared
source Localities through one exact occurrence boundary. The binding carries
the completeness boundary, Scope, Locality, Act, occurrence, and result
coordinates separately.

Thus the runtime already answers:

```text
what exists before the Measurement Act?
→ the exact material-result occurrence references
```

No Rule string is needed to address those subjects.

## Produced source Assertion

The first produced Assertion currently reverses those roles:

```text
assertion_subject:
    measurement_rule: BYTE_MEASUREMENT_RULE

dimensions.content:
    source_material:
        exact material-result occurrence references
    completeness_boundary:
        exact boundary identity
```

The Rule prose is therefore the only coordinate in the subject dictionary,
while the exact pre-Act subjects are presented as result content.

No live consumer interprets the Rule prose as the Assertion subject. The
downstream byte-pair road addresses this Assertion by its recorded result
occurrence and result-local Assertion position. It then reads:

```text
source material-result occurrence references
completeness boundary
Assertion Scope / source Localities
```

from the Assertion's exact carried coordinates. The public
`RecordedByteAssertion.content` is `None` for this source Assertion because it
only projects `assertion_subject.content`; byte-pair Applicability does not use
that empty projection.

## Coordinate separation

The source material references and completeness boundary are both exact, but
they do different work:

```text
material-result occurrence references
    the exact subjects measured

completeness boundary
    the exact cut through which those subjects are exhaustive

Scope / Locality
    where the subjects and boundary are carried

Measurement findings
    exact byte counts and Recurrence produced by the Act
```

Co-presence in `dimensions.content` does not make the boundary part of the
subject. Conversely, the binding already demonstrates that the material-result
references can be the exact subject while the boundary remains separate.

## DNA/RNA pressure

A molecule acted on by an enzyme is the subject of the interaction. A prose
description of the enzyme's stable behavior is not that subject. Boundaries
and local conditions can constrain the interaction without becoming the
molecule.

The plain-byte road has the same exact shape:

```text
exact material-result occurrences
+ exact boundary and Locality
+ exact byte-Measurement physiology
→ byte findings
```

`BYTE_MEASUREMENT_RULE` describes the last line. It does not supply the first.

## Smallest exact finding

```text
plain-byte pre-Act subject
    exact material-result occurrence references

completeness boundary
    separate exact coordinate

Scope / Locality
    separate exact coordinates

BYTE_MEASUREMENT_RULE as Assertion subject
    copied operation prose masking an already-carried subject
```

The source Assertion's subject coordinate is not actually vacant. The exact
subject was established before the Act and is already carried by the binding;
the produced Assertion fails to present it there.

## Falsifiable runtime frontier

On the plain-byte road only:

1. present the exact material-result occurrence references as the source
   Assertion subject;
2. keep the completeness boundary, Scope, and Locality separate;
3. subtract `BYTE_MEASUREMENT_RULE` from that Assertion subject;
4. preserve result-local Assertion addressing, source support, exact replay,
   byte findings, downstream byte-pair Applicability, and current-coordinate
   reads;
5. introduce no source-set identity, Rule replacement, wrapper, hash, or
   compound subject.

If any live reader independently addresses the Rule prose, stop at that exact
relation. Otherwise the source Assertion should expose the subject that the
Measurement binding already carries.
