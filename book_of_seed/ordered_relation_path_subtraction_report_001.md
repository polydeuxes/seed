# Ordered Relation Path Subtraction Report 001

## Question

Does `Path` establish anything beyond the exact recorded result occurrence,
result-local position, ordered input references, same source-position
coordinate, source material result, completeness boundary, and Locality?

This report retains `Path` in its title as the word under subtraction. It does
not admit Path.

## Specimen 1: same-position Measurement

The Measurement result previously recorded:

```text
shared-position Measurement result occurrence
└── ordered_relation_path
    ├── result_position = 0
    ├── first pair-position result reference
    ├── second pair-position result reference
    ├── same source-position coordinate
    ├── source material result occurrence
    └── completeness boundary
```

No independently occurring Path accompanied that dictionary:

```text
Path identity       not found
Path occurrence     not found
Path Act            not found
Path result         not found
Path relation       not found
later Path address  not found beyond parent result + position 0
```

The nested dictionary was result content at one already exact local position.
It is now recorded as:

```text
same-position Measurement result occurrence
└── result_positions
    └── position 0
        ├── first pair-position result reference
        ├── second pair-position result reference
        ├── same source-position coordinate
        ├── source material result occurrence
        └── completeness boundary
```

This is the same addressed-content physiology recovered during Assertion
subtraction:

```text
exact containing result occurrence
+ exact local result position
→ exact addressed result content
```

No second identity is needed for that content.

## Specimen 2: Compare against recorded pair findings

`04.Compare.B` previously described one ordered relation-path result position
and one recorded pair-finding Compare result.

The live reader already demanded the complete same-position Measurement result
and verified:

```text
result position 0
first pair-position result reference
second pair-position result reference
one equal source-position coordinate at their joint
exact ordered pair contents
source material result occurrence
completeness boundary
Locality
```

The Compare binding now addresses that exact result position directly:

```text
same-position Measurement result occurrence
+ result position 0
+ recorded pair-finding Compare result occurrence
→ exact Compare subject-to-Act binding
```

The recorded fields now name what they address:

```text
shared_position_measurement_result_reference
shared_position_result_position_reference
pair_position_result_reference
```

They no longer copy a Path object or describe a path-position object.

The Book clause and Witness Grammar now say `same_position`, not `shared`.
The exact equality of the two input position-coordinate references establishes
the sameness. Removing `Path` did not cause another adjective to enter Book
admission.

## Specimen 3: source-position Compare

The older source-position Compare family independently supplied a positive
control. It previously said that one ordered Path contained three source
positions and enumerated these pairs:

```text
(0, 1)
(0, 2)
(1, 2)
```

The reader obtains the three exact coordinates directly from the
same-position Measurement result:

```text
first input's first source position
the equal source position
second input's second source position
```

Each Compare binding then addresses one exact pair of indices into those three
coordinates. No Path identity or occurrence participates.

The family now records:

```text
same-position Measurement result reference
same-position result-position reference
first exact source-position coordinate
second exact source-position coordinate
first source-position index
second source-position index
```

All three position pairs remain independently compared. No selected pair,
unordered structure, or adjacency claim was added.

## Order survives subtraction

Removing Path does not remove order.

The following remain exact and independently refused when changed:

- first and second pair-position result references;
- first, same, and last source-position coordinates;
- result-local position 0;
- source occurrence identity;
- exact byte-pair content;
- source-position pair indices;
- result occurrence order and Locality where read.

Equal local shapes in separate parent result occurrences remain separate exact
addresses. Reversing the exact pair contents remains a different ordered
result. The subtraction removes no coordinate that established either fact.

## Runtime subtraction

The active families now name the existing physiology directly:

```text
comparison_of_shared_position_measurement_with_recorded_pair_findings
comparison_of_shared_position_source_position_material
source_position_determination_and_comparison
```

Their event kinds and durable material fields changed with the subtraction.
No compatibility wrapper preserves the retired Path-shaped material.

Downstream Distinction Measurement, Locality Movement, exact-material reading,
exact-result subject reuse, current-coordinate replay, and SQLite reopen now
resolve the direct same-position result coordinates.

## Lexical boundary

`path` is removed from active Book admission. Active Book clauses and Witness
Grammar contain no Path coordinate.

Filesystem paths, host command bytes such as `cat path`, and historical reports
are external or testimony uses of the ordinary English word. They do not
establish Seed Path physiology.

`expose` also adds no coordinate. One newly introduced test name was corrected
to `read`, and the one active runtime validation docstring now says it reads
the occurrences. No Expose Act or occurrence was introduced.

## What is not established

The subtraction establishes no:

- Path identity, occurrence, Act, result, relation, or wrapper;
- unordered pair content;
- adjacency beyond exact positions;
- continuation or successor relation;
- cross-boundary source adjacency;
- generic Address object;
- semantic sequence, expression, DNA fragment, or rule; or
- new constitutional word replacing Path.

## Disposition

Withdraw Path from this live physiology.

Keep:

```text
exact containing Measurement result occurrence
+ exact result position 0
+ exact pair-position references
+ one equal source-position coordinate
+ exact source and boundary coordinates
```

Later work may compose those exact coordinates directly. It must not recreate
Path merely because three positions can be read in order.
