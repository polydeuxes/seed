# Quantification closure census 001

## Scope

This census closes the active campaign over:

```text
each
both
every
one
```

It introduces no quantifier, set, iteration, progress, or completion
occurrence.

## Subtracted words

`each` and `both` leave active Book admission.

Their exact content survives as:

```text
exact plural subjects and references
the earlier and later exact results
the same-position result position and recorded pair-finding Compare result
one common exact boundary coordinate where established
```

No independently variable coordinate was carried by either word.

## `every`

`every` survives as bounded exhaustion.

The interruption control distinguishes:

```text
1 recorded Act / 4 exact pairs   incomplete
9 recorded Acts / 9 exact pairs  exhaustive
```

The selected boundary determines the addressed extent. Later coordinates do
not enter an earlier reading. No durable work extent or completion occurrence
is implied.

## `one`

Active `one` uses fell from 76 to 31 after predicate-role, singular-binder,
and relation-binder subtraction.

The residual uses have positive exact controls:

```text
one exact Act bound to addressed subjects
one Applicability Act for an exact binding
one result occurrence for an exact Act occurrence
one earlier and one later exact Compare input
one exact result in each of two distinct Compare input roles
one added exact source occurrence
one exact coordinate of each kind in a binding tuple
one of applicable or inapplicable
several named Emission surfaces are one occurrence and one result
```

Changing those cardinalities changes the relation, address, result refusal, or
identity claim. `one` therefore remains admitted.

No residual use claims one global Act, result, subject, boundary, relation,
position, or Locality.

## Final disposition

```text
each   connective distribution prose        withdrawn
both   backward pointer to two coordinates  withdrawn
every  exact bounded exhaustion              survives
one    exact local cardinality or identity   survives
```

Qualified coordinate domains remain load-bearing:

```text
one earlier result
!=
one added occurrence
!=
one Applicability answer
!=
one result-local position
```

No quantifier word supplies a selection occurrence, global singleton, frozen
work extent, or future completeness claim.
