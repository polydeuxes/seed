# Cross-surface relation multiplicity observation 001

## Boundary

Disposable observer and focused checks only. No active Book clause, Witness
Grammar coordinate, runtime road, or source-position rule changes.

`6bc61d6f` found no active owner for the exact one-to-one relation-extension
rule used by the cross-surface observer. This observation pressure-tests a
prior assumption:

```text
did the source material establish one-to-one correspondence,
or did Python require it before the material could answer?
```

The operation does not replace bijection with one preferred looser rule. It
walks all four exact directional conflict rules against every source-supported
frame relation:

```text
same first + changed second       conflicting | permitted
changed first + same second       conflicting | permitted
```

The four combinations are retained together. No combination receives a
preferred name, source, relation, orientation, or result.

## 1. Direct finding

The earlier arithmetic material does not establish the complete bijection
rule used by `382dcf52`.

Three different directional rules each produce the same two exact
continuations:

```text
changed first + same second conflicts only       2
same first + changed second conflicts only       2
both changes conflict                            2
```

Permitting both directional changes produces 240 continuations rather than
two.

Thus the material constrains relation extension, but it does not distinguish
which of the three narrower directional rules governs the two familiar
continuations.

The new many-to-one material gives the complementary result:

```text
both changes conflict                            0
one directional change conflicts                 3 in its answering orientation
the opposite directional change conflicts        3 in the reversed orientation
neither directional change conflicts             20
```

Therefore:

```text
one-to-one behavior in the arithmetic witness
is a property compatible with that exact material

!=

one universal relation-extension rule established by the material
```

The observer's complete bijection requirement was a family-local authored
rule. It was not a source-derived finding.

## 2. Where the earlier observer required bijection

`scripts/observe_cross_surface_frame_continuation.py` requires bijection in
four separate operations.

### 2.1 Anchor admission

`_exact_mapping()` refuses either:

```text
same first + changed second
changed first + same second
```

before one orientation can be walked.

### 2.2 Row compatibility

`_row_can_answer()` applies both restrictions again while addressing later
rows.

### 2.3 Row pairing

`_row_pairings()` permits each later row to answer at most one first row.

### 2.4 Coordinate extension

`_walk_coordinates()` again refuses both directional changes while adding
material pairs.

These rules are generic with respect to arithmetic and English material, but
they are not supplied by the exact endpoint relation. Python authored them.

## 3. Blind operation

The new observer begins with raw bytes and reuses the earlier blind source
physiology:

```text
raw material
-> every recurring-byte division
-> every rectangular projection
-> every stable-middle frame
-> every exact or reversed endpoint relation
```

It is not given row boundaries, item boundaries, source meanings, desired
frames, desired material values, or a preferred multiplicity rule.

For each source-supported endpoint relation, it then performs every one of
these exact rules:

| same first, changed second | changed first, same second |
| :---: | :---: |
| permitted | permitted |
| permitted | conflicting |
| conflicting | permitted |
| conflicting | conflicting |

For each rule it:

```text
orients the exact anchor pairs
-> addresses projections carrying both relation sides
-> addresses rows capable of answering the carried relation
-> walks exact coordinates
-> adds every compatible exact pair
-> retains every complete relation
```

The frozen artifact contains hashes and exact rule booleans. It contains no
plain source material or post-freeze human reading.

## 4. Many-to-one source material

The new source material uses opaque multi-byte occupants and ordinary bytes
that the observer is not told to treat as separators or relation content.

Post-freeze spacing and symbols are shown only so a human can audit the
material:

```text
larger first surface

qa : qa = td
rb : rb = td
sc : sc = td

larger later surface

zu ; zu ~ vx
zu ; zu ~ vx
zu ; zu ~ vx

relation-bearing surface

qa @ zu
rb @ zu
sc @ zu

reversed relation-bearing surface

zu # qa
zu # rb
zu # sc
```

The blind frame operation recovers one exact frame relation. Its forward
orientation carries:

```text
qa -> zu
rb -> zu
sc -> zu
```

The reverse orientation carries:

```text
zu -> qa
zu -> rb
zu -> sc
```

No human reading distinguishes either orientation. The exact directional rule
determines which orientation can be walked.

## 5. Frozen result

```text
artifact
/tmp/seed_cross_surface_relation_multiplicity_blind.json

SHA-256
e7a84e0ae3935923f278a66b2a6d32ce5579a2ce840654d1ab0761f20d1e46d7

artifact bytes
1,241,084

known loss
none

wall time
0.077 seconds
```

The observer considers three raw-material groups:

```text
group 0    earlier arithmetic four-surface material
group 1    many-to-one larger surfaces plus both relation-bearing surfaces
group 2    only the two many-to-one relation-bearing surfaces
```

Each group produces one exact frame relation.

## 6. Exact results

### 6.1 Earlier arithmetic material

| same first changed second | changed first same second | orientations | continuations | complete relation pair counts |
| :---: | :---: | ---: | ---: | --- |
| permitted | permitted | 2 | 240 | `8, 10..16` |
| permitted | conflicting | 2 | 2 | `8` |
| conflicting | permitted | 2 | 2 | `8` |
| conflicting | conflicting | 2 | 2 | `8` |

The familiar two continuations each carry:

```text
three source-supported anchor pairs
+ five added pairs
-> eight-pair complete relation
```

All three rules carrying at least one directional restriction produce those
same two findings. The arithmetic material therefore does not discriminate
full bijection from either one-directional rule.

The unrestricted rule adds 238 other continuations with complete relation
sizes from 8 through 16. Thus merely replacing one-to-one with unrestricted
pair accumulation does not recover a clean general rule.

### 6.2 Many-to-one material

| same first changed second | changed first same second | orientations | continuations | complete relation pair counts |
| :---: | :---: | ---: | ---: | --- |
| permitted | permitted | 2 | 20 | `6..9` |
| permitted | conflicting | 1 | 3 | `6, 8` |
| conflicting | permitted | 1 | 3 | `6, 8` |
| conflicting | conflicting | 0 | 0 | none |

The full bijection rule refuses the relation before a coordinate walk begins.
The two one-directional rules each retain exactly the orientation compatible
with that rule.

This result is source-structural. The observer has no rule saying that one
orientation is the true reading.

### 6.3 Relation-bearing surfaces alone

With only the two relation-bearing surfaces:

| same first changed second | changed first same second | continuations |
| :---: | :---: | ---: |
| permitted | permitted | 4 |
| permitted | conflicting | 0 |
| conflicting | permitted | 0 |
| conflicting | conflicting | 0 |

The unrestricted rule treats the two bridge surfaces as answering larger
continuations of one another. Every directional restriction refuses those
walks.

This is an important negative result:

```text
carried exact relation
+ proposed exact pair
-> always add the pair
```

is too weak. It promotes additional structure that the earlier exact rules
kept separate.

## 7. What the operation establishes

The operation establishes:

```text
the earlier arithmetic result depends on an authored multiplicity rule

the arithmetic material does not distinguish full bijection
from either one-directional restriction

many-to-one exact material can support complete continuations
that full bijection refuses

unrestricted exact pair growth creates many additional continuations

the exact family-local compatibility rule changes the lawful result
```

It does not establish:

```text
one preferred multiplicity rule
one active Seed relation-extension rule
one universal relation-update Act
one true orientation
arithmetic or language meaning
```

## 8. Row relation remains a separate authored coordinate

This pressure test changes only material-pair multiplicity. It retains the
earlier observer's complete one-to-one row pairing.

The many-to-one later surface contains three exact row occurrences with the
same exact material. Each later row occurrence answers one first row
occurrence. Therefore:

```text
first material occupants -> one later material occupant
```

is many-to-one while:

```text
first exact row occurrences -> later exact row occurrences
```

remains one-to-one in this observer.

This distinction prevents the result from being read as a general recovery of
arbitrary relation multiplicity. Row pairing requires its own pressure test.

## 9. Corrected interpretation of `6bc61d6f`

`6bc61d6f` correctly found that no active Seed road owns the observer's
complete one-to-one extension rule.

The present result narrows how that vacancy should be read:

```text
absent:
    this observer family's exact bijection rule

not established as absent:
    one universal relation-extension physiology that must be bijective
```

The common organization remains conditional:

```text
exact carried relation
+ exact proposed pair
+ exact family-local rule

-> compatible | conflicting | Unknown

compatible extension
-> exact result carrying the enlarged relation
```

The family-local rule determines whether either directional repetition is
compatible. Relation extension cannot supply that answer for itself.

## 10. Remaining authored apertures

The observer still relies on authored operations that are not active Seed
work:

```text
recurring-byte source division
rectangular projection
stable-middle frame
frame-relation production
the four multiplicity rules
complete row pairing
relation-to-projection coordinate walk
```

Running all four multiplicity rules avoids preferring one answer. It does not
make those rules active grammar.

The source grouping also remains an observer aperture. This experiment does
not repair the acquisition/material-continuity distinction or establish that
arbitrary packaging yields the same frame relation.

## 11. Disposition

Do not add one-to-one relation extension to active grammar.

Do not replace it with unrestricted relation extension.

Do not infer a multiplicity rule merely from the absence of a conflict in one
small source surface. In group 0, three unlike directional rules produce the
same narrow result.

The smallest next question is:

```text
what exact later consequence can distinguish
which family-local multiplicity rule governs one carried relation?
```

That question must begin with all answering rules still addressable. The next
source-derived consequence may refuse some of them. Python must not choose a
rule because its result resembles the human reading.

Until such a distinction is recovered, the exact multiplicity rule remains
Unknown.
