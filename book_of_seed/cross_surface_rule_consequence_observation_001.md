# Cross-surface rule consequence observation 001

## Boundary

Disposable observer and focused checks only. No active Book clause, Witness
Grammar coordinate, runtime road, source-position rule, or Candidate family
changes.

`3efddbc6` established:

```text
the arithmetic material does not distinguish
full bijection from either one-directional restriction
```

All three exact rules produce the same familiar two eight-pair relations. This
observation asks:

```text
can later raw material distinguish those rules
while the carried relation remains exactly the same?
```

The operation retains unrestricted growth as a fourth rule. It chooses no
rule, orientation, source meaning, or preferred continuation.

## 1. Direct finding

Later source material can distinguish exact directional conflict rules that
the initial arithmetic material cannot distinguish.

The operation first intersects complete relations across all four
multiplicity rules. It recovers exactly two shared relations: the two
orientations of the same eight exact material pairs.

Thus every later rule walk begins with identical relation content:

```text
same exact carried relation R
+ same later raw material M
+ different exact rule F
-> different exact consequence
```

The later controls establish:

```text
unchanged material
    all three directional rules remain compatible

same first + changed second
    only the rule permitting that direction extends

changed first + same second
    only the rule permitting that direction extends

both directional repetitions
    all three restrictive rules refuse
    unrestricted growth extends broadly
```

This is the first cross-surface observer result where later material
distinguishes rules over one unchanged carried relation.

It does not establish one governing rule. Unrestricted growth can remain the
only supplied rule that answers while producing 120 extensions in each
orientation. Last remaining among four authored alternatives is not the same
as warranted.

## 2. Exact causal order

The observer does not begin by choosing one multiplicity rule's result.

### 2.1 Initial relation production

It runs the `3efddbc6` operation over the original four raw arithmetic
surfaces under all four directional rules:

```text
same first + changed second       permitted | conflicting
changed first + same second       permitted | conflicting
```

Each result carries complete relation pair hashes.

### 2.2 Exact intersection

The observer intersects those complete relation pair sets across all four
rules. A relation continues to the later experiment only when its entire exact
pair set was produced under every rule.

The result is:

```text
complete relations shared by all four rules    2
```

Post-freeze, they are the first-to-later and later-to-first orientations of
the same eight material pairs.

No continuation count, human reading, or desired orientation chooses them.
Their exact pair-set equality does.

### 2.3 Later consequence walk

For each shared relation, each later raw-material group is projected through
the same blind recurring-byte operation. Every multiplicity rule receives:

```text
same carried relation pairs
same projection surfaces
same exact row occurrences
same coordinate order
```

Only the directional conflict rule differs.

Each walk records separately:

```text
addressed projection pairs
addressed row pairings
compatible complete walks
walks adding at least one exact relation pair
complete resulting relation
```

## 3. Later raw-material controls

The observer receives four groups by position. The byte material itself has no
group label in the frozen artifact.

### 3.1 Unchanged control

The first group repeats the original symbolic and written larger surfaces.

Post-freeze rendering:

```text
1 + 1 = 2                 one plus one equals two
1 + 2 = 3                 one plus two equals three
2 + 2 = 4                 two plus two equals four
2 + 3 = 5                 two plus three equals five
3 + 3 = 6                 three plus three equals six
```

### 3.2 Same first, changed second

The first surface remains unchanged. The later surface retains one carried
pair and presents a changed later occupant at the same coordinate in the
other rows:

```text
one plus one equals two
one add two equals three
two add two equals four
two add three equals five
three add three equals six
```

The observer is not told that `plus` and `add` have a human relation.

### 3.3 Changed first, same second

The later surface remains unchanged. The first surface retains one carried
pair and presents a changed first occupant at the same coordinate in the
other rows:

```text
1 + 1 = 2
1 * 2 = 3
2 * 2 = 4
2 * 3 = 5
3 * 3 = 6
```

The observer is not told that `+` and `*` have human meanings.

### 3.4 Both directional repetitions

The final group carries both distinctions in one bounded pair of raw
surfaces:

```text
1 + 1 = 2                 one plus one equals two
1 + 2 = 3                 one add two equals three
2 * 2 = 4                 two plus two equals four
2 * 3 = 5                 two add three equals five
3 * 3 = 6                 three add three equals six
```

The source arrangement forces:

```text
same first + changed second
changed first + same second
```

within one complete aligned walk.

## 4. Frozen result

```text
artifact
/tmp/seed_cross_surface_rule_consequence_blind.json

SHA-256
ba1b7f758b534844a620f9d1d87238d9fc63648092d5446598e5ef40f4af6046

artifact bytes
4,662,751

initial shared relations
2

known loss
none

wall time
0.316 seconds
```

The artifact carries source hashes, projection identities, shared relation
identities, exact rule coordinates, addressed row pairings, added pair hashes,
and complete relation hashes. It carries no plain source material or
post-freeze human reading.

## 5. Unchanged consequence

For each of the two shared relation orientations:

| same first changed second | changed first same second | compatible walks | extensions |
| :---: | :---: | ---: | ---: |
| permitted | permitted | 120 | 119 |
| permitted | conflicting | 1 | 0 |
| conflicting | permitted | 1 | 0 |
| conflicting | conflicting | 1 | 0 |

The three directional rules each retain one complete compatible row walk and
add nothing. The same eight relation pairs already answer the unchanged
material.

The unrestricted rule admits all 120 row permutations. Only one uses the
unchanged eight pairs without extension. The other 119 add between four and
eleven relation pairs.

Thus unchanged material preserves all three narrow rules while already
showing that unrestricted growth has a much broader consequence.

## 6. One directional repetition

For the same-first/changed-second group, the two shared orientations produce
opposite directional readings.

Across the two orientations:

```text
rule permitting same-first/changed-second
    extension counts    1, 0

rule permitting changed-first/same-second
    extension counts    0, 1

full bijection
    extension counts    0, 0

unrestricted
    extension counts    120, 120
```

The changed-first/same-second group reverses which shared orientation answers:

```text
rule permitting same-first/changed-second
    extension counts    0, 1

rule permitting changed-first/same-second
    extension counts    1, 0

full bijection
    extension counts    0, 0

unrestricted
    extension counts    120, 120
```

This is an exact orientation result, not a human choice. The same physical
relation read in the opposite direction exchanges which multiplicity
restriction is encountered.

## 7. Both directional repetitions

For each shared orientation:

| same first changed second | changed first same second | compatible walks | extensions |
| :---: | :---: | ---: | ---: |
| permitted | permitted | 120 | 120 |
| permitted | conflicting | 0 | 0 |
| conflicting | permitted | 0 | 0 |
| conflicting | conflicting | 0 | 0 |

Both one-directional rules and full bijection refuse before a complete walk.
Only unrestricted growth answers.

That result does not make unrestricted growth the governing rule. It produces
120 different complete extensions per orientation, adding between three and
fourteen pairs. The source has distinguished compatibility from conflict, but
has not narrowed the compatible consequence to one exact continued relation.

Therefore:

```text
one rule remains compatible among the supplied four
!=
one exact rule is warranted
```

## 8. What later consequence can do

The experiment establishes this observer-level operation:

```text
same exact carried relation R
+ multiple exact rules F
+ later exact material M

-> some F carry no complete compatible walk
-> some F carry one complete compatible walk
-> some F carry many complete compatible walks
```

This distinguishes three outcomes that the earlier binary phrasing compressed:

```text
refused by this rule
compatible without extension
compatible with one or more extensions
```

The number and exact contents of extensions remain part of the consequence.
Compatibility alone does not establish that one rule governs R.

## 9. Scope remains load-bearing

The later controls should not be collapsed into one universal verdict.

The same-first/changed-second material admits one directional rule in one
orientation. The changed-first/same-second material admits the other. Material
carrying both repetitions admits only unrestricted growth, and that rule
produces a broad result.

Therefore one exact relation can participate in unlike multiplicity behavior
under unlike exact source surfaces. This observation does not establish
whether those surfaces share one Scope or require different Responsibilities.

Any later active road would have to carry that exact boundary. A rule cannot
be rejected globally merely because it refuses one unlike source surface.

## 10. What remains authored

The observer still supplies:

```text
recurring-byte source division
rectangular projection
stable-middle frame
frame-relation production
the four directional rules
complete row pairing
relation-to-projection coordinate walk
intersection across complete relation pair sets
```

The operation makes no active Seed occurrence for any of these positions.

Generic `04.Compare` can preserve distinctions after one exact Responsibility
already carries exact subjects and an exact rule. Active Seed still has no
Responsibility branch carrying these observer subjects and rules.

The result therefore demonstrates rule consequence discrimination. It does
not create rule Standing, rule Candidates, Applicability, Participation, or a
Compare occurrence.

## 11. Direct disposition

Do not choose bijection because it matches the original human reading.

Do not choose either one-directional rule because one later surface admits it.

Do not choose unrestricted growth merely because it is the only supplied rule
that can answer both directional repetitions.

Do not add `Hypothesis`, multiplicity grammar, rule choice, or a scheduler.

The smallest exact distinction recovered is:

```text
later exact consequence
can distinguish exact rules over one unchanged carried relation

but

rule compatibility
!= rule warrant
```

The next question is not which rule wins. It is:

```text
what responsible source-derived occurrence can establish
that one exact rule governs one exact relation under one exact boundary?
```

Until that occurrence is recovered, each rule's relation to the carried
material remains Unknown.
