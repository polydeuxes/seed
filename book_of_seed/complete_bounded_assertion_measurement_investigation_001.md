# Complete bounded Assertion Measurement investigation 001

Investigation only. No active Book, witness grammar, runtime, Rosetta,
admission, or test amendment.

## Question

The calculator relation witness currently carries two independently
established subjects:

```text
calculator-claim Locality
    one recorded comparison finding around exact byte 0x3d

system invocation Locality
    one recorded position Assertion for exact calculator output b"4\n"
```

The preceding investigation asked whether this Seed bears the Responsibility
and Authority to move those two Assertions into one destination Locality.

This investigation reopens that premise. Before asking who may move or compare
the two Assertions, it asks what established them as the material to consider
together.

The alternative topology under investigation is:

```text
complete exact material under one rule and boundary
        |
        v
one bounded Measurement result
        |
        +-- exact source reference and coordinates
        +-- exact source reference and coordinates
        +-- every other exact member required by the rule
```

The result would be one local subject. Its source references would not establish
that the referenced Assertions moved into the result's Locality.

The words `table`, `population`, `fan-out`, `discriminator`, and `comparator`
are explanatory here. This investigation does not recover them as
constitutional kinds.

## Method

The investigation tests:

1. active Book material first;
2. active witness grammar as Fidelity testimony, not Book Authority;
3. current runtime as implemented testimony, not constitutional warrant;
4. earlier complete-enumeration work as chronology and bounded precedent;
5. whether removing the developer-selected pair leaves an exact subject,
   rule, boundary, and source relation;
6. what each candidate would establish and what it would not establish.

No usefulness claim supplies a missing Responsibility, input relation,
Applicability, or Authority.

## 1. The developer-selected pair is circular

The current calculator construction selects:

```text
A = the path-comparison finding around 0x3d
B = the calculator position Assertion carrying b"4\n"
```

It then moves A and B into one destination Locality.

The construction proves useful preservation properties, but usefulness does
not answer:

```text
why A and B rather than every other available Assertion?
```

The operator, developer, and reader know that `=` and `4` may matter together.
No established Seed finding currently supplies that significance.

Therefore:

```text
select A and B because their later comparison looks useful
!=
an exact input subject established before that selection
```

This does not prove either movement invalid in every use. It proves that the
two-movement construction does not independently recover why these two source
Assertions are the complete material for the mathematical-language road.

## 2. Active complete-enumeration precedent

### 2.1 Exact-byte Measurement

The strongest active precedent is the exact-byte Measurement.

Its rule is not `read useful bytes`. It is:

```text
explicitly named source Localities
+ every exact Ingest occurrence in those Localities
+ one append completeness boundary
+ every exact byte occurrence carried by those Ingest occurrences
```

Runtime measures every Ingest occurrence of the required kind in each named
Locality through that boundary. It refuses an absent Locality, an empty exact
source set, corruption, and duplicate entry of one Ingest occurrence.

It records one result in one recording Locality while preserving references to
source Ingest occurrences from the declared source Localities.

This establishes an important topology:

```text
many exact source occurrences
        |
        v
one bounded Measurement result in one recording Locality
```

It does not establish:

```text
source occurrences moved into the recording Locality
all ledger occurrences are input
all Assertions are input
the result may carry coordinates not required by its exact rule
```

### 2.2 D.2 addressed-reference determination

`01.Source.D.2` supplies another complete-enumeration shape. Given one exact
addressed source-byte position-coordinate reference within one exact direct
position Measurement result, its result carries every exact pair-occurrence
position Assertion reference that addresses that byte occurrence. It carries
none addressing only other byte occurrences.

This is complete only under its exact addressed result, occurrence, position,
Locality, and completeness boundary. It is not a general Assertion scan.

### 2.3 Fidelity test collection

The Fidelity pytest boundary enumerates every test collected for one pytest
occurrence. Each collected test must resolve to exactly one Fidelity subject.
That is honest completeness for one externally established collection
boundary. It is not every callable, Act, Assertion, or ledger occurrence.

### 2.4 Historical recurrence recovery

The earlier recurrence work is testimony for the discipline, not active
Authority for this new Measurement.

Its correction explicitly withdrew `cohort` and `population` as constitutional
vocabulary. What survived was:

```text
exact bounded exchanges
+ every recorded occurrence consumed by the finding
+ every coordinate of the declared grouping rule
+ exact counting Scope
```

It found two important defects even where the resulting count happened to be
correct:

1. a result omitted some occurrences on which it stood;
2. grouping omitted one declared Scope coordinate.

The precedent therefore supports:

```text
complete exact input and complete rule coordinates first
-> bounded finding
```

It does not establish one universal population kind or a right to scan every
Assertion in a ledger.

## 3. Candidate exact material boundaries

### 3.1 Current Standing in `calculator-claim`

This boundary is exact and Locality-bounded. It carries the ordered path and
recorded path-comparison result.

It does not carry the calculator system result. Therefore it cannot be the
complete input containing both roads.

Disposition: **exact but too narrow**.

### 3.2 Current Standing in the invocation system Locality

This boundary is exact and Locality-bounded. It carries the exact calculator
Ingest and its direct position Measurement result.

It does not carry the claim path or recorded distinction.

Disposition: **exact but too narrow**.

### 3.3 The union of both current Locality Standings

No active clause establishes that union as one subject. Naming the two
Localities because the reader expects their contents to correspond repeats the
selection problem at the Locality level.

The two Locality boundaries also need not advance together. One global append
boundary can bound storage chronology, but it does not turn two Locality
Standings into one current Standing.

Disposition: **not established as one exact subject**.

### 3.4 Every ledger Event through one append boundary

An append boundary supplies an exact storage prefix. It does not by itself
supply:

- Assertion Standing for every Event;
- one Locality relation among every Event;
- one exact rule identifying which Events are Measurement material;
- an input relation to a particular Measurement Act;
- Authority to reinterpret an event-kind registry as constitutional grammar.

Using every runtime event kind that a developer table classifies as an
Assertion would let machine enumeration supply the missing constitutional
boundary.

Disposition: **exact storage boundary, no exact Assertion subject**.

### 3.5 Every Assertion in this Seed's Witness

`this Witness` is an exact addressed subject in the maintenance and Fidelity
material. Current Fidelity physiology bounds deterministic tests comparing
this Witness with its witness grammar.

No active relation makes `this Witness` the dynamic EventLedger Assertion set
or makes every runtime result Assertion material of one Measurement.

Disposition: **different exact subject**.

### 3.6 Explicitly named source Localities under exact-byte Measurement

This is the one currently established multi-source shape. It can include all
exact Ingest occurrences from several named Localities through one boundary.

Its input species is exact Ingest material. The claim-side path comparison
finding and calculator-side position Assertion are not both exact Ingest
occurrences. Generalizing the rule from Ingest occurrences to every Assertion
would create a different declared Measurement subject and result.

Disposition: **lawful topology, wrong exact material species**.

### 3.7 The two calculator Assertions

This candidate contains what the reader expects to matter and omits everything
else. No earlier Seed finding establishes the omission rule.

Disposition: **developer-selected; circular**.

## 4. Does 01.Source.D assign the proposed Responsibility?

Active `01.Source.D` says:

```text
where a Seed performs
one Seed-native declared Measurement
over exact material related to that Seed
by exact Locality Evidence
that Seed bears the Measurement Responsibility
```

The clause supplies a conditional family shape. It does not declare every
possible Measurement, its exact input subject, its enumeration rule, or its
result coordinates.

Active witness grammar names specific Measurement rules, including:

- exact byte occurrences of an exact Ingest source set;
- positions of byte-pair occurrences in one exact Ingest result;
- recurrent byte-pair occurrence position;
- shared position of exact recorded pair-occurrence relations.

It does not name one Measurement over every Assertion across several
Localities.

Therefore:

```text
01.Source.D
-> Responsibility for an already-exact Seed-native declared Measurement

01.Source.D
-/-> declaration of this proposed complete-Assertion Measurement
```

For the proposed road, the exact Measurement is absent. The conditional clause
cannot fill its own antecedent by usefulness.

This conclusion concerns Responsibility. Authority for an exact Measurement
Act remains a separate coordinate and is not recovered by calling the road
Seed-native.

## 5. Exact completeness boundary and exact rule

The active exact-byte road shows that a complete source requires at least:

```text
named source coordinates
+ eligible occurrence kind
+ one completeness boundary
+ exact enumeration order
+ duplicate treatment
+ corruption treatment
+ exact empty treatment
```

For the proposed Assertion result, only a possible append boundary is readily
available. The following remain absent:

- exact source Locality coordinates broad enough to contain both roads without
  selecting them for expected significance;
- an exact rule determining which recorded results or nested Assertions enter;
- an exact ordering across independently advancing Localities;
- a proof that every eligible source occurrence is included;
- treatment of an eligible result containing several nested Assertions;
- treatment of an Assertion represented by several supporting occurrences;
- treatment of recorded material that carries no current Standing;
- exact empty-result physiology.

Consequently, an append boundary `B` does not independently recover material
`P`.

## 6. What one lawful result could carry

Active roads establish that an exact result rule may require preservation of:

- every exact matching Assertion reference;
- every exact source occurrence reference on which the result stood;
- source Locality and completeness coordinates;
- Evidence, Authority, Scope, limits, conflicts, and Unknown;
- exact ordering required by the rule.

Active `01.Source.D` also limits the result:

```text
result carries nothing beyond the exact rule and boundary
```

Therefore the proposed result shape is possible only after its exact rule says
which coordinates it carries. A generic desire to preserve everything cannot
supply those coordinates.

An exact reference may let a later responsible Act resolve and validate source
coordinates. Reference existence alone does not establish those coordinates as
applicable input to that later Act. Whether the result duplicates exact source
coordinate readings or carries their exact references is itself part of the
missing result rule.

Disposition: **lawful shape under a missing exact rule; not currently
instantiated**.

## 7. Does one local result move its sources?

No.

The exact-byte Measurement already records one result in a recording Locality
while preserving references to Ingest occurrences from named source
Localities. The source occurrences retain their original Localities.

`05.Recording` also distinguishes resolving a record reference for one later
Act from moving or strengthening the referenced Assertion. Resolution and
coordinate validation belong to that later Act's input boundary.

Therefore:

```text
result T in Locality L
+ T carries exact reference to source A in Locality A

does not establish:

A moved to L
A has Standing in L
A is applicable to another Act in L
```

The proposed result topology does not require source-Assertion movement merely
to preserve exact references.

Disposition: **established topology in narrower Measurement families**.

## 8. Does the result authorize later complete enumeration?

No generic later enumeration follows from result existence.

Current complete-enumeration roads are family-local:

- exact-byte Measurement enumerates exact Ingest occurrences and bytes under
  its exact rule;
- D.2 enumerates exact matching pair-position Assertion references under one
  addressed occurrence;
- Fidelity enumerates every test under one collected-test boundary.

A later Measurement over the proposed result would still require:

- its exact subject;
- its exact rule and completeness boundary;
- its Responsibility and Authority;
- input Applicability;
- result coordinates and Unknowns.

The first result cannot manufacture that second Responsibility by carrying
many references.

Disposition: **no current general road**.

## 9. Matrix

| Question | Current finding |
| --- | --- |
| Is one complete exact input containing both calculator roads recovered? | No |
| Does one current Locality Standing contain both? | No |
| Does the ledger append prefix establish an Assertion set? | No |
| Can a declared Measurement consume sources from named Localities and record one result elsewhere? | Yes, for exact Ingest material under the exact-byte rule |
| Does that move the source occurrences? | No |
| Does 01.Source.D declare the proposed all-Assertion Measurement? | No |
| May a result carry every exact reference and required coordinate? | Yes, when its exact rule requires them |
| Does current grammar contain that rule for all Assertions? | No |
| Does carrying the result authorize a later general enumeration or Compare? | No |
| Are the two calculator Assertions independently warranted as the complete input? | No |

## 10. Result

The movement investigation began with the wrong complete material:

```text
chosen A + chosen B
```

The better topology is:

```text
complete exact material under one rule and boundary
-> one bounded Measurement result carrying exact source references
```

That topology is already evidenced in narrower active Measurement families.
It avoids moving source subjects and avoids selecting a pair before a lawful
basis exists.

The current repository does not, however, establish the required exact
material for the calculator crossing. No current Locality Standing contains
both roads; no active rule turns the ledger prefix into one complete Assertion
subject; and `01.Source.D` does not declare the missing Measurement merely by
describing the family condition under which a Seed bears Measurement
Responsibility.

The remaining vacancy is therefore:

```text
What exact already-warranted rule enumerates
the complete material from which the relevant Assertions
may emerge without developer significance selection?
```

Until that rule, source boundary, and completeness boundary are recovered:

- the two Assertions are not established as one complete input;
- movement is not established as a necessary rung of this road;
- no later Measurement, Compare, relation, equality, or meaning follows;
- the represented relation around exact byte 0x3d remains Unknown.

## What this investigation does not establish

- That Assertion movement is universally invalid.
- That the existing movement preservation tests are false.
- That every ledger Event has Assertion Standing.
- That ledger chronology is Locality Standing.
- That `population`, `table`, `fan-out`, `discriminator`, or `comparator` is a
  constitutional kind.
- That exact reference is a constitutional Identity relation.
- That a result reference moves its source.
- That source availability establishes Applicability or Participation.
- That `01.Source.D` supplies Authority for an undeclared Act.
- That the calculator distinction and output correspond.
- Equality, arithmetic correctness, represented relation, or meaning.
