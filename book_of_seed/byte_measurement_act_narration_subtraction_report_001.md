# Byte Measurement Act-narration subtraction report 001

## Question

Can the plain-byte road use the Book's exact `Measurement` Act without the
subject family narrated inside its Act word?

## Subtraction

The runtime Act coordinate and its retained result copy change together:

```text
exact-byte Measurement
→ Measurement
```

No field, identity, occurrence, result, relation, or replacement Act is added.
The plain-byte Act remains distinguished by its exact subjects, source
Localities, source completeness boundary, recording cut, Act Locality, and
actual Ledger occurrence identity.

## Independent testimony

`01.Source.D` names `Measurement`, and the Witness Grammar declares:

```text
01.Source.D.exact_Act = Measurement
```

The byte domain remains in the exact material-result subjects and count
findings. It is not a second Act coordinate.

Changing the Act occurrence from `Measurement` to `Compare` refuses even when
all subject and boundary coordinates remain unchanged.

## Preserved physiology

```text
exact material-result subjects
+ source Localities
+ source completeness boundary
+ recording cut
+ Measurement
        ↓
Measurement Act occurrence A
        ↓
Measurement result R
```

Act-before-result order, A-without-R, one result per A, restart, current
coordinates, byte findings, byte-pair Measurement, and Movement remain exact.

The result still carries `exact_act = Measurement`. Whether that copy is needed
when `R` already addresses `A.identity` is deliberately not answered here.

## Family boundary

No other Measurement family changes. No Byte Act, ExactByteMeasurement, or
family-local Act identity is introduced.

## Disposition

```text
Measurement                                  retained exact Act
exact-byte subject and finding coordinates   retained
exact-byte Measurement Act narration         withdrawn
result exact_act copy                         retained for later pressure
```
