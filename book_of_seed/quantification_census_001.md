# Quantification census 001

## Scope

This census pressure-tests four admitted words:

```text
every
each
one
both
```

It changes no Book clause, admission, machine grammar, runtime occurrence,
Act, result, relation, identity, or reader.

The active Book surface contains:

```text
every     4 occurrences
each      7 occurrences
one      76 occurrences
both      2 occurrences
```

These counts include capitalization and exclude admission comments and
investigation reports.

## Population decomposition

A quantified claim does not establish a `Population` object.

The exact physiology is decomposed into:

```text
one selected reading boundary B
exact member addresses current through B
the addressed set derived at B
exact per-member occurrences or result coordinates
comparison of addressed members with recorded occurrences
```

For the active Applicability control:

```text
S(B) × C(B)                         exact members at B
Applicability Act subject pairs     recorded members through B
set equality                         exhaustive coverage at B
```

No frozen member set, iteration occurrence, progress occurrence, or
completion occurrence is established.

## `every`

The four active uses are:

```text
current coordinate for every subject in one binding
every coordinate of one Measurement binding
every pair-position reference for one byte subject
every exact finding of one Compare result
```

In each case the member set is established elsewhere by exact binding or
result coordinates. `every` adds no member address, boundary, completeness
occurrence, or result.

The boundary interruption control rules out an unbounded reading:

```text
every member at B1 = 4 members
every member at B2 = 9 members
```

Thus `every` can mean only:

```text
no addressed member of this exact set at B is omitted
```

It cannot mean all future members, one frozen work set, or completed work.

## `each`

The seven active uses distribute a statement over exact members:

```text
each exact subject is one input
preserve each Measurement result reference
preserve each pair-position result reference
one boundary coordinate belongs to each finding
one count finding preserves each occurrence reference
```

No active physiology distinguishes these sentences from the same sentences
using `every` or naming the complete exact references directly.

The apparent distributive role decomposes into:

```text
exact member address
+ one stated coordinate or relation for that member
```

It does not establish a distributive Act or member-walk occurrence.

Disposition: `each` has no independent constitutional distinction in the
active Book surface. It is a strong subtraction candidate.

## `both`

The two active uses occur in `04.Compare` after exactly two subjects have been
named:

```text
earlier result + later result
same-position result position + recorded pair-finding Compare result
```

`both` merely points backward to those two exact addresses. It adds neither
cardinality nor a collective occurrence:

```text
both results
=
the earlier and later exact results
```

and:

```text
both subjects
=
the two exact subjects named by the clause
```

Disposition: `both` is anaphoric prose, not an independently established Book
coordinate. It is a strong subtraction candidate.

## `one`

`one` is not one physiology across all 76 uses. At least three roles occur:

### Exact cardinality

```text
one result per Act occurrence
one result position
one boundary
one Act occurrence
```

Runtime refusal of a second result for the same Act is a positive control.
Replacing this role with an indefinite article would lose exact cardinality.

### One member of an exact alternative set

```text
one of applicable or inapplicable
```

This establishes exactly one result coordinate from a two-member alternative
set. Absence remains distinct from either member.

### Singular prose

Many uses have the surface form:

```text
one exact subject
one exact relation
one exact Locality
```

Some may only introduce a singular noun where `an` or `the exact` would carry
the same coordinates. Others establish cardinality one. A global subtraction
would conflate these roles.

Disposition: retain `one` pending a use-by-use split between exact cardinality
and singular prose.

## `each` versus `every`

The active road does not establish two quantifier physiologies:

```text
every exact member has one Act
each exact member has one Act
```

Both are validated by the same exact comparison:

```text
members at selected boundary B
==
subjects of recorded Acts current through B
```

Per-member addressability is carried by the Act subject references. Exhaustive
coverage is carried by the boundary-relative set comparison. Neither word
establishes another coordinate between them.

## Finding

```text
every    bounded exhaustive reading; no independent occurrence
each     distributive prose over exact members; no distinct physiology found
both     backward reference to two named exact subjects; no distinct physiology
one      mixed surface; exact cardinality survives, prose uses remain under pressure
```

## Amendment after first subtraction

```text
`each`  replaced by exact subject/reference wording
`both`  replaced by the two named exact subjects
```

Both words leave active Book admission. No runtime or machine-grammar
coordinate changes. The exact subjects, references, common boundary
coordinate, and result preservation claims remain.

The next falsifiers are:

```text
1. split `one` use by use; preserve every cardinality-one refusal
```

Do not add Quantifier, Population, Member, Distribution, Iteration, Progress,
or Completion occurrences.

## Amendment after `every` falsifier

`every_boundary_exhaustion_census_001.md` uses the interrupted Applicability
control:

```text
1 recorded Act / 4 exact pairs   not exhaustive
9 recorded Acts / 9 exact pairs  exhaustive
```

`every` survives as the distinction that no exact coordinate in the bounded
extent is omitted. The selected boundary supplies the extent. No set,
iteration, progress, or completion occurrence is added.

## Amendment after `one` domain census

`one_coordinate_domain_census_001.md` separates:

```text
exact cardinality one
singular binding of one addressed instance
one coordinate role inside a larger exact structure
one alternative from a stated result set
```

Exact cardinality and identity-collapse controls survive. Some singular-binder
and coordinate-role uses remain prose-subtraction candidates. `one` stays in
active admission; no global replacement is licensed.

## Closure

`quantification_closure_census_001.md` records the completed campaign.

Predicate-role, singular-binder, and relation-binder subtraction reduce active
`one` from 76 uses to 31. The residual uses preserve exact cardinality,
coordinate-tuple arity, one-to-one Act/result relations, stated alternatives,
or identity collapse.

`each` and `both` are absent from active admission. `every` and `one` survive
under exact qualified domains.
