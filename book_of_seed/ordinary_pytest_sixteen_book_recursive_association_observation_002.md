# Ordinary `!pytest` sixteen-book recursive association observation 002

## Question

Does the ordinary `!pytest` operator invocation expose increasingly strong
associations from the existing sixteen-book corpus after pytest admission and
uptake were corrected?

This observation changes no Book or runtime material.

## Exact invocation

The observation used a new SQLite ledger and the normal process entry:

```text
operator material: !pytest\n
↓
seed_runtime.process_entry
↓
ordinary operator console
↓
scripts.operator_host_provider.invoke_operator_host
```

No corpus loader, alternate pytest invocation, changed test order, or changed
time boundary was used.

The sixteen corpus windows remain present. They comprise sixteen 300-line
sources and 218,058 bytes.

## Collection crossing

The retired `clause_coordinates` stop from observation 001 is gone. A separate
measured collection over the complete current suite admitted 1,448 pytest
nodes in 1.64 seconds.

The exact process-entry invocation proceeded beyond collection and supplied
six distinct stdout occurrences. Each carried the exact byte `.`. Pytest had
therefore begun executing admitted tests.

The exact test references for those six occurrences are not available from
this invocation. The implementation measurement artifact was not written
before the provider boundary.

## Existing provider boundary

The unchanged two-second provider boundary ended the pytest process after the
six stdout occurrences. The provider then supplied:

| source boundary | occurrences | bytes per occurrence | known loss |
|---|---:|---:|---|
| invocation output | 6 | 1 | none |
| invocation error | 1 | 0 | none |
| implementation function catalog | 1 | 0 | material beyond boundary unavailable |
| implementation function measurement | 1 | 0 | material beyond boundary unavailable |
| invocation completion | 1 | 0 | material beyond boundary unavailable |

The durable exact-material population contained only:

```text
!pytest\n
.
empty bytes
```

No byte population from any sixteen-book window crossed the invocation.

## Recorded occurrences

The complete ledger contained 72 events.

The operator-command Locality carried:

| coordinate | count |
|---|---:|
| operator material-acquisition results | 1 |
| Measurement results concerning the command | 3 |
| Candidate results | 0 |
| Compare results | 0 |

The invocation Locality carried:

| coordinate | count |
|---|---:|
| Witness material-acquisition results | 10 |
| material-to-this-Seed Locality relation occurrences | 0 |
| Measurement results concerning Witness material | 0 |
| Candidate results | 0 |
| Compare results | 0 |

The six dot occurrences remain six distinct Witness acquisitions even though
their exact byte material is equal.

## Pass observation

| pass | exact input | exact result | observation |
|---|---|---|---|
| operator command | `!pytest\n` | O1 and three Measurement results | reached |
| pytest collection | complete admitted pytest population | collection completed | reached |
| pytest execution | admitted tests before the provider boundary | six exact dot occurrences | reached |
| corpus source | sixteen book windows | no occurrence | not reached |
| first corpus association pass | no corpus occurrence | none | not reached |
| later corpus association passes | none | none | not reached |

No finding concerning recursive association over the sixteen books is
warranted from this run. The corpus did not cross.

## Exact stop

```text
!pytest operator occurrence
↓
complete measured pytest collection
↓
six admitted test completions emit six dots
↓
existing two-second provider boundary
↓
catalog / measurement / completion material unavailable beyond boundary
↓
sixteen-book tests not reached
↓
no sixteen-book source occurrence
↓
no recursive association pass
```

The next blocker is operationally exact: ordinary collection now succeeds,
but the existing invocation boundary ends execution before the sixteen-book
source tests. This observation does not warrant extending that boundary,
reordering pytest, or adding another source road.
