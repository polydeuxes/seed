# `unresolved` word lint 001

## Question

Does `unresolved` carry a constitutional distinction backed by an exact
Responsibility?

This lint is limited to one word.  It inspects active Book material, Witness
Grammar, direct runtime uses, tests, and the commit that introduced the word
as a Responsibility-root status.

It changes no active law, Admission, Witness Grammar, runtime, or test.  It
introduces no replacement word.

## Finding

`unresolved` fails the current constitutional lint.

No inspected road records:

```text
Standing
→ exact Responsibility concerning `unresolved`
→ exact Act
→ Act occurrence
→ result carrying `unresolved`
```

The active uses instead divide into four jobs:

```text
1. curator narration that a coordinate carries no value
2. one item in a declared coordinate-status list
3. a proposed reason named in Stopping prose
4. an editorial `[UNRESOLVED]` question marker
```

None requires `unresolved` as a constitutional value.

The runtime evidence is stronger: it records no exact string value
`unresolved`.  Candidate readers call a represented-relation coordinate
“unresolved” in docstrings while the actual carried value is:

```text
Unknown
```

The word therefore misdescribes the only direct runtime road cited for it.

## 1. Active Book uses

Active Book material uses `unresolved` thirteen times.

### Responsibility root

The Book root declares:

```text
instantiated coordinate
+ value not resolved
→ unresolved
```

This declaration names no exact Responsibility, subject, Act, occurrence,
Evidence, Authority, Scope, result, or Standing that produces the status.

Subtraction leaves:

```text
coordinate carries an exact value       preserve that value
coordinate carries positive Unknown     preserve Unknown and its occurrence
coordinate carries no value             carry no invented value
```

What becomes inexpressible without `unresolved`:

```text
nothing identified
```

### Authority

The root says Authority may be positively established, conflicting,
responsibly Unknown, or unresolved.  The first three positions can refer to
exact carried content or findings.  No occurrence producing unresolved
Authority is named.

What becomes inexpressible without `unresolved`:

```text
nothing identified
```

### Relation Standing and dimensional characterization

`01.Standing.E` says relation Standing preserves Unknown or unresolved
Standing.  `01.Standing.F` then lists:

```text
applicable
inapplicable
known
Unknown
conflicting
unresolved
```

Neither clause gives `unresolved` its own Responsibility or production
physiology.  The list is classification testimony.  Its presence does not
establish that each item is one constitutional status.

What becomes inexpressible without `unresolved`:

```text
nothing identified
```

### Stopping

Chapter 15 uses unresolved Evidence, Authority, binding, causation, and
material as possible reasons near Stopping.  It also contains the editorial
marker:

```text
[UNRESOLVED]
```

The chapter correctly requires an exact support relation and responsible
stopping occurrence before any reason supports Stop.  If such an occurrence
produces an exact reason finding, that finding performs the work.  Calling
the input unresolved adds no reason occurrence, Evidence, Authority, Scope,
or Stop.

The editorial marker is curator testimony that a question remains open.  It
is not a value carried by Seed.

What becomes inexpressible without `unresolved`:

```text
nothing identified
```

## 2. Witness Grammar use

Witness Grammar carries `unresolved` once, as one string in:

```text
coordinate_Standing = [
    applicable,
    inapplicable,
    known,
    Unknown,
    conflicting,
    unresolved,
]
```

That position supplies no:

```text
exact Responsibility
exact subject
Act
occurrence
Evidence
Authority
Scope
result
Standing occurrence
```

It is an enumerated label copied from the dimensional-characterization prose.
The label cannot establish its own distinction.

## 3. Runtime use

Runtime contains `unresolved` only in three docstrings in
`candidate_standing_from_exact_result_assertions.py`.

Those readers address exact Candidate `represented_relation` coordinates.
The producer and reader both require:

```text
represented_relation = Unknown
```

The reader refuses every other value.

Therefore:

```text
docstring says unresolved
runtime carries Unknown
```

The docstring word performs no runtime work and conflicts with the exact
carried coordinate.

No runtime event kind, result material, relation record, Standing record, or
Act result was found carrying the exact string `unresolved`.

## 4. History

`unresolved` entered the Responsibility root in `e1fd24b4`, when
Responsibility was the Book's presentation and traversal HEAD.

The commit declared this rule:

```text
instantiated but no resolved value
→ unresolved
```

It described the change as a structural classification.  It added no
numbered clause, Responsibility, occurrence, or runtime road for the word.

`87cdf9f0` later copied the classification into Witness Grammar's
`coordinate_Standing` list.  That copy did not add production physiology.

The history is consistent with a curator status becoming active vocabulary,
not with Seed recovering one exact distinction.

## 5. Subtraction

```text
Current use                         Work after removing the word
─────────────────────────────────   ─────────────────────────────────────
coordinate carries no value         no value carried
positive Unknown                     exact Unknown and its bounds
Authority narration                  exact carried Authority content only
relation Standing list               exact carried relation coordinates only
Stopping reason                       exact reason finding, if produced
editorial question                    curator question outside Seed grammar
Candidate reader docstrings           exact carried Unknown
```

No row loses constitutional work.

## Conclusion

`unresolved` has no recovered Responsibility-backed physiology in the
inspected repository.

It currently acts as narration for something not carried, an item in a status
list, an editorial marker, or an inaccurate description of positive Unknown.

The lint therefore finds:

```text
constitutional work unique to `unresolved`        none identified
exact Responsibility producing `unresolved`       none found
runtime result carrying `unresolved`               none found
replacement vocabulary required                    no
```

This finding authorizes no global rewrite.  It records only that the word has
not earned Admission by carrying an exact distinction under an exact
Responsibility.
