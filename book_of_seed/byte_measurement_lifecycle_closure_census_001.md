# Byte Measurement lifecycle closure census 001

## Question

After the plain-byte Measurement lifecycle subtractions, does an obvious
copied durable coordinate remain on its Act or result?

This census changes no runtime road, Book clause, Witness Grammar, admission,
Act, result, or occurrence.

## Current durable shape

```text
Measurement Act A
    Event.identity
    Event.locality_identity = recording Locality L
    act = Measurement
    subject_reference = exact material result occurrences
    source_localities = exact source Localities
    completeness_boundary_identity = bounded source read B

derived recording cut C
    latest occurrence in L through B

Measurement result R
    Event.identity
    Event.locality_identity = A.locality
    act_occurrence_event_identity = A.identity
    result_positions = exact source-material-set, count, and recurrence findings
```

## Act-coordinate pressure

`Measurement` independently distinguishes the Act from another Act over exact
subjects.

The subject references address the exact material result occurrences. The
source Localities address the declared source domains; a declared Locality need
not be recoverable merely by listing the Localities of the material result
subjects. The completeness boundary bounds the source read, so material
appended after B cannot enter the result.

The recording cut C is a different boundary domain from B. It addresses the
recording Locality through B and is reconstructed from those exact coordinates.
Later recording-Locality occurrences do not change C for the fixed B.

The Act occurrence's Event Locality addresses the recording Locality. That is a
distinct coordinate role from `source_localities`, even when the same Locality
identity appears in both.

No remaining Act field merely copies another field on A or R.

## Result-coordinate pressure

R's exact occurrence identity remains distinct from A and from an equal result
occurrence. Its Event Locality must equal A's Locality. Its Act-occurrence
reference supplies the exact A that preceded it.

The result positions are the durable Measurement findings. Their local
dimensions address distinct positions and finding content. They include the
exact source-material-set and its bounded-read coordinate. Changed, missing,
extra, reordered, or truncated findings refuse.

No fixed lifecycle identity, binding reference, Act copy, source-Locality copy,
boundary copy, preservation label, or top-level dimensions narration remains
on R.

## Stoppable floor

The remaining independently readable stages are:

```text
A absent
A present, R absent
A present, R present
```

The former binding-without-A stage has no surviving occurrence. One result per
A remains enforced without a prospective result identity.

## Family boundary

This closure applies only to plain-byte Measurement. Byte-pair Measurement,
Movement, and other Measurement families retain their own lifecycle shapes
until independently pressured.

Compatibility parameters and read-breadth mechanics are not durable
coordinates and are outside this semantic closure.

## Disposition

```text
plain-byte Measurement binding coordinates   retained on A
plain-byte Measurement Act occurrence        retained
plain-byte Measurement result occurrence     retained
exact result positions                        retained
recording cut C                               retained by reconstruction
obvious copied durable coordinate             none found
plain-byte Measurement semantics              closed
```
