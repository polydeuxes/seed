# Bounded Applicability Population Report 001

## Question

Does brute-force work over one exact bounded Applicability-result population
need Candidate, selection, enumeration, or another occurrence between positive
Applicability and Act occurrences for their exact bindings?

The experiment uses the live ordered relation-path and pair-finding Compare
road. It changes no active Book clause and adds no Act, result, relation,
identity, kind, Candidate object, selection object, completion occurrence, or
constitutional word.

## Correction to the owner question

Earlier censuses established four independently stoppable floors:

```text
Compare binding
Applicability result
Compare Act occurrence
Compare result
```

They correctly proved:

```text
positive Applicability result
!=
Act occurrence for its exact binding
```

They then asked what prior occurrence chose or initiated the bound Act.
That question assumed one applicable member had to be selected from other
applicable members.

The live road has another shape:

```text
complete exact Applicability-result population through boundary B
↓
one Compare Act occurrence for every positive result and its exact binding
zero Compare Act occurrences for every negative result
```

There is no preferred member and no unanswered reason why one member proceeds
instead of another.

## Exact bounded reader

`read_ordered_path_pair_finding_compare_applicability_results_and_acts(...)`
reads:

```text
exact through-occurrence boundary
exact positive Applicability result occurrences
exact negative Applicability result occurrences
exact Compare Act occurrences by positive result
exact positive results without an Act occurrence through that boundary
```

The reading is bounded by the exact `through_event_occurrence_identity` in the
supplied current coordinates. It reads the Ledger prefix through that
occurrence. Act occurrences appended afterward do not appear when the earlier
boundary is reopened.

The reader records nothing. It returns an
`ApplicabilityResultActReading`, not a `Population` object.

The current implementation-family name still contains `ordered_path`. This
report does not admit Path. The exact Compare input resolves as one
shared-position Measurement result occurrence, result position 0, its two
pair-position result references, shared source-position coordinate, source
material result, completeness boundary, and Locality. Whether `path` adds any
distinction beyond those coordinates remains under subtraction.

Its identity tuples describe exact results that already occurred. They are not
Candidate occurrences and do not assert that a possible population was first
materialized or numbered.

## Population subtraction

`Population` is not introduced as a durable thing. The complete reading
decomposes into:

```text
one exact through-occurrence boundary
exact Applicability result occurrences through that boundary
the positive or negative content of each exact result
exact Act occurrences addressing positive results and their bindings
exact positive result identities not addressed by such an Act through B
```

No independent Population identity, occurrence, Act, result, relation, member
record, enumeration record, or boundary exists beyond those coordinates.

Deleting the descriptive word `population` loses nothing from the reading.
The operative statement can be made directly:

```text
for every exact Applicability result through B
    positive → one Act occurrence for its exact binding
    negative → no Act occurrence for its exact binding
```

The report retains `population` in its title as the word under subtraction,
not as admitted physiology.

A residual census found no `population` in active Book chapters or Witness
Grammar. Four active runtime prose or diagnostic uses decomposed directly into
exact occurrence positions, exact read occurrences, exact result positions,
and one ordered append sequence. No runtime coordinate or stored field changed.

`trigger` is likewise absent from active Book and Witness Grammar. Remaining
runtime uses are SQLite `TRIGGER` syntax for storage-integrity enforcement, not
Seed causation. `explicit` had no exact coordinate and was removed from active
runtime, test, and census prose.

`govern` is absent from active Book and Witness Grammar too. The exact
subject-to-Act binding already identifies the Act and subjects. This report
therefore uses Act occurrence plus binding coordinates instead of admitting a
second governing relation.

## Four-result control

The focused runtime fixture establishes four exact Compare bindings and four
exact Applicability results:

```text
result 0  applicable
result 1  inapplicable
result 2  inapplicable
result 3  applicable
```

At the exact boundary after Applicability and before the Compare Act
occurrences, the
population reading is:

```text
positive results:                 (result 0, result 3)
negative results:                 (result 1, result 2)
Compare Act occurrences:          ()
positive results without an Act:  (result 0, result 3)
```

This remains a valid stoppable state. It proves only that Applicability does
not itself perform the Act for the binding.

The existing brute-force operation then records:

```text
result 0 → Compare Act 0 for binding 0
result 3 → Compare Act 1 for binding 3
```

It records no Compare Act for results 1 or 2.

At the later boundary:

```text
positive results:                 (result 0, result 3)
negative results:                 (result 1, result 2)
result-to-Act mapping:            ((result 0, Act 0),
                                   (result 3, Act 1))
positive results without an Act:  ()
```

Reopening the earlier boundary after both Acts exist still returns the earlier
reading with zero Compare Acts. Exact occurrence boundaries, not present
Ledger size, determine the population state.

A repeated brute-force call records zero Acts and leaves current coordinates
unchanged.

## Runtime invariant

The bounded operation now reads the exact Applicability results
before processing it and again before returning.

It refuses:

```text
two Compare Act occurrences for one positive result
one Compare Act occurrence for a negative result
one Compare Act occurrence without an exact current result
return while one exact positive result still has no Act occurrence
```

This adds no completion event. Completion is the readable equality:

```text
positive Applicability result identities
=
Applicability result identities addressed by Act occurrences
```

under one exact boundary.

## Biological-material control

The two overlapping mitochondrial-reference windows from
`exact_biological_result_subject_reuse_report_001.md` establish 20 exact
ordered-path Compare Applicability results:

```text
10 applicable
10 inapplicable
```

The final bounded population reading establishes:

```text
10 positive results
10 Compare Act occurrences
10 exact positive-result-to-Act pairs
0 positive results without an Act
0 negative results with an Act
```

One pair Compare result participates in ten later Compare bindings. Brute-force
processing realizes every positive binding while preserving each path's exact
material occurrence and local positions. It does not choose one path as the
reason for another path's exclusion.

## Population is not Candidate enumeration

The operation iterates in host language. That mechanical loop does not
establish:

```text
Candidate occurrence for every possibility
ordered candidate list
selection occurrence
priority relation
winner or loser
scheduler
reason why one positive member proceeds
```

The exact population consists of Applicability results that already exist
through one exact boundary. Its members are not hypothetical interpretations.

Brute force needs a bounded population. It does not need a constitutional
enumeration of candidates.

## Applicability decomposition

The experiment keeps three distinctions apart:

```text
exact binding exists
Applicability establishes positive or negative
Act occurrence for the exact binding exists or does not exist through B
```

Applicability owns the middle distinction. It neither creates the earlier
binding nor performs the later Act.

The later crossing needs no selecting occurrence when the bounded operation
processes every positive result. The exact Act occurrence still addresses its
binding and positive Applicability result after it exists.

## What is not established

The experiment establishes no:

- Candidate physiology;
- Selection, Initiation, or Scheduler;
- ordering or preference among positive results;
- causal claim that Applicability performs the Act for its binding;
- generic dispatcher across unrelated Act families;
- generic completion Act or result;
- automatic processing beyond the exact family-local operation; or
- constitutional meaning for the host-language loop.

## Disposition

Withdraw the selection-owner vacancy.

Keep the exact stoppability distinction:

```text
Applicability result
!=
Act occurrence for its exact binding
```

For this live road, bounded brute-force coverage is established without
Candidate enumeration:

```text
every exact positive result through B → exactly one Act occurrence for its binding
every exact negative result through B → no Act occurrence for its binding
```

Future roads may use the same falsifier one family at a time. Do not generalize
the runtime invariant beyond independently tested populations.
