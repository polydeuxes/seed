# Predicate-role `one` subtraction report 001

## Question

When an exact addressed thing “is one subject,” “is one occurrence,” or “is
one coordinate,” does `one` establish cardinality or only ordinary singular
role assignment?

## Falsifier

Replace only predicate-role uses with `a` or `an`.

Do not change coordinate lists, result uniqueness, exact alternatives, added
occurrence count, or claims identifying several surfaces as one occurrence.

## Result

The following exact roles remain:

```text
material result                         an occurrence in its Locality
supplied material                       an exact binding subject
another operator boundary occurrence    an exact subject in another binding
failure count                           a failure coordinate
Act result                              a current coordinate for every subject
addressed result content                a subject
current addressed content               an exact binding subject
through boundary                        a coordinate of every exact position
carried prior boundary                  an exact earlier boundary
```

No clause counted a complete bounded extent through these predicate phrases.
The exact nouns, containing bindings, result positions, Locality, and order
basis preserve the roles.

## Positive controls

The pass leaves exact cardinality and identity claims unchanged:

```text
one result for an Act
one result-local position
one added occurrence
one of two Applicability answers
Emission Act, attempt, and accepted write are one occurrence and one result
```

## Finding

Predicate-role `one` is ordinary singular prose in this slice. Cardinality
`one` remains a load-bearing admitted word. The two uses are not globally
interchangeable.
