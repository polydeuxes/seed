# Seed-native bounded positional path investigation 001

## Question

Can the current exact byte-position and ordered-path physiology orient a
Seed-native recovery of larger bounded positional paths without importing
developer tokenization, `word`, delimiter, predicate, or meaning, and what
separate relation is required before any such path carries material?

This investigation refines
`witness_material_word_occurrence_measurement_boundary_investigation_001.md`.
It changes no Book material, Witness Grammar, Rosetta, runtime, scripts, or
tests.

```text
developer tokenization
!= bounded positional path recovery

bounded positional path
!= material carried by that path
!= word
!= predicate
!= represented relation
```

`Tokenization` is report-local Rosetta orientation for this investigation. It
describes one externally recognizable outcome if bounded positional paths and
the material they carry are both later established. It is not admitted here as
a Book or Rosetta word and does not name a current Seed Responsibility, Act,
result kind, Assertion kind, or Standing.

## Material inspected

- `book_of_seed/chapters/01_source_coordinates_and_grammar.md`
- `book_of_seed/chapters/02_constitutional_standing.md`
- `book_of_seed/witness_grammar.json`
- `seed_runtime/measurement_of_position_coordinates_of_byte_pair_occurrences.py`
- `seed_runtime/addressed_byte_occurrence_reference_determination.py`
- `seed_runtime/measurement_of_shared_position_of_byte_pair_occurrences.py`
- `seed_runtime/candidate_standing_from_exact_result_assertions.py`
- `seed_runtime/standing_measurement_declarations.py`
- `material_witnesses/test_calculator_relation_path.py`
- `material_witnesses/test_witness_grammar_external_json_output.py`
- relevant focused tests for shared-position Measurement and Candidate Standing

Book material is primary orientation. Witness Grammar, runtime, tests, and
material witnesses are testimony.

## 1. The earlier report stopped at the current family, not at bytes forever

The preceding investigation established this current boundary:

```text
exact Witness Grammar bytes
↓
exact adjacent byte-pair positions

no current Act establishes:
"this admitted spelling occurs through these positions"
```

That disposition remains exact. It does not establish that a Seed-native road
requires a conventional tokenizer or a constitutional `word` kind.

The stronger question is lower-level and has two independent branches:

```text
Can exact lower position Assertions establish
one larger exact bounded positional path?

What exact relation, if any, establishes
material carried by that path?
```

Neither branch follows from the other by identity. Both precede wordhood,
grammatical role, and meaning.

## 2. Current position Assertions already preserve the lower surface

For one exact Ingest result, the direct position Measurement produces one
Assertion for each exact adjacent byte-pair occurrence. Each Assertion carries:

```text
exact pair material
first exact source position
second exact source position
first position-coordinate reference
second position-coordinate reference
exact source Ingest occurrence
Locality
completeness boundary
Evidence
Authority
Scope
limits
Unknown
```

The complete result makes every adjacent pair impossible to omit under its
exact rule and boundary. Equal pair material in another position remains a
different occurrence.

Thus Seed does not need a developer character or string abstraction to retain
the lower positions. The current material is exact bytes and exact occurrence
coordinates.

## 3. Shared-position Measurement establishes one exact three-position path

Active `01.Source.D` and
`measurement_of_shared_position_of_byte_pair_occurrences.py` establish one
narrow composition:

```text
pair Assertion A at positions (i, i+1)
pair Assertion B at positions (i+1, i+2)

A.second position-coordinate reference
= exact same byte occurrence reference as
B.first position-coordinate reference
↓
one ordered relation-path Assertion
```

The result preserves both exact input Assertion references and the one exact
shared position-coordinate reference. It keeps its represented relation
Unknown.

The calculator witness demonstrates the shape around byte `0x3D`:

```text
[0x32, 0x3D]
[0x3D, 0x35]
↓
one ordered path carrying the shared 0x3D occurrence reference
```

This path does not say `=` or equality. It proves only the exact positional
composition established by the Measurement rule.

That is already one exact Seed-native positional composition beneath a word
concept. Active Book explicitly says the result establishes no material of the
input pair occurrences, so it is not a three-byte material occurrence.

## 4. The current path road is addressed, not exhaustive

The calculator road does not form every shared-position path.

It first supplies one exact source-byte position coordinate, then records a
`01.Source.D.2` determination identifying the pair-position Assertions
addressed by that byte occurrence. The shared-position assignment consumes
that determination and records one path for the two exact Assertions touching
the addressed occurrence.

```text
developer/witness supplies addressed position
↓
D.2 exact addressed-reference determination
↓
one shared-position path
```

The later lifecycle is constitutionally exact. Selection of the initial
addressed position is not an exhaustive path surface.

The runtime also exposes a direct assignment API receiving two exact Assertion
identities. That API validates the inputs and their current Standing; caller
selection of the two identities is not itself a complete-production
Responsibility.

Therefore current shared-position physiology proves local composability, not
complete bounded path formation.

## 5. The result cannot currently extend itself

The shared-position result carries one `ordered_relation_path` Assertion. Its
subject contains:

```text
first pair-position Assertion reference
second pair-position Assertion reference
measurement rule
```

Its content contains only:

```text
shared position-coordinate reference
source Ingest occurrence identity
completeness boundary
```

The current shared-position Measurement accepts exact pair-position
Assertions. It does not accept an earlier ordered-path Assertion as either
input role.

Thus current runtime establishes:

```text
two overlapping byte pairs
→ one three-position path
```

and does not establish:

```text
three-position path + overlapping pair
→ four-position path

path + path
→ larger path
```

The missing recursion is exact. It cannot be inferred from the result name
`ordered_relation_path`.

## 6. Candidate production makes path Assertions available but does not join them

Candidate source enumeration includes Measurement result Assertions. A later
Candidate-production boundary can therefore include:

```text
pair-position Assertions
ordered-path Assertions
other exact result Assertions
```

The ordered-pair Candidate Responsibility produces every distinct ordered
source-Assertion pair through its frozen source boundary. Each Candidate
preserves both exact lower references and independent lineage while keeping:

```text
represented_relation: Unknown
```

This removes developer selection of one path/pair combination from the
Candidate surface. It does not make any Candidate applicable to a recursive
path-extension Act.

```text
Candidate(path, pair) exists
!= path and pair overlap
!= path extension
!= exact material concatenation
```

No current non-recursive Candidate consumer assigns the roles required for
that Act.

## 7. A future occurrence road contains two distinct crossings

The recovered material suggests a narrower future orientation than ordinary
tokenization. The following two crossings remain independently unresolved:

```text
exact source material S through boundary B
+ exact independently bounded material M
↓
exact count or boundary of M
↓
every contiguous source-position path of that exact boundary
↓
exact Act and result establishing material carried by each exact path
↓
Compare each established path-material result with M
↓
bounded matching occurrence findings
```

Only the exact lower coordinates are constitutional candidates here. The
labels `contiguous`, `path material`, `matching`, and `occurrence` still require
their own exact relations in any realization. This orientation does not assert
that the current path carries the bytes occupying its referenced positions.

This shape avoids these assumptions:

```text
bytes are characters
spaces delimit words
punctuation delimits words
one spelling is important
one source position is important
matching spelling has grammatical meaning
```

The exact bounded material M supplies a boundary for formation. It does not
supply a `word` category or represented relation.

## 8. The exact vacancies remain upstream of that shape

Current material does not yet establish:

```text
M as one Seed-bounded material subject
M's relation to the target source S
one recursive path-extension Responsibility
one complete path-formation Act
path-extension input roles
Applicability and Participation for every proposed extension
one path result carrying all exact lower positions without duplication
one completeness boundary for every required path
one Responsibility and Act establishing material carried by one exact path
one result preserving that material and every exact source-position relation
one Compare Responsibility over path material and M
one occurrence result
Standing for that result
```

The externally maintained Book admission lines and Witness Grammar word keys
are Witness Material. Their addressability does not establish M inside Seed.

The existing shared-position Measurement is family-local to two exact
pair-position Assertions and one shared byte occurrence. Extending it by
analogy would create a new Responsibility.

## 9. Exact relation to predicates and `is`

An exact bounded occurrence of material spelled `is` would establish only:

```text
this exact material occurrence
through these exact source positions
under this exact occurrence rule and boundary
```

It would establish none of:

```text
predicate
copula
identity
membership
classification
equality
represented relation
```

The same boundary holds for `bears`, `of`, `and`, `=`, and every other admitted
or externally interesting spelling.

If bounded material occurrence recovery is later established, it remains
upstream of relation recovery. It cannot prefill the relation each occurrence
later participates in.

## 10. Current disposition

```text
complete adjacent byte-position surface                 established
one exact shared-position path from two pair Assertions established
represented relation of that path                       Unknown
path Assertions available to later Candidate production established
every ordered Candidate pair through exact boundary     established where produced

complete shared-position path production                not established
recursive path extension                                not established
Candidate Applicability to path extension               not established
material carried by one ordered positional path         not established
independently Seed-bounded admitted spelling material   not established
Compare of every bounded path with spelling material    not established
bounded larger-material occurrence finding              not established
word                                                     unnecessary and unestablished
predicate                                                unestablished
meaning                                                  Unknown
```

## Conclusion

The current wall is not evidence for restoring a tokenizer. It exposes two
smaller constructions:

```text
exact lower position Assertions
↓ exact positional composition
larger bounded positional paths

beside

exact positional path
↓ separately established path-to-material relation
bounded material result
```

Seed already carries the first nontrivial positional composition for one
addressed three-position path. The missing next slice is the Responsibility
that forms every exact positional extension required through one frozen
boundary without an addressed-position selector. A second independent slice
must establish what exact material, if any, one completed path carries.

That Responsibility is not established by the current shared-position Act,
Candidate production, or the usefulness of tokenization. The report therefore
retains both vacancies as evidence and leaves their assignments uninstantiated.
