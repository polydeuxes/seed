# Ordered path Candidate and Compare uptake investigation 001

## Question

The exact positional-path Assertion now enters the complete ordered Candidate
surface. Active `04.Compare.B` separately assigns this Seed one bounded
Responsibility to Compare an exact ordered path with an exact recorded
pair-finding comparison result.

This report asks:

```text
Does the Candidate occurrence establish uptake of that path into 04.Compare.B,
or does the current Compare road still require a separately addressed input
pair?
```

It changes no Book material, Witness Grammar, Rosetta, runtime, scripts, or
tests.

## Material inspected

- `book_of_seed/chapters/01_source_coordinates_and_grammar.md`
- `book_of_seed/chapters/07_result_boundaries_and_movement.md`
- `book_of_seed/witness_grammar.json`
- `seed_runtime/candidate_standing_from_exact_result_assertions.py`
- `seed_runtime/measurement_of_shared_position_of_byte_pair_occurrences.py`
- `seed_runtime/comparison_of_ordered_relation_path_with_recorded_pair_findings.py`
- focused Candidate, shared-position, and ordered-path Compare tests

Book material is primary orientation. Witness Grammar, runtime, and tests are
testimony.

## 1. The path already has one exact non-recursive consumer

Active `04.Compare.B` establishes this family-local physiology:

```text
current Standing in one Locality
├── one exact ordered relation-path Assertion
└── one exact recorded pair-finding comparison result
        ↓
Responsibility assignment
        ↓
Applicability of both exact input-to-Compare relations
        ↓
Participation under two exact roles
        ↓
Compare Act occurrence
        ↓
Yield
        ↓
bounded comparison finding
```

Applicability requires:

```text
path source occurrence
= exact added occurrence of recorded comparison

each exact path pair subject
has complete recorded comparison findings
```

The result preserves each ordered path role, exact pair subject, and exact
recorded finding reference. It establishes no source relation, recurrence,
represented relation, or composite material.

Therefore the current repository does not lack every non-recursive Act taking
an ordered path. It has one exact Compare family.

## 2. Candidate production now includes the path without selecting its partner

The exact path is one Assertion carried by a Measurement result. Under active
`01.Source.E.1`, that Assertion enters a later frozen Candidate source surface.

The complete ordered-pair Candidate Act records:

```text
Candidate(path, every other exact source Assertion)
Candidate(every other exact source Assertion, path)
```

with no self-pair. Each Candidate retains:

```text
first exact source Assertion reference
second exact source Assertion reference
independent source coordinates
represented_relation: Unknown
```

The integration testimony in
`test_positional_path_assertion_enters_every_required_ordered_candidate_without_material`
checks the exact `2 * (N - 1)` path-involving population under the frozen source
boundary. It also checks that the Candidate result occupies its own Locality
without moving the path Assertion from its source Locality.

Thus developer selection of one partner is absent from Candidate production.
Every partner required by the exact ordered Candidate Act is recorded.

## 3. Candidate source roles are not Compare input roles

The Candidate carries these Act-local source roles:

```text
first_source_Assertion
second_source_Assertion
```

The `04.Compare.B` occurrence carries different exact roles:

```text
ordered relation path
recorded pair comparison result
```

No current Assertion or responsible occurrence establishes:

```text
Candidate first_source_Assertion
= ordered relation path Compare input

Candidate second_source_Assertion
= recorded pair comparison result input
```

The source order also establishes no role assignment. A Candidate exists in
both orders, while `04.Compare.B` assigns distinct family-local roles.

```text
Candidate(path, comparison Assertion)
!= path participates in 04.Compare.B

Candidate(comparison Assertion, path)
!= path participates in 04.Compare.B
```

The Candidate's `represented_relation: Unknown` is one Candidate coordinate.
It is not the `04.Compare.B` input relation, Applicability finding,
Participation relation, comparison rule, or result by identity.

## 4. The exact current Compare assignment remains directly addressed

The runtime assignment entry receives:

```text
path_result_event_identity
comparison_result_event_identity
current Locality Standing
```

It then replays both exact results, validates the path Assertion, checks the
Locality, freezes the Standing boundary, and records the exact assignment.

Those validations make the recorded assignment exact after the two inputs are
supplied. They do not establish what responsible occurrence addressed those
two result identities together before assignment.

The Candidate result is absent from this assignment physiology. The assignment
does not read:

```text
Candidate result reference
Candidate Assertion reference
Candidate source roles
Candidate represented_relation coordinate
Candidate completeness boundary
```

Therefore current `04.Compare.B` does not consume the Candidate wrapper or use
Candidate completeness to establish its input pair.

## 5. Assertion reference and result reference remain distinct

Candidate production enumerates exact Assertions carried by Measurement,
Compare, and Candidate results. Its source reference includes an exact
Assertion coordinate within one result occurrence.

`04.Compare.B` instead requires:

```text
one exact path Assertion reference
one exact recorded pair-comparison result reference
```

The pair-comparison result can itself carry result and finding Assertions, and
those Assertions enter Candidate production. Their presence does not replace
the exact result reference required by `04.Compare.B`.

```text
Assertion carried by comparison result
!= comparison result by identity

Candidate source Assertion reference
!= 04.Compare.B result input relation
```

This is another endpoint boundary. Reference resolution alone establishes no
input relation.

## 6. Compare does not establish material carried by the path

The `04.Compare.B` reader recovers both exact pair subjects from the path's
two input position Assertions and locates complete recorded findings for each
pair subject. Its finding preserves those pair subjects beside the path and
comparison references.

Active Book explicitly limits the result:

```text
one pair subject under both inputs
establishes no source relation,
recurrence,
represented relation,
or composite material
```

Therefore this existing non-recursive consumer does not cross the separate
path-to-material vacancy:

```text
ordered path
+ two exact pair subjects
+ complete recorded findings
!= exact aggregate material carried by the path
```

It supplies distinctions about recorded findings, not a three-byte or larger
material occurrence.

## 7. The remaining assignment frontier

The repository now establishes both surfaces independently:

```text
SURFACE A
complete ordered Candidate field
including every path/source pairing required by E.1

SURFACE B
one exact family-local Compare
for an addressed path result and pair-comparison result
```

The missing crossing is not another generic Compare kind. It is the exact
responsible occurrence, if any, that establishes which Candidate-carried
source coordinates bear the two exact `04.Compare.B` input relations.

One future orientation under investigation is exhaustive assignment formation
over every exact Candidate whose lower sources satisfy the `04.Compare.B`
input species and Locality boundary. Current Book does not assign:

```text
Candidate-to-04.Compare.B Applicability traversal
complete 04.Compare.B assignment production
Candidate source-role translation
Candidate Participation in 04.Compare.B
```

The existence of every Candidate does not establish those occurrences.

## 8. Disposition

```text
path Assertion enters complete Candidate source surface       established
every E.1-ordered Candidate involving that path               established
Candidate represented relation                                Unknown
path source Locality preserved                                established

exact 04.Compare.B Responsibility and Act                     established
exact path participation in an addressed 04.Compare.B Act     established where recorded
exact recorded comparison-result participation                established where recorded

Candidate Assertion participates in 04.Compare.B              not established
Candidate source roles become Compare input roles              not established
Candidate completeness establishes Compare assignment         not established
exact occurrence addressing path and comparison result         not established by this road

04.Compare.B establishes path material                         no
04.Compare.B establishes represented relation                  no
recursive positional path formation                            not established
```

## Conclusion

The play exists, but the audition field does not currently cast it.

```text
complete Candidate field
        ↓
        ?
        ↓
exact 04.Compare.B assignment
        ↓
Applicability
        ↓
Participation
        ↓
Compare
```

Candidate production makes the path impossible to omit from the bounded
source-pair field. It does not assign either Candidate source role to the
existing Compare roles. The current Compare remains exact after its two input
results are directly addressed, while the responsible crossing from complete
Candidate field to that assignment remains unestablished.

That vacancy is independent of recursive path formation and path-to-material
recovery. No one supplies another by identity.
