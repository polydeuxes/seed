# Open-world consequence-asymmetry observation 001

## Boundary

This is a findings-only continuation from the exact internal-variation
population frozen at `bb950186` and mechanically compressed at `8fbc25e9`.

Discovery read only:

```text
the complete internal-variation manifest
the 17 exact source shards it addresses
the 17 exact source windows
```

It did not read the Book, witness grammar, Rosetta material, dictionaries,
expected words, language categories, organizer roles, heads, or roots.  It
made no Book, machine-grammar, runtime, or test-law amendment.

The question was not:

```text
which one of these positions governs another?
```

It was:

```text
where the source carries genuinely joint internal variation,
do substitutions at two coordinates yield the same immediate outward
material-population consequence or different consequences?
```

## Fair comparison boundary

Separate local variation is not enough to compare two coordinates fairly.
The two substitution frames may have different source support.

This pass therefore admits one coordinate pair only where the source carries
a complete substitution square while every other coordinate class remains
exact:

```text
x0 y0    x0 y1
x1 y0    x1 y1
```

Each cell is one complete exact production that independently recurs.  The
square gives each coordinate exactly two controlled substitution edges:

```text
change X while Y=y0
change X while Y=y1

change Y while X=x0
change Y while X=x1
```

No caller supplies `x0`, `x1`, `y0`, `y1`, the fixed remainder, or the number
of coordinate classes in the larger surface.

The complete bipartite cell graph is the retained representation.  Every
2x2 square remains exactly addressable from that graph; the artifact does not
eagerly copy all square objects.

## First consequence rung

For every recurrent complete production in a joint frame, the observer reads
the exact scalar immediately outside its complete source range on both sides:

```text
left adjacent scalar
right adjacent scalar
exact left/right pair
```

The material populations and their source occurrence counts remain exact and
separate.  Every production refers back to the prior shard carrying all exact
support start identities.

Same/different is measured over the exact distinct context-material
population.  Occurrence counts remain carried evidence but do not decide that
finding, so unequal support multiplicity cannot manufacture a difference.

For each controlled substitution edge, this pass asks only whether the exact
context-material population is:

```text
same
different
```

It does not pre-classify unequal populations as subset, overlap, dependency,
or direction.  Those would be later relation work over an already-addressed
unequal pair.

## Frozen findings

The complete source manifest is:

```text
/tmp/seed_open_world_consequence_asymmetry_manifest.json
bytes:            14,385
findings SHA-256: 8cc0e28597ea0e25b18e7d1af2c79262396fcc0be45c53674f7a1d5079c0980b
known loss:       none
```

Its 17 independently bounded source artifacts total:

```text
23,644,233 bytes
```

The controlled-square pressure projection is:

```text
/tmp/seed_open_world_consequence_asymmetry_pressure.json
bytes:            73,072
findings SHA-256: 86c684060eee5e53d7e9b6b665f104e8442e03963271b0f7bf347c63d6a8cf5a
known loss:       none
wall time:        39.538 seconds with six source workers
```

Every pressure result reopens its source artifact, verifies its byte count and
digest, reconstructs every square from the exact cell graph, and refuses a
source or frame whose reconstructed population differs from its frozen count.

## Population contraction

The prior source population contained 1,359 recurrent surfaces with at least
two separately varying coordinate classes.

The stronger shared-support requirement contracts it to:

```text
source surfaces carrying a complete joint square       389
exact fixed-remainder joint frames                    5,127
complete 2x2 substitution squares                  482,321
first-coordinate controlled substitution edges     218,110
second-coordinate controlled substitution edges    221,637
```

Thus:

```text
separate local variation
!=
joint factorial support
```

Every one of the 17 source populations contributes complete squares.

The square population by complete extent is:

```text
extent    squares
2         242,220
3         201,280
4          35,211
5           2,844
6             473
7             170
8              67
9              45
10              8
12              3
```

No extent was selected before observation.

## Primitive consequence findings

Across the complete controlled-edge population:

```text
context surface    same       different     total
left              21,835       417,912     439,747
right             21,525       418,222     439,747
joint                929       438,818     439,747
```

The fair square comparison counts how many of each coordinate's two controlled
edges changed the context-material population.  For the joint left/right
context, the complete result is:

```text
X changes    Y changes    squares
0            0                 12
0            2                 13
1            1                 26
1            2                797
2            0                 62
2            1              1,119
2            2            480,292
```

Only 1,991 of 482,321 squares have unequal joint change counts.  Almost every
joint immediate context changes under both coordinates.

The left and right surfaces separately expose a much larger mirrored
asymmetry:

```text
left context
first coordinate changes more than second       64,449
second coordinate changes more than first        4,503

right context
first coordinate changes more than second        4,880
second coordinate changes more than first       66,942
```

Here `first` and `second` preserve coordinate-class source order.  They do not
name grammatical roles.

At least one left, right, or joint inequality occurs in:

```text
129,357 complete squares
3,325 exact joint frames
277 source surfaces
```

All 17 sources contribute such squares.

## Why this is not a head

The dominant result is a mirror around the complete source range:

```text
earlier coordinate-class variation
→ more left-adjacent consequence difference

later coordinate-class variation
→ more right-adjacent consequence difference
```

That is exact positional pressure, but the first explanation supplied by the
measurement itself is boundary proximity.  The operation observes one scalar
immediately outside each side.  It would be circular to rename this mirrored
surface organization or dependency.

The joint context strengthens the refusal.  In 480,292 squares both
coordinates change both-sided context on both controlled edges.  A mere
difference in one immediate neighboring population does not establish which
internal coordinate organizes the complete structure.

Therefore:

```text
immediate side-specific consequence asymmetry
!=
organizer/dependent relation

boundary proximity
!=
head

more changed contexts
!=
governance
```

The 1,991 joint-asymmetric squares are exact later subjects.  They are not
promoted by this report.

## Performance and representation

The first source representation copied every support-index list into every
context finding and expanded every substitution edge.  Eight completed shards
already occupied 87 MiB before the bounded run stopped.

The faithful corrections preserve:

```text
prior production index
→ exact support identities remain at the referenced internal-variation shard

one source-local context population
→ intern once

one fixed frame graph
→ every controlled edge and complete square remains addressable

same/different is the measured relation
→ do not compute subset/overlap relations prematurely
```

The proving Chinese artifact fell from 954 KiB to 198 KiB before later
source-wide interning reduced the final representation further.

Complete content-addressed source artifacts are resumable.  A bounded parent
run no longer recomputes a complete source because a sibling source remained
unfinished.  Reconstructing the complete 17-source manifest from verified
source artifacts took 46.998 seconds.

Fresh mechanics are not fully beneath the desired boundary.  In one six-worker
pass, the first six large sources recorded:

```text
Spanish       57.528 seconds
Franklin      59.438 seconds
Goold Brown   61.637 seconds
Grimm         66.329 seconds
Emerson       69.936 seconds
Roget         76.811 seconds
```

The completed artifacts are exact, but the sources above one minute remain a
mechanics bottleneck.  The pressure projection itself finishes in 39.538
seconds once those exact shards exist.

## Exact stopping boundary

The open-world road now reaches:

```text
source-derived recurrent same/different surface
↓
several separately varying coordinate classes
↓
source-carried complete 2x2 substitution square
↓
exact immediate left / right / joint consequence populations
↓
controlled same/different findings
↓
mirrored boundary-proximity pressure
↓
STOP
```

The next unbiased question is not which coordinate is a head.  It is whether
any exact consequence asymmetry survives when consequence extent is itself
allowed to grow by source-supported recurrence beyond the immediate adjacent
scalar.

Only a pressure that survives that outward growth can begin to separate
boundary proximity from a more persistent directional relation.  Even then,
the yielded finding will be only that exact persistent asymmetry.  Organizer,
dependent, head, and root remain unestablished.

## Finding

The source naturally supplies hundreds of thousands of fair, jointly supported
substitution squares.  Their first outward consequence is not featureless, but
its strongest directional surface follows the nearest source boundary.

The experiment therefore advances and refuses at once:

```text
directional pressure is measurable

but

the first pressure is positional proximity,
not hierarchy
```

That refusal is the exact input to the next rung.
