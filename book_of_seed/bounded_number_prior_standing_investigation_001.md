# Bounded number prior Standing investigation 001

## Question and boundary

The open-world scalar road remains parked at `ccfa9729`:

```text
manifested material
-> exact recurring structure
-> controlled consequence pressure
-> mirrored source geometry
-> no warranted hierarchy
-> STOP
```

This investigation tests a different orientation:

```text
bounded warranted domain Standing
+ exact subjects and Evidence
-> generic Seed physiology
-> new exact consequences
```

It does not ask manifested material to reconstruct the domain that produced it.
It also does not provide the desired consequence as testimony.

The first proving domain is a finite natural-number prefix with opaque subject
identities.  The permitted prior testimony is only:

```text
exact subjects
one distinguished origin subject
exact sameness/equality testimony
one exact successor relation
an exact finite boundary
Unknown beyond that boundary
```

No arithmetic expression, numeral meaning, magnitude, addition, subtraction,
multiplication, infinity, HEAD, parser, or evaluator is supplied.

This report changes no Book chapter, machine grammar, runtime, script, or test.

## Amendment after boundary review

The first version compressed Recording, Admission, and Standing establishment
into the phrase `prior-Standing testimony admission`.  That phrase is
withdrawn.

Recording preserves that one exact testimony or Assertion exists with its
source coordinates.  Required Admission is a separate boundary before
Participation.  Neither makes the recorded content Standing by identity.

The repository already has the boundary skeleton needed for responsible work
over new material:

```text
exact source/current-Standing address through B
+ one exact recorded testimony T
-> Responsibility-specific source rule R
-> exact bounded input surface
-> Applicability / required Admission / Participation where required
-> responsible Act occurrence under Authority and Scope
-> Yield
-> result through later boundary C
-> replay validates every coordinate owed relative to B and T
```

`C Complete` is report orientation for that last replay property, not an active
constitutional object.  `B` is an immutable append-prefix address, not
Standing or a source-population rule by identity.  `R` determines what through
`B` belongs to this Responsibility.  A new testimony appended after `B` also
requires its own exact reference; chronology cannot read it backward into B.

The missing owner is therefore not a generic recorder or an operation that
promotes supplied material automatically.  It is one generic responsible
Standing-establishment crossing that consumes already-recorded bounded
testimony relative to an exact prior boundary, determines Applicability under
Authority and Scope, and yields the exact result that current Standing may
carry for the Responsibility's exact subject.

## Direct finding

The active Book can describe the proposed prior testimony without a Number or
Math species:

- `01.Standing.A` can concern each opaque exact subject;
- `01.Standing.D` and `01.Standing.E` can carry exact binary relation
  Assertions and their responsible occurrences;
- `01.Standing.D.1` can preserve an exact origin Assertion and its source
  coordinates;
- `08.Authority.B` and `08.Scope.A` can preserve the testimony source and its
  finite experimental boundary;
- exact subject identity and explicit relation testimony do not depend on a
  numeral rendering;
- generic `04.Compare` can constitutionally compare exact subjects under an
  exact rule.

The live runtime can record family-specific Assertions and results, but it
cannot yet establish Standing from bounded prior-domain testimony through one
generic responsible road.  There is no Responsibility that consumes one exact
recorded testimony source relative to an exact prior boundary, performs its
Applicability/Participation work under Authority and Scope, and yields a result
carrying warranted Standing coordinates for the testimony's exact subject.

There is a second downstream vacancy: no active generic operation establishes
an arbitrarily bounded ordered path from already-standing relation occurrences.
The only live ordered-path producer is the source-byte shared-position
Measurement.  It accepts two overlapping byte-pair position Assertions,
produces exactly three source positions, and cannot extend path-plus-edge or
path-plus-path.  It also cannot represent the zero-edge identity path required
by the granted origin/equality structure.

The first missing capability is earlier than path comparison:

```text
exact recorded bounded testimony T
+ exact prior boundary B
-> exact Standing-establishment Responsibility
-> Applicability / Participation
-> responsible Act occurrence
-> Yield
-> result through C, complete relative to B, T, and its exact rule
-> current Standing carries that result for the Responsibility's exact subject
```

Without that occurrence, a test fixture containing Python dictionaries would
be developer state outside Seed.  Calling those dictionaries Standing would
invalidate the experiment.

Disposition:

```text
one exact generic capability is missing
-> responsibly establish Standing from one recorded bounded testimony surface
-> STOP
```

Do not build the successor-path consequence until that input boundary exists.

## Existing B-to-C physiology

The recovered Candidate-production road gives the most complete witness:

```text
B = exact immutable source append boundary
R = exact source-Assertion enumeration rule
S(B) = exact source surface R reads through B
G = exact formation rule
E = every coordinate G requires from S(B)

C = later production-result boundary
A = exact ordered coordinates actually recorded through C

complete result
iff
E and A agree under G's identity, order, role, duplicate, and integrity rules
```

The result must preserve enough coordinates to recover both B and C.  A later
append cannot enter `S(B)`.  An occurrence merely present through B does not
enter `S(B)` unless R addresses it.

The declared-Measurement road independently demonstrates that several
Responsibilities can take exact subjects from one frozen B while recording
their lifecycles in either durable order.  Each assignment preserves the same
responsible Standing boundary.  The exact-byte Measurement separately
preserves B as its completeness boundary.  Durable append order does not make
one Measurement an input of the other.

The byte-position source reader makes the refusal explicit:

```text
availability through B
!= required Locality relation
!= assignment subject
!= Responsibility assignment
!= Applicability
!= Standing
```

Thus B/C supplies the honest stable comparison surface and replayable
completeness mechanism.  It does not remove the need for the exact
Responsibility and Act that interpret recorded testimony as an input to a
Standing-establishing result.

For a new testimony `T`, two lawful arrangements remain mechanically possible
until the responsible road is recovered:

```text
prior Standing through B
+ separately addressed later testimony T

or

one later frozen input boundary B_T whose rule R addresses T
```

They are not interchangeable by chronology.  The missing Responsibility must
state which boundary is its prior Standing, which occurrence is its testimony,
and which exact population its rule owes.  This report does not choose the
shape from implementation convenience.

## 1. Capability inventory

| Required capability | Active owner | Runtime testimony | Disposition |
| --- | --- | --- | --- |
| Exact opaque subject | `01.Standing.A`, `01.Standing.D.1` | Many families preserve exact subject references | Constitutionally usable; no generic testimony-to-Standing Responsibility |
| Standing concerning the subject | `01.Standing.A`, `01.Standing.A.1` | Locality readers carry results under exact Responsibility ownership | Constitutionally usable; generic Standing-establishment owner missing |
| Distinguished origin | one exact Assertion under `01.Standing.D.1` and a responsible recording occurrence under `05.Recording.A` | Assertion recorders are embedded in source-specific result families | Recording preserves the testimony; separate Standing-establishment work is missing |
| Exact binary successor relation | `01.Standing.D`, `01.Standing.E` | Participation, Carriage, Yield, Locality, Support, and several measurement relations each have dedicated writers | Generic relation anatomy exists; no generic testimony relation recorder |
| Exact sameness/equality | explicit exact relation testimony under `01.Standing.E`, or exact subject comparison under `04.Compare` | Live same-content findings compare byte/material coordinates inside specialized families | No number-specific species required; live generic subject comparison absent |
| Relation occurrence and Evidence | `01.Standing.D`, `01.Standing.E`, `02.Acts.A` | Every active family records its own Responsibility, Act Evidence, occurrence, Yield Evidence, and result | Shape is established; no family-independent input road |
| Bounded Scope and Unknown | `08.Scope.A`, `08.Authority.B` | Active families preserve their family-local bounds | Usable if the testimony road carries the exact finite subject/edge population and refuses a universal terminal claim |
| Ordered relation path | consumed by `04.Compare.B`; byte-specific production under `01.Source.D` | shared byte-position Measurement creates one two-edge, three-position path | Too specialized and fixed in extent |
| Compare exact path structure | generic `04.Compare` | path Compare implementations require byte-position paths, pair findings, or source-position material | Constitutional rule is generic; live owners are specialized |
| Preserve a later derived relation | `01.Standing.E` carries first subject, exact relation content, and second subject | no generic relation-result writer | Exact representation must be recovered after path work; do not assign `B` to relation content yet |

## 2. The proposed prior Standing does not require number grammar

### 2.1 Opaque subjects

The witness subjects can be rendered to the developer as `N0` through `N5`,
but their internal identities need carry no numeral material:

```text
sable
fern
cobalt
ivory
amber
quartz
```

The familiar rendering is a separate observer map:

```text
sable  -> "0"
fern   -> "1"
...
```

Changing or deleting that map must not change any relation occurrence, path,
comparison, or derived result.

### 2.2 Origin

The testimony distinguishes `sable` as origin.  That may be preserved as one
exact Assertion concerning `sable`, with the testimony source and responsible
recording occurrence.  `origin` is the content supplied by the domain
testimony; it is not inferred from spelling or position in a Python tuple.

The experimental boundary does not say the origin is the universal origin of
every number system.  It says only that this testimony identifies it as origin
inside this exact bounded system.

### 2.3 Successor

The permitted relation population is explicit testimony:

```text
sable  --R--> fern
fern   --R--> cobalt
cobalt --R--> ivory
ivory  --R--> amber
amber  --R--> quartz
```

Every edge requires its own exact relation occurrence.  `R` is one exact
relation content supplied as successor testimony.  The subject names and their
source order establish no edge by themselves.

The testimony says nothing about a successor beyond `quartz`:

```text
successor beyond the experimental boundary: Unknown / outside Scope
```

It does not assert that `quartz` universally lacks a successor.

### 2.4 Equality/sameness

Exact reference identity is enough to recognize that two path endpoints name
the same supplied subject.  It must not silently be renamed mathematical
equality.  If the domain grants equality as a relation, that relation must be
explicit testimony with its own exact occurrences.

For the first path experiment, the zero-edge path requires an exact subject at
both endpoint roles and no successor occurrence.  It does not manufacture a
self-successor edge.

## 3. Why current ordered-path machinery is not reusable

The live producer in
`measurement_of_shared_position_of_byte_pair_occurrences.py` requires:

```text
byte-pair position Assertion (i, i+1)
byte-pair position Assertion (i+1, i+2)
same exact shared byte-position coordinate
-> ordered_relation_path Assertion
```

Its inputs and validation require:

- byte material;
- source-material acquisition occurrence;
- integer source positions;
- position-coordinate digests;
- one material completeness boundary;
- exact byte-pair Measurement result families.

Its result is always a two-edge, three-position source path.  The producer does
not accept an earlier path as input.  The path readers and live consumers also
resolve byte-position assertions and byte material.

Therefore substituting successor Assertions into this API would require fake
byte coordinates and a fake material-acquisition source.  That would make a
source representation detail constitutional by accident.

Active `04.Compare.B` confirms that an ordered relation-path Assertion can be a
Compare input.  It does not define how a general path is established.  The
active Book presently has no generic clause stating:

```text
ordered exact relation occurrences
+ matching endpoint roles
+ exact finite boundary
-> one bounded ordered relation path
```

Nor does it state the zero-edge case.  A later generic path recovery must
preserve at least:

```text
exact ordered subject references
exact ordered relation-Assertion references
exact relation-occurrence references
first and final subject
testimony Standing boundary
completeness boundary
Authority
Scope
Locality
limits
Unknown
```

This report does not add that law because the earlier testimony-to-Standing
Responsibility is not live yet.

## 4. The bounded structural consequence

Once the prior Standing is lawfully carried, the intended non-arithmetic
operation is:

```text
origin O --R...R--> extent subject B
start  A --R...R--> result subject C

the two paths carry the same bounded relation-path structure
-> exact derived relation coordinates (A, B, C)
```

`B` is not chosen because its rendering looks like a magnitude.  It is the
exact endpoint of the origin path whose structure is compared with the path
from `A`.

The observer may report the consequence as `(A, B, C)`.  That tuple does not
decide the constitutional relation representation.  In particular, this
investigation does not establish that `B`, a path reference, or a compound of
both belongs in `exact relation content`.  The generic path and comparison work
must first establish what exact result is produced and which later
Responsibility, if any, can preserve it as a relation Assertion.

Only after freezing that population may an observer say that it corresponds
to bounded addition.

## 5. Disposable opaque-identity probe

A read-only disposable probe used only the explicit edge map above.  It:

1. began at each exact subject;
2. followed only supplied successor edges;
3. preserved each edge relation-content occurrence in order;
4. associated each origin path with its exact endpoint `B`;
5. matched another path only when the complete relation-content sequence had
   the same extent;
6. emitted `(A, B, C)` references.

It did not call host-language integer addition to obtain `C`.

For six opaque subjects and five supplied edges it found 21 bounded triples.
Post-hoc numeral rendering produces:

```text
(0,0,0) (0,1,1) ... (0,5,5)
(1,0,1) (1,1,2) ... (1,4,5)
(2,0,2) ... (2,3,5)
(3,0,3) ... (3,2,5)
(4,0,4) (4,1,5)
(5,0,5)
```

This is observer feasibility testimony only.  It is not a Seed result because
the prior Standing, paths, Compare occurrences, and derived relation
occurrences were not recorded through live physiology.

## 6. Required falsifiers

### 6.1 Subject renaming

Apply a bijection to every opaque subject identity and every endpoint
reference.  The recovered graph, path population, and derived relation
population must be isomorphic.  Any dependence on `N0`, `N1`, or lexical order
is a refusal.

### 6.2 Rendering replacement

Replace familiar numeral renderings with arbitrary bytes or omit them.  The
Seed event population must remain identical because rendering is outside the
testimony Authority.

### 6.3 Wrong successor edge

An edge with a missing subject, changed relation occurrence, duplicate
successor from one first subject, or changed endpoint must fail exact testimony
validation.  The expected relation population must not repair the edge.

### 6.4 Missing intermediate occurrence

Remove one required edge occurrence.  Every path crossing that gap is absent.
No transitive endpoint relation may bridge it merely because the observer
expects a result.

### 6.5 Different extents

Two paths with different complete ordered relation-occurrence counts do not
have the same path structure under this rule, even if an endpoint rendering is
familiar.

### 6.6 Outside boundary

Where the required successor lies beyond the supplied Standing boundary, the
derived relation is unavailable/`Unknown`, not false.  No terminal-subject
claim may be inferred.

### 6.7 No result oracle

The implementation may walk supplied edge references, compare exact path
structures, and resolve endpoints.  It may not use:

```python
int(subject_rendering)
a + b
expected_sums[(a, b)]
```

to choose or validate `C` inside discovery.

## 7. Calculator testimony

The toy calculator witness demonstrates why system Standing must precede
manifestation brute force, but it is not part of the first number build.

Three subjects were previously compressed:

```text
exact calculator system C
!= one calculator invocation occurrence
!= one arithmetic expression
```

The current witness records two separate roads:

```text
calculator-claim Locality
-> bytes such as `2+2=5\n`
-> pair Measurement
-> addressed byte position
-> shared-position path
-> Compare findings

calculator-operator Locality
-> command bytes
-> host-provider mechanics
-> stdout bytes `4\n`
```

It explicitly says no current Responsibility compares the invocation result
with the path finding or establishes arithmetic meaning.  It also does not
record one exact calculator subject carrying several invocation occurrences.

Thus the witness began at two manifestation leaves and tried to recover
structure separately.  A better later experiment would preserve:

```text
calculator subject C
└── exact invocation occurrence
    ├── exact input
    └── exact output
```

without claiming what function C computes.  Separately, expression Standing
may later preserve an operator-like organizer.  Neither distinction belongs in
the first number-system experiment:

```text
number subject
!= numeral rendering
!= arithmetic expression
!= calculator system
!= calculator invocation
```

## 8. Smallest missing coordinate and stop

The proposed testimony and later Standing are separately representable in
active grammar, but no live generic physiology owns the responsible crossing
between them.

The smallest missing runtime physiology begins with one already-recorded exact
testimony occurrence carrying:

```text
testimony source occurrence
exact bounded subject population
exact origin Assertion and its recording occurrence
exact relation Assertions and relation occurrences
Authority
Scope
Locality
limits
Unknown beyond the experimental boundary
```

One separate exact Responsibility must then address that testimony relative to
the exact prior boundary B, establish Applicability and Participation, perform
its exact Act, and Yield a result through C.  Replay must recover the source
surface owed by the rule through B, validate the testimony reference, and
refuse missing, additional, substituted, repeated, or reordered result
coordinates.  Only that result may become a coordinate of current Standing for
the exact subject of its Responsibility branch.

This must not be a `NumberStanding` object, a fixture dictionary treated as
truth, a global relation registry, or a rule that every recorded Assertion
automatically gains Standing.

After that boundary exists, the next separate investigation can recover the
smallest generic bounded ordered-path operation, including zero-edge and
arbitrary finite extents, and then test path-structure comparison.

Final disposition:

```text
one exact generic capability is missing
-> responsible Standing establishment from recorded testimony relative to B
-> name no arithmetic consequence yet
-> STOP
```

The principle remains viable but untested live:

```text
Seed need not rediscover that a domain has irreducible structure.
Prior warranted Standing may state that structure.
Seed must still determine what follows from it.
```
