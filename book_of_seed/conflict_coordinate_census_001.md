# Conflict coordinate census 001

## Boundary

This investigation begins at `a33f3949`, after `Unknown` was withdrawn from
live Seed physiology. It changes no Book clause, Witness Grammar coordinate,
runtime occurrence, reader, or test.

The census separates:

```text
an exact Act result or Compare finding that establishes conflict
!=
a universal conflicts collection copied through unrelated occurrences
```

DNA/RNA supplies the adversarial question:

```text
what exact interaction established the conflict,
between which exact subjects,
under which exact occurrence and boundary?
```

Absence of an agreement is not a conflict by identity.

## Exact conflict testimony

Live Compare produces `conflicting_findings` when two exact findings address
the exact subject and carry different exact content. The finding preserves the
earlier and later Assertion references and both contents.

Live Applicability admits `conflicting` as a distinct possible result. No
current road inspected here produces that value, so this census does not
subtract or reinterpret it.

These are exact results or findings. They are not evidence that every
Assertion, binding, Act, result, source, or current-coordinate read needs a
generic conflicts collection.

## Generic conflicts census

The live runtime constructions inspected here create generic `conflicts`
coordinates only as empty lists. They occur in:

```text
operator invocation Locality result
ordered-path source-position Compare lifecycle
source-position Measurement bindings
recurrent exact-material Measurement coordinates
bounded current-coordinate projection
```

The current-coordinate projection merges strings from those occurrence
lists, but no live producer supplies one. No generic conflict occurrence,
identity, Yield, result, address, or reader exists independently of the
surrounding occurrence.

## Smallest exact finding

```text
conflicting Compare finding                established
possible conflicting Applicability result preserved
generic nonempty conflicts producer        not established
generic conflicts occurrence/address       not established
empty conflicts lists                       copied staging
current-coordinate conflicts collection     no live positive input
```

## Next falsifiable frontier

Subtract the empty collections one live road at a time. Preserve exact
subjects, exact conflicting findings, possible conflicting Applicability,
Locality, Act occurrences, Yield, results, loss, current boundaries, and every
refusal.

Only after the live roads survive may Book and Witness Grammar withdraw the
generic `conflicts` coordinate. Do not introduce Conflict, Agreement,
Consistency, or another replacement object.
