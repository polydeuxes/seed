# Open-world outer-context dependence observation 001

## Boundary

This is a findings-only continuation from the frozen variable-extent artifact
at `15d738d6`.

Discovery read only:

```text
frozen variable-extent artifact
frozen extent-relation artifact
the exact source windows addressed by those artifacts
```

It did not read the Book, machine grammar, Rosetta material, dictionaries,
language labels, grammar categories, or expected words.  It made no Book,
machine-grammar, runtime, or test amendment.

No source material was rewritten.  What this report renders as an internal
substitution is always two materials that the source itself placed in distinct
branches of one recurrent frame.

The governing refusal remains:

```text
current Seed cannot name distinction X
!=
X has earned constitutional grammar
```

## Frozen artifacts

The exact outer-context artifact is:

```text
/tmp/seed_open_world_outer_contexts_blind.json
bytes:            2,225,955
findings SHA-256: f8eb504b6e75fbe4e7424a08427858d7f65db1141b81807beaecac999cdb313c
known loss:       none
wall time:        48.864 seconds
```

The exact population-pressure projection is:

```text
/tmp/seed_open_world_outer_context_pressure.json
bytes:            50,200
findings SHA-256: 4c21bc20a5556b5adee2b8b077b19f96ae326f4960cde619ce4866e1ea1f9949
```

Wall time is mechanics testimony and is not encoded into either canonical
findings artifact.

## Exact operation

For one frozen variable-extent branch, the original recurrent frame range is
the internal bound.  Every already-addressed left/right extent around that
range becomes one exact outer context:

```text
left material outside the frame
right material outside the frame
exact supporting extent-family reference
exact support population reference
```

For each pair of naturally occurring internal branches under one frozen frame,
the observer measures:

```text
outer contexts shared by both branches
outer contexts carried only by the first branch
outer contexts carried only by the second branch
exact support populations for each shared context
```

The first-only and second-only populations remain exact set differences against
the two referenced branch populations.  The artifact does not repeat every
reference in those differences.

## Performance boundary and correction

The first implementation crossed one minute in Grimm frame 3 branch 1.

Two repeated representations caused the delay:

```text
one outer context
→ copied all exact source ranges already carried by its support reference

30,988 exact contexts
→ serialized as 30,988 independent records despite their extent families
```

The correction preserves:

```text
support reference + exact occurrence count
→ full ranges remain at the frozen variable-extent address

support-preserving context family
→ complete left/right coordinate rectangle remains addressable
```

Pair comparisons carry every exact shared context.  A branch-only population is
preserved as an exact set difference from the referenced complete branch
population.

The complete 17-window observation then finished in 48.864 seconds with no
unfinished frontier.

## Measured population relations

The source supplied 39 internal branch pairs.

```text
outer-context population relation                  count
equal                                                   4
first branch is a proper subset of second               8
second branch is a proper subset of first              10
partial overlap                                        17
                                                     ----
                                                       39
```

Every comparison carries at least one shared nonempty outer context.

That result does not mean every branch preserves deep external structure.  The
three most recurrent shared nonempty contexts are:

```text
(" ", "")    in 28 comparisons
("", " ")    in 28 comparisons
(" ", " ")   in 28 comparisons
```

These are exactly the immediate space surfaces around many frozen apertures.
They establish that distinct occupants remain possible inside that local
surface.  They do not establish a dependency relation.

Four branch comparisons have exactly equal outer-context populations:

```text
Franklin: loss / news
Russian frame 1: 23 / 3
Russian frame 1: 23 / 7&#189;
Russian frame 1: 3 / 7&#189;
```

Each of these equal populations contains only four context surfaces: the empty
outer context and the three immediate-space surfaces.  Equality is exact but
the established aperture remains shallow.

Other branches preserve deeper common contexts.  The largest belong primarily
to representation/layout recurrence:

```text
Hume line-break / space branches       up to 27 outer scalars
Grimm illustration/layout branches    up to 26 outer scalars
Kittredge layout branches              up to 3 outer scalars
```

The source also preserves bounded non-layout common contexts, including:

```text
Latin frame 3                         up to 10 outer scalars
Russian frame 3                       up to 3 outer scalars
many other branch pairs               only the two immediate spaces
```

No ordinary-language role is assigned to these populations.

## Same internal material under distinct frames

Only two exact internal materials occupy recurrent varying positions in more
than one distinct frame:

```text
LF
    context populations: 27 / 520
    shared:               9
    first-only:          18
    second-only:        511

CR
    context populations: 28 / 642
    shared:                8
    first-only:           20
    second-only:         634
```

Thus the outer consequence does not follow exact internal material alone.  The
same material participates in sharply different context populations when its
source/frame position changes.

This is real positional pressure, but its only current testimony is layout.
It does not yet establish a language-bearing dependency.

## Why direction does not follow yet

Every frozen recurrent frame in this observation has exactly:

```text
one independently varying internal position
```

The other carried material is stable aperture material.  Consequently the
observer can measure:

```text
change the one internal occupant
↓
outer population equal / contained / overlapping
```

It cannot compare:

```text
change internal position X
vs
change independently varying internal position Y
under the same exact larger source boundary
```

Population size, proper inclusion, or greater outward reach cannot substitute
for that missing comparison.  They may arise from occurrence multiplicity,
duplicated representation material, or one branch's source distribution.

Therefore:

```text
outer-context consequence difference
!=
directional dependency
```

## Nested composition does not repair the vacancy

The prior observation established 100 exact frame-position occupancies after
duplicate derivations were removed.  All 100 are in the Grimm illustration /
spacing population.

They provide two containment levels:

```text
inner recovered layout extent
↓ occupies
outer layout-frame varying position
```

But each level still exposes only one independently varying position, and no
measured relation says that one level's substitution consequence governs the
other's.

Thus:

```text
nested containment
!=
parent-like relation
```

No independently established organizer relation is available to compose.

## Exact stopping boundary

The current road reaches:

```text
source-selected scalar aperture
↓
recurrent internal occupant population
↓
support-preserving variable extents
↓
exact outer-context populations
↓
equal / proper-subset / overlap findings
↓
STOP
```

The missing subject population is:

```text
one exact larger source-derived structure
├── independently varying internal position X
├── independently varying internal position Y
└── exact outer-context consequences for changing X and Y separately
```

Without that population, there is no measured asymmetry from which a local
organizer/dependent-like direction could arise.

## Route toward a topmost organizer

The next source aperture should not ask for a head, dependent, modifier, or
root.  It should recover a bounded naturally occurring population in which at
least two internal positions vary independently while one larger occurrence
boundary remains exact.

Only then can the observer measure:

```text
substitution at X
→ exact outer contexts retained / lost

substitution at Y
→ exact outer contexts retained / lost

X consequence population
Compare
Y consequence population
```

If one directional relation is established locally, later source evidence may
compose those exact relations.  A topmost organizer could be reported only
when every other organizer in one bounded structure has an established upward
relation and no further upward relation is established for that one.

That topology is not present in this artifact.

## Finding

Looking outward adds genuine pressure.  Internal substitutions yield four
different exact relations among their outer-context populations, and the same
material can behave differently in distinct source positions.

The pass does not recover a root-like structure.  It establishes why: the
current source-selected aperture has only one independently varying internal
position.  Direction requires at least two such positions under one exact
larger boundary.  Larger extents, deeper recurrence, or more frequent material
cannot manufacture that missing coordinate population.
