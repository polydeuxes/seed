# Direct / already vocabulary subtraction report 001

## Result

`direct` and `already` are absent from active Book chapters and Book lexical
admission.

`direct` had been admitted for one Locality sentence:

```text
Act to establish a direct Locality relation
```

The clause independently records the exact relation occurrence and its
coordinates:

```text
recorded through-occurrence boundary reference
Locality Act
destination Locality
recorded result = Locality relation occurrence
```

Removing `direct` loses no coordinate, occurrence, order, or relation.

## Compression found

The same adjective had been used in implementation prose for several
different claims:

```text
no separate Applicability lifecycle
result content addressed by parent occurrence + local position
one Locality relation whose occurrence is the result
one source reference rather than an intermediate wrapper
```

Those are not one distinction. The active clauses and the two current
Applicability subtractions now state their exact coordinates or explicitly say
which Applicability occurrences are absent.

## `already`

`already` was not admitted Book vocabulary. In the current work it compressed:

```text
established before this occurrence
present in current coordinates
one result exists for this Act
```

The active Book did not use it. Current subtraction reports and changed
runtime refusals now name the exact state instead:

```text
established before the Act
carried in current coordinates
Act has one result
```

No temporal coordinate is inferred from `already`.

## Validation

```text
active Book / lexical admission / Locality controls  69 passed
byte-pair / recorded-pair / current-coordinate       139 passed
active Book chapter occurrences of either word       zero
Book admission entries                               zero
```
