# Scope coordinate census 001

## Boundary

This investigation begins at `87e5c540`, after separately copied Rule prose
was removed from active Book grammar and the inspected runtime families. It
changes no Book clause, Witness Grammar coordinate, runtime occurrence,
reader, or test.

The census asks whether Scope is an independently exact Seed subject or a
human name for exact coordinates already carried with another subject,
relation, Act, occurrence, result, boundary, or Locality.

The DNA/RNA adversarial control is:

```text
independently real bounded condition
    an exact boundary, relation, material condition, or occurrence changes
    and a later occurrence can address that distinction independently

observer orientation
    exact participants and local coordinates already bound the interaction
    an observer calls their bounded extent a scope
```

The test does not ask whether exact boundaries matter. It asks what work is
done by a separately carried `Scope` coordinate.

## Active Book declaration

Active Book grammar says:

```text
Scope bounds subjects, relations, Acts, occurrences, results, and Localities
```

Subject-to-Act binding, Support, Participation, Carriage, Yield, source
preservation, Measurement, Compare, Movement, Locality relations, and
Emission all list Scope among their exact coordinates.

The Book does not currently declare a Scope-producing Act, Scope occurrence,
Yield to Scope, Scope result, Scope source, Scope provenance, or independently
addressed Scope reference.

## Runtime census

The live runtime contains 76 stored or copied `scope` dictionary surfaces
across 16 modules. Repository search finds no live:

```text
scope_identity
scope_reference
scope_occurrence
scope_result
record_scope
read_scope
```

Every Scope dictionary is reached through another exact occurrence. Readers
either:

```text
reconstruct the parent occurrence and compare its embedded Scope dictionary

or

copy the embedded dictionary from binding to Act to result
```

No later road addresses a Scope independently of its parent occurrence.

## Carried coordinates

The contents of Scope dictionaries are exact and often load-bearing. They
include coordinates such as:

```text
Locality identity
source material-result occurrence
completeness boundary
through-occurrence boundary
addressed Act identity
source Assertion reference
destination Locality
support result references
```

Those coordinates must not disappear merely because the Scope wrapper is
suspect.

However, the inspected Scope dictionaries repeatedly copy coordinates already
carried elsewhere:

```text
plain-byte Measurement
    scope.source_localities
        == binding.source_localities

    scope.completeness_boundary_identity
        == binding.completeness_boundary_identity

Assertion movement
    every Scope coordinate
        == one top-level binding coordinate

occurrence-position and recurrent-position Measurement
    every Scope coordinate
        == one top-level binding coordinate

Applicability families
    scope.addressed_act_identity
        == binding.addressed_act_identity

one-Locality families
    scope.locality_identity
        == parent occurrence Locality
```

Other families place a coordinate only inside the Scope dictionary, but that
does not establish an independent Scope subject. The coordinate may instead
belong directly to the parent binding, relation, Act, or boundary.

## Addressability

The durable address is always the parent occurrence identity plus its exact
carried coordinates. Scope currently adds neither:

```text
an independently minted identity
an independently recorded occurrence
an independently produced result
an independently addressed relation
a lookup boundary unavailable from the parent occurrence
```

Changing an embedded Scope copy is refused when the parent reader reconstructs
its full material. That establishes exact-copy consistency, not an independent
Scope physiology.

## DNA/RNA comparison

Biological interactions have exact local extents: a binding pocket, membrane
boundary, molecular complex, ribosomal position, or covalent material extent.
Those distinctions are real because exact structure or relation changes and
later interactions can address the changed condition.

Biology does not require another object named Scope beside those coordinates.
The current Seed Witness similarly shows exact boundaries and Localities, but
no independently participating Scope.

This does not establish that bounded extent is unreal. It separates:

```text
exact boundary and Locality coordinates    established
Scope as a separately exact subject        unestablished
embedded Scope dictionary                  repeated coordinate projection
```

## Smallest exact finding

Scope currently has no independent identity, occurrence, result, reference,
or relation. Its embedded dictionaries project exact coordinates carried by
their parent occurrences. Some projected coordinates are essential; the
separately named wrapper has not shown an additional discriminator.

## Falsifiable runtime frontier

Start with plain-byte Measurement because its entire embedded Scope dictionary
is copied from two top-level binding coordinates:

```text
source Localities
completeness boundary
```

Remove only the embedded Scope dictionary. Preserve the exact material-result
subjects, source Localities, completeness boundary, recording Locality,
through-occurrence boundary, Act lifecycle, Yield, findings, replay, and every
refusal.

Do not replace Scope with another wrapper or identity. If a later occurrence
cannot be read or refused without the embedded dictionary, stop at that exact
consumer. If the complete live road survives, repeat only where all projected
coordinates are independently exact.

