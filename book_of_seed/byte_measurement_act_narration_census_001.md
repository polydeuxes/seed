# Byte Measurement Act-narration census 001

## Question

Does `exact-byte Measurement` identify an Act distinct from `Measurement`, or
does it repeat the subject family inside the Act word?

This census changes no runtime road, Book clause, Witness Grammar, admission,
Act, result, or occurrence.

## Book testimony

`01.Source.D` addresses:

```text
exact Measurement subject-to-Act binding
+ Measurement
→ Measurement result
```

The Witness Grammar independently records:

```text
01.Source.D.exact_Act = Measurement
```

Neither surface declares `exact-byte Measurement` as an Act. The byte domain is
carried by the exact material-result subjects and the result findings.

## Runtime shape

The plain-byte Act currently carries:

```text
act = exact-byte Measurement
exact material-result subject references
source Localities
source completeness boundary
recording cut
Act Locality
```

Holding those coordinates fixed leaves no control in which
`exact-byte Measurement` and `Measurement` are separate Acts. `exact-byte`
narrates which exact subjects this Measurement addresses.

The result repeats the same narrated Act label in `exact_act`. That copy must
change with the Act word during the falsifier so it cannot claim an Act other
than the exact Act occurrence it addresses. Whether the result needs any copied
Act label remains a later, independent subtraction question.

## Required falsifier

Replace only the Act coordinate and its result copy:

```text
exact-byte Measurement
→ Measurement
```

Retain:

```text
exact subjects
source Localities
source completeness boundary
recording cut
Act and result occurrence identities
Act-before-result order
one result per Act
all byte findings
restart and current-coordinate replay
downstream byte-pair Measurement and Movement
```

Changing the Act to `Compare` or another admitted Act must refuse. No Byte Act,
ExactByteMeasurement, or family-specific Act identity may replace the narrated
string.

## Family boundary

This census addresses only the active plain-byte Measurement Act. Byte-pair,
occurrence-position, shared-position, and Compare-Distinction Measurement
families receive no result by analogy.

## Disposition

```text
Measurement                                      independently testified Act
exact-byte subject domain                        retained in exact coordinates
exact-byte Measurement                           failed as separate Act
result exact_act copy                             retained for later pressure
Act-word subtraction                             next falsifier
```
