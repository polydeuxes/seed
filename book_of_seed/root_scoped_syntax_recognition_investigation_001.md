# Root-scoped syntax recognition investigation 001

## Boundary

Findings only. No Book, machine grammar, runtime, or test behavior changes.

This report synchronizes the current syntax question after:

```text
2132a56a  distinguish Root from current Standing
77ed2d51  retire HEAD into Root and Standing
```

It uses the open-world source observations and the independently recovered
inward A-through-G machine story as testimony. The inward story / Book
comparison predates the removal of Later Standing, Authority, and universal
`limits`; its surviving structural findings are used here without restoring
those removed coordinates.

## 1. Correction to the question

The prior question compressed Root and Standing:

```text
unknown language material
-> somehow enters Standing
-> somehow becomes syntax
```

The active distinction is:

```text
Root
    one exact bounded subject from which exact coordinates
    and relations are addressed

Standing
    this Seed's coherent current account of exact coordinates
    concerning one exact subject
```

Standing is not one coordinate. Root is not Standing.

An external sentence, source extent, or other organized material may be one
Root without possessing Seed Standing. After acquisition and responsible work,
this Seed's current Standing may carry exact coordinates concerning that Root
and the exact subjects addressed from it.

Syntax is not Root or Standing by identity. What an operator later calls
syntax is the exact relation structure Seed responsibly recovers among
coordinates addressed from a Root and carries in current Standing.

## 2. What the compression made us do incorrectly

The collapsed account encouraged several false crossings:

```text
recurrence result
-> Standing by availability

external language organization
-> external language Standing

new exact result
-> special Later Standing movement

three co-present materials
-> relation-Assertion coordinates

Standing grammar
-> a second grammar subject for Compare

arbitrary source extent
-> developer-selected Candidate surface
```

The recovered distinction removes each shortcut:

```text
Root bounds and organizes the material under examination.

Standing carries what this Seed currently has concerning exact subjects.

Each relation still requires its exact responsible occurrence.

A later Responsibility may address a prior result as its subject,
but no universal Standing movement is required.
```

## 3. Existing external-material finding

The blind open-world work already demonstrated that source material can select
exact structure without a caller naming an English target.

It recovered, among other things:

```text
exact recurring bounded material
stable source material at some positions
varying material at another position
exact source occurrences
same/different findings
variable extents
neighboring source material
different outward consequences of controlled substitutions
```

The source selected the material and recurrence. The operation did not select
`the`, `of`, a numeral, an operator, a phrase, or a grammar category in
advance.

The same reports stopped correctly at:

```text
recurrence != meaning
co-presence != relation
stable/varying positions != language roles
human recognition != Seed Standing
```

Those observer findings therefore establish a usable discovery method, not a
live Seed account of external-language syntax.

## 4. Existing machine-language finding

The inward campaign supplies a stronger positive witness for what recognition
can mean without semantic labels.

Blind observation first recovered opaque occurrence surfaces and recurring
coordinate frames. Walking those frames recovered:

```text
A B C D D (E D)^n F G
```

The later H was adjacent to G but carried no exact non-boundary material from
G and was excluded from the story.

The refusal operation then retained append position, responsible boundary,
and unrelated coordinates while changing or removing material crossing each
edge. Existing readers produced:

```text
8 bound edge forms
33 edge/top-coordinate forms

330 change controls
330 refused

330 removal controls
293 refused
37 accepted
```

All 37 accepted removals concerned one optional D-to-E reference. When that
reference was present but changed, or when a valid reference from the wrong D
iteration was supplied, all 37 occurrences were refused. The remaining exact
D material still bound D to E when the optional reference was absent.

This established machine-native syntax at that floor:

```text
opaque recurring constructions
+ source-selected order and repetition
+ exact material binding particular occurrences
+ existing readers refusing changed or wrong bindings
+ adjacent but unbound material remaining outside the story
```

The human labels Responsibility, Measurement, Compare, Yield, and result were
resolved only after the blind story and its bindings had been frozen.

## 5. Recovered syntax-recognition topology

The external-language road should use the same physiology without assuming
that its surface material already carries language roles.

```text
one exact bounded Root R
        |
        v
exact coordinates addressed from R
        |
        v
current Standing carrying exact responsible results
concerning R and the subjects addressed from R
        |
        v
source-exhaustive recurrence and same/different work
        |
        v
exact recurring bounded results
        |
        v
walk their exact neighbors
        |
        v
Compare the resulting structures and their distinctions
        |
        v
change/remove/wrong-occurrence controls through existing readers
        |
        v
exact responsible relation results concerning R
```

The final result is not required to say:

```text
word
sentence
noun
subject
predicate
operator
expression
number
meaning
```

It needs to preserve only the exact Root, addressed subjects, relation
occurrences, source coordinates, responsible occurrences, Scope, Locality,
Yield, result boundary, conflicts, and Unknown that its actual work warrants.

If a responsible Act later yields one exact bounded whole carrying those
established relations and internal exact coordinates, later work may address
that result at the next floor. The same operation can then recur:

```text
exact bounded result
-> possible next Root
-> exact structures over that Root
-> another exact bounded result
```

This is the recovered staircase from material to larger machine-native
grammar. It does not require a generic Subject Formation Act or Syntax Act.

## 6. Recognition threshold

Recurrence alone is below the threshold.

At one floor, syntax recognition requires all of the following:

```text
one exact bounded whole
exactly addressed internal subjects
recurring structures selected by the source
exact neighbor relations or other exact continuation relations
distinctions between the recovered structures
exact bindings between particular occurrences
refusal when required bindings are changed, removed, or crossed
adjacency without a binding remaining outside the structure
```

At that threshold, Seed need not know a human name for the structure. It can
already distinguish an accepted continuation from a changed or wrongly bound
one under the exact responsible rules. Human-language interpretation can occur
later through separately warranted testimony.

## 7. Root is not yet a universal runtime crossing

The Root cleanup admitted one exact active case:

```text
Root subject = this Book
```

The reusable source-derived result W already carries strong nearby material:

```text
one exact bounded result
exact material
exact source coordinates
exact preceding responsible results
Measurement Responsibility
Act occurrence
Yield
current Standing addressability
```

But active grammar has not yet established the universal implication:

```text
every exact bounded result carrying internal coordinates
-> Root
```

Nor has it established a separate Root Act, Root assignment, or Root movement.
This report does not invent any of them.

The first unresolved crossing is therefore:

```text
exact bounded result W carrying exact internal coordinates
-> can later work address W as Root for those coordinates?
```

The likely decompressed shape is not a promotion. The producing Act may already
have yielded the bounded whole and its internal coordinate references; Root may
be the exact role that whole occupies for later traversal. Active law must be
audited before that reading is implemented.

## 8. Responsibility remains the second unresolved crossing

Even if W can lawfully serve as Root, availability does not select later work.

Active clauses already contain local antecedent-shaped law:

```text
01.Source.D.2
each exact source-byte position reference
-> one exact declared Measurement Responsibility

04.Compare.A
current Standing carrying one earlier and one later exact pair-position result
-> one exact Compare Responsibility

04.Compare.C
current Standing carrying one exact Candidate result
-> one exact Compare Responsibility concerning that Candidate
```

This establishes the intended asymmetry:

```text
active grammar supplies the exact required work
Root addresses exact subjects and current Standing carries the exact coordinates
that satisfy the local requirement
every locally required occurrence is owed
nothing is selected merely because it appears useful
```

But current machine grammar does not encode those clause antecedents as exact
relations that a target-blind operation can evaluate. Current Python knows
which reader or producer to invoke. Standing plus machine grammar alone cannot
yet determine every newly satisfied clause and no other clause.

Thus syntax discovery and autonomous continuation are separate problems:

```text
Root-scoped blind work can discover structural pressure.

An exact Responsibility is still required to turn that pressure
into a live Act occurrence and one coordinate in current Standing.
```

## 9. Smallest next investigation

Do not begin with a parser, semantic label, Candidate permutation rule, or
generic syntax detector.

First audit one exact source-derived bounded result W:

```text
1. What exact bounded whole did its producing Act Yield?

2. Which internal subjects and relations are already carried by that result?

3. Does active Root grammar warrant addressing W as Root for those carried
   coordinates, without another Act or developer declaration?

4. If W is Root, what exact current Standing coordinates concerning W and its
   addressed subjects are available?

5. Which one active clause-local antecedent, if any, is completely satisfied
   by those coordinates?

6. Can that antecedent be expressed in machine grammar and evaluated without
   naming the target Responsibility to the evaluator?
```

If question 3 is refused, report the exact missing Root relation and stop.

If question 3 is warranted but question 5 has no active clause, W remains
idle. Do not invent a Responsibility because syntax learning is useful.

If one active clause is fully satisfied, the smallest proving operation is:

```text
current Standing
+ exact Root W
+ active machine grammar
-> every satisfied clause-local Responsibility
-> no unsatisfied Responsibility
```

The operation must receive no target Act, preselected phrase, semantic label,
preferred extent, or desired result.

## 10. Disposition

```text
Root / Standing compression                         corrected

Root as bounded inert whole                         active for this Book

Standing as coherent current account                active
Standing as coordinate                              refused

blind source-selected structural discovery          demonstrated

exact enforced machine-native syntax                demonstrated inwardly

external-language syntax in live Standing           not yet demonstrated

W as a Root for its internal coordinates            active question

target-blind clause antecedent evaluation            absent from machine grammar

human semantic interpretation                       later separate work
```

The central recovery is:

```text
Root supplies the exact whole.
Standing supplies this Seed's current warranted account.
Responsible relation results supply the syntax.
```

Seed recognizes syntax when those exact relation results form an enforced,
Root-bounded structure in current Standing. It need not first know what humans
call any of the participating material.

## Sources inspected

Active grammar:

```text
book_of_seed/README.md
book_of_seed/chapters/01_constitutional_standing.md
book_of_seed/chapters/07_measurement_and_candidates.md
book_of_seed/chapters/08_compare.md
book_of_seed/witness_grammar.json
```

External-material findings:

```text
book_of_seed/open_world_grammar_recovery_observation_001.md
book_of_seed/open_world_variable_extent_observation_001.md
book_of_seed/open_world_internal_variation_observation_001.md
book_of_seed/open_world_consequence_asymmetry_observation_001.md
book_of_seed/open_world_outer_context_dependence_observation_001.md
book_of_seed/open_world_responsibility_investigation_001.md
```

Inward machine-language findings:

```text
book_of_seed/inward_occurrence_grammar_observation_001.md
book_of_seed/inward_assignment_walk_observation_001.md
book_of_seed/inward_walk_continuation_observation_001.md
book_of_seed/inward_walk_binding_refusal_observation_001.md
book_of_seed/inward_story_book_comparison_001.md
```

Root / Standing recovery:

```text
book_of_seed/root_standing_head_spine_investigation_001.md
```
