# Inward occurrence grammar observation 001

## Boundary

This is findings only.

No Book, machine grammar, or runtime law is amended here. No occurrence label,
Book coordinate, machine coordinate, or expected Responsibility coordinate was
read by the blind operations.

The question was:

```text
When Seed's exact recorded occurrences are measured as unknown material,
what recurrent machine-language frames does that material establish?
```

The result is positive but bounded:

```text
one nine-coordinate frame emerged without its name
its occurrences have a consistent source-order direction
all 228 closed frames have distinct coordinates and distinct neighbors

but

the nine-coordinate frame is narrower than active Responsibility grammar
adjacency does not establish what causes the frame to appear
```

## 1. Causal order

The observation was separated into exact frozen steps.

```text
current Seed execution over opaque material
-> exact occurrence-source artifact

occurrence material only
event labels withheld
scalar values withheld
-> exact coordinate-surface artifact

coordinate-name lookup
-> separately addressed post-freeze artifact

coordinate surfaces only
-> every closed recurring coordinate frame

every frozen frame
-> immediate prior/later surfaces
-> exact scalar sameness with those surfaces
-> exhaustive frame-to-frame comparison

only afterward
-> runtime labels and active Book comparison
```

The discovery scripts read no Book or machine grammar.

## 2. Exact source material

`scripts/record_inward_occurrence_material.py` supplied four meaningless byte
sequences of different lengths to four fresh Localities:

```text
8 bytes
11 bytes
12 bytes
14 bytes
```

The bytes contain no mathematical notation, grammar label, command, or name of
an Act. They differ so equal acquisition length cannot manufacture a common
occurrence frame.

Current operator acquisition and its declared work recorded:

```text
source 0       231 occurrences
source 1       327 occurrences
source 2       359 occurrences
source 3       423 occurrences
total        1,340 occurrences
```

The source uses the current process-local `EventLedger`. The JSON artifact
freezes the resulting occurrence material, but this experiment does not test
SQLite durability or ledger integrity testimony.

The sources were recorded in parallel in 34.719 seconds. Each source carries no
known loss.

```text
source artifact SHA-256
c2d4ff9f112e99da30e296f6903cf529d0b076ec36a89b574b1de84190f7929a
```

The source artifact preserves occurrence labels for later reading. The blind
observer never reads them.

### Isolation limitation

One source containing two lines in the same Locality presently fails during the
second line:

```text
ValueError:
byte-pair position-coordinate result carries no exact Act
```

One line completes with 103 occurrences; two lines reach 113 occurrences and
then fail while current Standing reads a source-position Compare Applicability
Act. Four separate fresh Localities were therefore used for the blind source.

This means the observation establishes cross-Locality recurrence. It does not
yet establish that the complete finding is unchanged when all four materials
enter one continuing Locality. The runtime refusal is exact and is not repaired
in this work.

## 3. First blind aperture: occurrence coordinate surfaces

`scripts/observe_inward_occurrence_surfaces.py` reads only:

```text
occurrence material
source number
append position
```

For each occurrence it measures:

```text
exact top-level coordinate material
immediate value type
immediate container member count as a separate exact structure distinction
```

It withholds:

```text
event label
scalar value
Book
machine grammar
runtime constants
nested coordinate interpretation
```

The observer necessarily reads exact coordinate material in order to hash it.
The first version placed its hash-to-plaintext lookup beside the blind finding;
the blind operations did not consult that lookup, but physical separation was
weaker than the report first stated. The lookup is now written as a separate,
addressed artifact. The blind coordinate-surface artifact contains hashes only.

The first result is:

```text
1,340 occurrences
-> 33 exact coordinate surfaces

four differently sized sources
-> one shared 218-occurrence adjacent frame

stable first and last surfaces with a varying middle
-> 2 exact frames
```

The common 218-occurrence frame begins at append position zero in every source.
The shortest source ends the common frame; the longer sources continue with
additional source-position work.

```text
blind coordinate-surface artifact SHA-256
68dda00a44dbbea35bc363bc1e42b1d371fb0c551220859e77f1a6da4bccaa6f

separate coordinate-material lookup SHA-256
0771d046d834445bda582bcdb57f2c66e07fc98380f7bb73bc14f88e063dfca9
```

## 4. Second blind aperture: closed recurring frames

`scripts/observe_inward_coordinate_frames.py` begins only from the frozen
coordinate surfaces. It intersects all exact coordinate bodies until no new
intersection exists. It retains a frame only when:

```text
the frame is the exact intersection of every surface carrying it
at least two distinct surfaces carry it
all four exact sources carry it
```

No target coordinate or frame size is supplied.

```text
all nonempty intersections             257
closed frames carried by all sources   228
```

```text
blind closed-frame artifact SHA-256
a4ee1501bb33d92b0298113084dd1b343e7aec5ab8ef3ef504274170ec10e5cf
```

The previously noticed nine-coordinate frame emerges as frame 52 under this
blind rule:

```text
9 coordinates
8 distinct occurrence surfaces
106 exact occurrences
4 exact sources
```

Only after that artifact was frozen were its coordinate materials read:

```text
assignment_identity
assignment_subject_identity
book_clause_identity
limits
responsibility
responsible_boundary
result_boundary_identity
scope
unknown
```

Only after freezing were its occurrence labels read. All eight surfaces are
currently labeled as Responsibility-assignment recordings from acquisition,
byte Measurement, byte-pair Measurement, source-position Measurement,
Locality-position Measurement, addressed-byte determination, and shared-pair
Measurement.

Thus the nine-coordinate frame did not depend on selecting labeled
Responsibility occurrences. The blind coordinate rule found it among 228
frames.

### The broader two-coordinate frame

Another exact closed frame contains only:

```text
responsibility
responsible_boundary
```

It occurs 930 times across 32 of the 33 coordinate surfaces and all four
sources. Those two coordinates are the complete intersection of those 32
surfaces.

The remaining surface occurs 410 times. Post-freeze reading identifies every
one as an exact Yield relation occurrence.

This establishes a broad machine-language distinction:

```text
32 occurrence surfaces carry responsibility + responsible_boundary
Yield relation surface does not
```

It does not establish that all 32 surfaces are Responsibilities. The frame is
broader than the nine-coordinate assignment frame.

## 5. Walking every frame to its neighbors

`scripts/observe_inward_frame_neighbors.py` applies the same immediate
source-order measurement to all 228 frames.

```text
blind neighbor artifact SHA-256
4201c6446bc2d3fba760f59bb86a447845577dc35333fe2ce736adf7f458f9b6
```

For the blind nine-coordinate frame:

```text
106 occurrences total

prior:
    102 have an exact prior surface
      4 begin one exact source

later:
    106 have an exact later surface
      0 end one exact source
```

After freezing, the 102 prior surfaces read as yielded results. Every one of the
106 later surfaces reads as an Act occurrence or Applicability Act occurrence.

This is exact direction in the recorded machine language:

```text
prior result or source beginning
-> nine-coordinate frame
-> Act / Applicability Act
```

It does not yet establish that the prior result causes the frame or that the
frame warrants the Act.

## 6. Exact sameness carried into neighboring occurrences

`scripts/observe_inward_frame_neighbor_equalities.py` compares every top-level
scalar coordinate of every frame with every top-level scalar coordinate of its
immediate prior and later occurrences. Coordinate names and values are not
selected; values are retained only by scalar type and digest.

An initial implementation mistakenly measured every extra coordinate of every
surface carrying a frame. That produced a 12 MB artifact and blurred the frame
boundary. Restricting the operation to coordinates actually belonging to each
frame reduced it to 4.1 MB and 7,986 source-recurring equalities.

```text
blind neighbor-equality artifact SHA-256
fd4d84c24c93a7efb3798ebc407eea3212c1bf0443bc35492bda7a4c34c18381
```

For frame 52, post-freeze reading gives:

```text
responsible_boundary
    same exact scalar in all 106 later occurrences
    same exact scalar in all 102 available prior occurrences

responsibility
    same exact scalar in 102 later occurrences
    same exact scalar in 4 prior occurrences

result_boundary_identity
    same exact scalar in 8 later occurrences
```

No top-level scalar equality carries `assignment_subject_identity` into the
immediate later occurrence under this first aperture.

The finding therefore establishes more than adjacency: two coordinates of the
nine-coordinate frame continue as exact scalar material into almost every
later Act surface. It does not establish the constitutional relation between
those coordinates and the Act.

## 7. Comparing every frame

`scripts/compare_inward_coordinate_frames.py` exhaustively compares all 25,878
frame pairs by exact coordinate containment/overlap and exact prior/later
signature.

```text
coordinate-unique frames       228 / 228
distinct neighbor signatures   228 / 228

disjoint pairs                     471
partial-overlap pairs           22,398
one frame contained in another   3,009
```

```text
blind frame-comparison artifact SHA-256
0f011b1a1edd374080478d048cb7ed7cade97761c9d83d896115c0fff649a0ee
```

Thus none of the 228 frames is a duplicate under either measured surface:

```text
each has its own exact coordinate body
each has its own exact prior/later signature
```

Frame 52 has two immediate larger coordinate frames:

```text
frame 35
    frame 52 + standing_boundary_identity

frame 36
    frame 52 + act_occurrence_identity
```

It has two immediate smaller frames:

```text
frame 135
    book_clause_identity
    limits
    responsibility
    responsible_boundary
    scope
    unknown

frame 202
    responsibility
    responsible_boundary
    result_boundary_identity
    scope
```

This places the nine-coordinate finding inside measured grammar over grammar;
it is neither an isolated bag of names nor the only Responsibility-shaped
coordinate body.

## 8. Comparison with active constitutional grammar

Only after all blind artifacts were frozen was frame 52 compared with active
Responsibility anatomy.

Active grammar requires one exact Responsibility to carry:

```text
responsible boundary
subject
exact Act
Authority
Scope
Locality
limits
required relations
```

Frame 52 carries plausible exact counterparts for:

```text
responsible boundary       responsible_boundary
subject                    assignment_subject_identity
Scope                      scope
limits                     limits
```

It also carries assignment, Book-clause, result-boundary, responsibility, and
Unknown coordinates.

The first blind aperture does not establish uniform coordinates for:

```text
exact Act
Authority
Locality
required relations
```

Act identities occur under several surface-specific coordinate names outside
the common nine-coordinate frame. Authority is not a uniform top-level
coordinate. Locality may occur inside `scope`, but nested coordinates were not
measured and cannot be promoted by inspection.

Disposition:

```text
frame 52 is an exact recurrent machine-language construction
frame 52 corresponds to current Responsibility-assignment recordings
frame 52 is narrower/compressed relative to active Responsibility grammar
frame 52 != complete constitutional Responsibility
```

## 9. A concrete absence exposed by the common source-order frame

The common 218-occurrence frame repeatedly contains this post-freeze sequence:

```text
source-position Compare Applicability Act
-> Yield
-> Applicability result
-> Compare Act
-> Yield
-> Compare result
```

No separate source-position Compare Responsibility recording precedes that
sequence. `comparison_of_ordered_path_source_position_material.py` embeds a
responsibility string and responsible-boundary string directly into the
Applicability and Compare materials, but defines no exact Responsibility record
or reference for that Compare occurrence.

This is consistent with the blind result: the broad two-coordinate frame is
present, while the nine-coordinate assignment frame is absent at that exact
entrance.

Active `04.Compare` grammar requires an exact Compare Responsibility before
Applicability and Participation. The live source-position Compare road does not
presently record that exact preceding physiology.

This is a concrete runtime discrepancy. It is not repaired here.

## 10. Result and stop

The experiment establishes the proposed scientific move:

```text
Seed's exact occurrences
-> blind coordinate surfaces
-> blind closed frames
-> one unnamed nine-coordinate construction
-> exact directional neighbors
-> post-freeze correspondence with Responsibility-assignment recordings
```

It does not yet establish:

```text
what condition makes the frame appear
that adjacency warrants the later Act
that frame 52 is complete Responsibility
that all material entering one continuing Locality gives the same finding
```

The next lawful observation, after review, is narrower than a generic
Responsibility selector:

```text
for each blind assignment-like frame occurrence,
which exact coordinate values continue into its later Act,
including nested coordinates and family-specific Act coordinates?
```

That can test whether the frame carries the later Act by exact relation rather
than merely preceding it.

Before relying on continuing-Locality results, the second-input replay refusal
also needs a separate diagnosis.

No Book amendment and no runtime repair are made in this observation.
