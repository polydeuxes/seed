# AttributedOperatorExpression active-road Fidelity recovery 001

## Scope and negative authority

This is a bounded, report-only recovery of the current repository standing of `AttributedOperatorExpression` and `attribute_operator_expression(...)` on the current merged `main` worktree. PR 1909 is ignored entirely: no report, patch, conclusion, test, vocabulary addition, or proposed Unknown handling from that closed PR is used as evidence.

This report does not amend the Book, production code, tests, artifact identity, field names, Unknown dimensions, Runtime wiring, CLI routes, ingress design, interpretation design, conversation design, question formation, or any universal external-material abstraction.

The governing question is limited to the current producer -> `AttributedOperatorExpression` -> consumer road, if any, and whether any current consumer relies more strongly than the artifact's preserved standing warrants. Constructibility, exports, tests, signatures, type annotations, future handoffs, and documentation are treated as availability or testimony only unless a current non-test executable production-to-consumption handoff is evidenced.

## Book clauses actually needed

Only current canonical Book clauses were needed:

1. **External grammar and attribution limits.** External grammar can be identified, compared, and attributed without becoming Seed constitutional grammar; Unknown limits must remain visible. External representations can become addressable source-attributed material only while retaining source/provenance/grammar/scope/uncertainty/confidence/authority limits. Cross-seam consumers must preserve these limits and must not mutate external material into cluster truth or relocate authority.
2. **Operator/question boundary.** Question-shaped external material is testimony, pressure, goals, constraints, corrections, or response; Seed owns any later internal question-forming act. An operator ask may create attributed testimony or inquiry pressure only, unless a bounded translation separately preserves the required identity, source, scope, evidence demand, authority limit, uncertainty, and lawful stop.
3. **Evidence/provenance limits.** Evidence-shaped material must distinguish evidence identity from existence and preserve source attribution, context, subject, supported claim, authority boundary, uncertainty, and provenance relation. Provenance references contribute represented lineage only when the referenced material exists and is applicable; a string or coherent lineage is not verified provenance, occurrence proof, fact, or independent support.
4. **Testimony limits.** Attributed testimony or premise-relative input preserves attribution, source-relative limits, conflicts, and uncertainty, but it does not establish the testified content as fact, prove producer occurrence, or strengthen standing by repetition or copied lineage.
5. **Authority scope limits.** Operator expressions and approvals may contribute authority only after interpretation and scope binding. Downstream reliance may consume warranted content only within the preserved role, purpose, source/provenance, evidence, authority, scope, uncertainty, Unknown, and negative-authority limits.

No historical report from 1885-1908 was needed to interpret this exact boundary.

## Symbol and path search method

Searches were independent of PR 1909 and limited to the current repository. The following repository searches and file inspections were used as evidence:

- `rg -n "AttributedOperatorExpression|attribute_operator_expression" .`
- `rg -n "AttributedOperatorExpression|attribute_operator_expression|interpret_operator_expression|bind_operator_authority_scope|establish_bounded_operator_goal_from_authority_scope_binding" --glob '!book_of_seed/attributed_operator_expression_active_road_fidelity_recovery_001.md' --glob '!*.md' .`
- Direct inspection of:
  - `seed_runtime/operator_expression_interpretation.py`
  - `seed_runtime/operator_authority_scope_binding.py`
  - `seed_runtime/bounded_operator_goal_establishment.py`
  - `seed_runtime/__init__.py`
  - current Book clauses in `book_of_seed/01-grammar-and-standing/external-and-constitutional-grammar.md`, `book_of_seed/04-inquiry-and-examination/questions-and-inquiry.md`, `book_of_seed/05-evidence-and-knowledge/evidence-provenance-and-explanation.md`, `book_of_seed/05-evidence-and-knowledge/testimony-and-established-fact.md`, and `book_of_seed/08-authority-communication-and-stopping/authority-scope.md`.

The search covered calls, direct construction, imports/re-exports, typed parameters and return values, serializer methods, wrapper artifacts, tests/fixtures, and current non-test runtime-module paths. Documentation and prior reports were not used as proof of active execution.

## Producer recovery

### Producer P1: `attribute_operator_expression(...)`

- **Entrypoint or caller:** Public callable function `attribute_operator_expression(...)` in `seed_runtime/operator_expression_interpretation.py`. It is also re-exported by `seed_runtime/__init__.py`.
- **Current callers:** Current non-documentation callers found by search are tests only: `tests/test_operator_expression_interpretation.py` and `tests/test_operator_authority_scope_binding.py`. No non-test repository path calls this function.
- **Input standing consumed:** Caller-supplied `exact_text`, `input_representation`, `source_channel`, `workspace_ref`, `session_ref`, `operator_ref`, `provenance`, `received_scope_context`, `uncertainty`, and `unknowns`.
- **Responsible act performed:** The function strips and collapses whitespace into `normalized_text`, builds a stable payload containing exact and normalized text plus supplied attribution/context/uncertainty material, computes a stable `expression_id`, filters/sorts tuple fields through `_refs`, and returns an immutable `AttributedOperatorExpression`.
- **Required warrant evidenced in code:** The code requires only callable construction inputs. It does not validate source occurrence, identity, authority, addressability, provenance applicability, scope adequacy, or Unknown completeness before construction.
- **Resulting artifact standing:** A read-only representation preserving exact operator material, normalized form, caller-supplied representation/channel/workspace/session/operator/provenance/scope/uncertainty/Unknown strings, and non-mutation flags.
- **Preserved limits:** The artifact carries a boundary note that exact operator material is preserved independently from normalized interpretation form. Its default operational flags are `read_only=True`, `writes_event_ledger=False`, and `mutates_cluster=False`.
- **Stronger standings explicitly not created:** The producer does not establish verified operator identity, operator role, authority, addressability, provenance verification, provenance sufficiency, complete lineage, interpreted meaning, bounded question, permitted scope, event-ledger mutation, cluster truth, or runtime occurrence.
- **Non-test executable production path:** Not evidenced. The function is constructible and importable, but no current non-test executable call path was found that invokes it.

### Producer P2: Direct construction of `AttributedOperatorExpression`

- **Entrypoint or caller:** The class is a frozen dataclass and can be manually constructed by any importing code.
- **Current callers:** Current direct construction found by non-documentation search is test-only: `tests/test_bounded_operator_goal_establishment.py` constructs instances for fixture purposes.
- **Input standing consumed:** Direct caller supplies all fields, including `expression_id`; direct construction bypasses the normalization and stable-id payload responsibility of `attribute_operator_expression(...)`.
- **Responsible act performed:** Dataclass initialization only.
- **Required warrant evidenced in code:** No validation beyond Python dataclass construction.
- **Resulting artifact standing:** A directly constructed object shaped like `AttributedOperatorExpression`, not proof that the repository producer performed normalization or stable attribution.
- **Preserved limits:** Only whatever field values the direct caller supplied.
- **Stronger standings explicitly not created:** Direct construction does not establish live producer occurrence, normalization correctness, stable identity derivation, operator identity, authority, addressability, provenance verification, interpretation, or consumer handoff.
- **Non-test executable production path:** Not evidenced.

## Current callers

Current non-documentation callers of the producer function are test-only. Current non-documentation direct constructors are test-only. Package re-export in `seed_runtime/__init__.py` makes the class and function importable but does not constitute a production route.

## Non-test production paths

Not evidenced.

There is no current non-test repository call path that:

1. receives or derives operator material,
2. calls `attribute_operator_expression(...)` or directly constructs `AttributedOperatorExpression`, and
3. passes that produced instance onward.

The module-level functions are executable when directly invoked by an external caller, but a callable library surface is constructibility, not an evidenced current repository production path.

## Artifact standing

`AttributedOperatorExpression` fields currently stand as follows:

- `expression_id`: stable identifier computed by the producer function from the supplied payload, or arbitrary caller-supplied identifier under direct construction. It identifies the represented expression artifact, not verified operator identity or producer occurrence.
- `exact_text`: literal operator material supplied by the caller. It is preserved material, not interpreted meaning or a Seed-owned question.
- `normalized_text`: whitespace-normalized form derived by the producer function. It is an interpretation input convenience, not interpretation established.
- `input_representation`: caller-supplied representation label, defaulting to `operator text`. It is not a recovered grammar warrant.
- `source_channel`: caller-supplied channel/classification label, defaulting to `external input`. No code observes a crossing or verifies the channel; default realization convention is therefore plausible. It establishes no more than preserved caller/default channel classification.
- `workspace_ref`, `session_ref`, `operator_ref`: caller-supplied reference strings. Presence establishes only reference presence for later equality checks; it does not establish identity, role, authority, or addressability.
- `provenance`: filtered/sorted nonempty caller-supplied strings. Nonempty provenance establishes only represented provenance-reference presence in the artifact. It does not establish verification, sufficiency, complete lineage, source occurrence, or applicability.
- `received_scope_context`: filtered/sorted nonempty caller-supplied strings. It preserves received scope-context material when supplied; an empty value means no nonempty scope-context string survived filtering, not `not applicable`, `unsupported`, `Unknown`, or `verified no scope`.
- `uncertainty`, `unknowns`: filtered/sorted caller-supplied strings. Empty values mean no nonempty strings were supplied or preserved; they do not prove absence of uncertainty or Unknowns.
- `boundary_notes`: fixed note distinguishing exact material preservation from normalized interpretation form.
- `read_only`, `writes_event_ledger`, `mutates_cluster`: default non-operational flags in the artifact shape. They represent artifact non-mutation posture, not runtime occurrence.

The exact responsibility remains narrower than addressability establishment on current implementation evidence: representation construction and caller-supplied attribution/context preservation are evidenced; addressability establishment is at most a plausible intended interpretation under Book-compatible external-material grammar, but no current producer validates the minimum addressability limits.

## Possible consumers

### Consumer C1: `interpret_operator_expression(...)`

- **Consumer responsibility:** Read one `AttributedOperatorExpression` together with grammar recovery and applicability artifacts, parse/interpret the normalized operator expression under a recovered grammar, preserve unresolved/unsupported/unknown/conflict material, and optionally produce a future authority/scope-binding handoff when interpreted.
- **Non-test path supplying the artifact:** Not evidenced. Tests supply produced fixtures, but no current non-test executable path supplies a produced instance.
- **Fields consumed:** `normalized_text` and `exact_text` are consumed by `_parse`/span construction; `unknowns` and `provenance` are carried into projection-level Unknowns/provenance; `expression_id` becomes `attributed_expression_ref`; `operator_ref`, `workspace_ref`, and `session_ref` are copied into `FutureOperatorAuthorityScopeBindingHandoff` if interpretation succeeds. `source_channel`, `input_representation`, `received_scope_context`, `uncertainty`, and artifact operational flags are included in `expression.to_json_dict()` for stable payload identity but are not otherwise interpreted locally.
- **Standing expected:** An attributed expression object with exact and normalized text, not a verified authority grant.
- **Standing actually inferred:** Interpreted/ambiguous/unsupported/unknown/conflict projection under independent grammar recovery/applicability checks. It explicitly states that interpretation does not establish operator authority; requested scope is not permitted scope; authority-bearing language is not an authority grant; extracted domain expressions are not resolved Seed entities; interpretation does not formulate a bounded constitutional question; and it does not authorize, schedule, emit, or execute.
- **Empty or missing values encountered:** Empty `operator_ref`, `workspace_ref`, `session_ref`, `provenance`, `unknowns`, or scope context would be copied or omitted by tuple filtering; current code does not convert emptiness into verified absence, authority, or truth. `source_channel` has a default value, so absence at call time can become the default string `external input`.
- **Unknowns preserved or removed:** Input `expression.unknowns` are merged into output `unknowns`. Tuple filtering removes empty strings, but no typed absent/unsupported/not-applicable distinction is represented.
- **Stronger assertion produced, if any:** No current non-test consequence is evidenced. As a callable function, it can produce interpreted expression components from normalized text, but this is not an active consumer dependency without a non-test supplied instance.

### Consumer C2: `bind_operator_authority_scope(...)`

- **Consumer responsibility:** Bind an interpreted operator request and future handoff to separately supplied operator identity, workspace/session authority, and scope-binding contexts.
- **Non-test path supplying the artifact:** Not evidenced. Tests supply the expression/handoff/interpretation chain, but no current non-test executable path supplies a produced instance.
- **Fields consumed:** `expression_id` is compared against interpretation and handoff references; `operator_ref` is compared against handoff and operator identity context; `workspace_ref` is compared against workspace/session and scope contexts; `session_ref` is compared when a session ref is required. Other fields are not used for local authority/scope decisions.
- **Standing expected:** Matching references sufficient for consistency checks among independently supplied expression, interpretation, handoff, identity, workspace, and scope artifacts.
- **Standing actually inferred:** Matching string references permit continued binding calculation; separately supplied `OperatorIdentityContext`, `WorkspaceSessionAuthorityContext`, and `ScopeBindingContext` determine identity/authority/scope standing. The function does not treat `operator_ref` alone as verified identity because it compares it with `operator_identity.operator_ref` and uses `verified_identity_ref or operator_ref` only as output reference. It does not use expression provenance as authority.
- **Empty or missing values encountered:** If matching empty strings are supplied, equality checks can pass for optional refs unless `required_session_ref` forces session mismatch against a nonempty required session. That is representational weakness, but a Fidelity crossing is not established without a non-test supplied instance and downstream consequence.
- **Unknowns preserved or removed:** Unknowns from interpretation, handoff, operator identity, workspace/session, scope binding, and call are merged. Expression-level Unknowns are not separately merged here except insofar as interpretation/handoff already carried them.
- **Stronger assertion produced, if any:** No current non-test consequence is evidenced. In callable form, permitted/blocked/unknown/conflict binding standing is produced from independent contexts, not from expression attribution alone.

### Consumer C3: `establish_bounded_operator_goal_from_authority_scope_binding(...)`

- **Consumer responsibility:** Establish or refuse one bounded operator goal from a permitted authority/scope binding, matching interpretation, and expression; preserve ingress snapshots and upstream refs; avoid question formation and work authorization.
- **Non-test path supplying the artifact:** Not evidenced. Tests directly construct an expression fixture and call this function; no current non-test executable path supplies a produced instance.
- **Fields consumed:** `expression_id` is compared with binding and interpretation references, included in operator acceptance provenance, upstream source refs, and the consumed ingress material snapshot. `expression.unknowns` are merged with interpretation and binding Unknowns. The entire expression `to_json_dict()` is snapshotted. Other expression fields are preserved in the snapshot but not used to decide orientation/permission.
- **Standing expected:** A matching expression artifact to preserve ingress lineage and Unknowns after authority/scope binding has independently established permitted standing.
- **Standing actually inferred:** Goal establishment/refusal depends primarily on binding state, interpretation state, conflicts, unresolved scope, permitted scope, and orientation. The expression participates as match/lineage/snapshot material, not as independent authority.
- **Empty or missing values encountered:** Empty expression fields can be snapshotted or omitted from tuple refs. Current code does not strengthen empty expression provenance, scope context, source channel, or operator refs into truth or authority.
- **Unknowns preserved or removed:** `expression.unknowns` are merged into goal Unknowns. Empty unknown strings are not represented.
- **Stronger assertion produced, if any:** No current non-test consequence is evidenced. In callable form, a bounded goal can be established only from a permitted binding and interpreted material, not from expression construction alone.

## Current non-test consumers

No current non-test consumer dependency is evidenced. The non-test functions above accept or process the artifact, but repository search did not find a current non-test executable path supplying a produced `AttributedOperatorExpression` instance to them.

## Consumer-local field reliance

Consumer-local field reliance is therefore signature/callable reliance, not active dependency:

| Consumer | Non-test supplied produced instance? | Fields locally relied upon | Stronger standing locally created from expression alone? |
|---|---:|---|---|
| `interpret_operator_expression` | not evidenced | `normalized_text`, `exact_text`, `expression_id`, `operator_ref`, `workspace_ref`, `session_ref`, `provenance`, `unknowns`, full JSON payload for ID | no; interpretation standing is bounded by grammar/applicability and boundary notes |
| `bind_operator_authority_scope` | not evidenced | `expression_id`, `operator_ref`, `workspace_ref`, `session_ref` | no; authority/scope depends on separate identity/workspace/scope contexts |
| `establish_bounded_operator_goal_from_authority_scope_binding` | not evidenced | `expression_id`, `unknowns`, full JSON snapshot | no; goal standing depends on permitted binding and interpreted orientation |

No current consumer distinguishes absent, empty, Unknown, unsupported, not applicable, not supplied, and not established for every field. The dominant implementation distinction is nonempty string retained versus empty string filtered/defaulted. That asymmetry is a representational limitation, not by itself a Fidelity crossing.

## Serialization, reconstruction, or wrapper paths if present

- `AttributedOperatorExpression.to_json_dict()` serializes the dataclass to a JSON-shaped dictionary and converts tuple fields to lists. No `from_json`/deserialization/reconstruction function for `AttributedOperatorExpression` was found.
- `interpret_operator_expression(...)` includes `expression.to_json_dict()` in its stable payload and returns `attributed_expression_ref`; if interpreted, its future handoff carries `attributed_expression_ref`, `operator_ref`, `workspace_ref`, `session_ref`, provenance, Unknowns, conflicts, and source spans.
- `establish_bounded_operator_goal_from_authority_scope_binding(...)` snapshots the full expression/interpretation/binding JSON under `consumed_ingress_material_snapshot` and carries expression and related refs in upstream lineage.
- Package exports expose the class and constructor through `seed_runtime.__init__`, but exports are not active routes.
- No persistence table, CLI JSON loader, event ledger writer, runtime reconstruction path, or API route for this artifact was evidenced by the bounded search.

## Compact topology

```text
current non-test producer, if any
not evidenced
↓
AttributedOperatorExpression
↓
current non-test consumer, if any
not evidenced
↓
resulting standing or reliance
not evidenced
```

## Active-road classification

**Constructible only.**

Definitions, imports, exports, type annotations, serializer methods, tests, fixtures, direct manual construction, and callable producer/consumer functions exist. No current non-test executable production path is evidenced. No current non-test executable producer-to-consumer handoff is evidenced. Test fixture calls plus consumer signatures remain constructible-only under the requested classification rule.

## Producer/consumer cross-examination

The producer function preserves exact material, normalized material, caller-supplied references, tuple-filtered provenance/scope/uncertainty/Unknown material, a stable expression id, and read-only/non-mutation flags. It does not validate or establish addressability, authority, provenance applicability, identity, role, or interpreted meaning.

The possible consumers do not need the producer to have established authority or truth. `interpret_operator_expression(...)` uses the expression as interpretation input under separately supplied grammar recovery/applicability. `bind_operator_authority_scope(...)` uses the expression mainly for reference consistency against independent contexts. `establish_bounded_operator_goal_from_authority_scope_binding(...)` uses the expression for matching, lineage, Unknown preservation, and snapshotting after authority/scope binding.

No current non-test executable handoff connects these functions. Therefore, separate constructible producer and consumer functions cannot be joined into an active road.

## Fidelity classification

No true Fidelity crossing is established.

The required crossing elements are not all present:

1. A producer preserving standing S exists as a callable function, but no current non-test production path is evidenced.
2. No current non-test consumer is evidenced as receiving the produced artifact instance.
3. No current consumer consequence is evidenced in which S is treated as stronger standing S+.
4. Possible consumer functions contain boundary checks and notes that generally prevent attribution alone from becoming authority, addressability from becoming interpretation, and interpretation from becoming authorization.

## False or stronger assertions actually evidenced

No active false or stronger assertion is evidenced in a current non-test road.

Callable-path risks or compressions observed but not active Fidelity crossings:

- `source_channel` defaults to `external input`, which may look like observed crossing testimony but is only a caller/default classification in the producer implementation.
- `operator_ref`, `workspace_ref`, and `session_ref` can be copied or compared as strings, but presence does not verify identity, role, authority, or addressability.
- Nonempty `provenance` is copied/merged as represented provenance; it does not verify complete lineage or occurrence.
- Empty tuple fields are filtered out and can lose distinctions among absent, empty, Unknown, unsupported, not applicable, not supplied, and not established.
- Matching empty string references could satisfy some equality checks in callable consumers, but no current non-test supplied instance or downstream consequence is evidenced.

## Representational asymmetries that are not crossings

The following are asymmetries or compression candidates only:

- empty `operator_ref` is not typed as absent, unknown, not supplied, or not established;
- empty `provenance` is not typed as provenance Unknown or provenance unavailable;
- empty `received_scope_context` is not typed as no received scope, not applicable, unsupported, or Unknown;
- empty `unknowns` is not proof that no Unknown exists;
- `source_channel` has a broad default rather than observed-channel proof;
- broad string fields preserve references without reference verification;
- `AttributedOperatorExpression` has no dedicated addressability artifact or typed addressability state;
- serializers preserve shape but do not reconstruct or validate standing.

Under the requested Fidelity test, these are not crossings without a current consumer consequence.

## Remaining Unknowns

- Whether repository designers intended `attribute_operator_expression(...)` to be addressability establishment, attribution-only preservation, representation construction, or interpretation-input preparation remains not fully decidable from current implementation alone.
- Whether an external caller outside the repository invokes these public functions at runtime is outside current repository evidence.
- Whether future handoff fields are intended to become active authority/scope ingress is not present consumer evidence.
- Whether more granular absent/empty/Unknown distinctions are desirable is not answered here because no active downstream strengthening is evidenced.

## Lawful stopping point

The operation stops at **Constructible only** because no current non-test producer-to-consumer handoff is evidenced. It also stops because no current consumer strengthening of empty, missing, supplied, or Unknown material is evidenced, no true Fidelity crossing is established, and the artifact's exact responsibility remains plausibly narrower than addressability with multiple alternatives preserved.

No implementation repair is warranted by this bounded recovery.

## Final direct answers

1. **Is `AttributedOperatorExpression` Active, Partially active, Constructible only, Historical or stale, or Unknown?** Constructible only.
2. **What current non-test producer path, if any, constructs it?** Not evidenced.
3. **What constitutional act, if any, does that producer actually perform?** No active constitutional act is evidenced. The callable producer performs representation construction: exact-text preservation, whitespace normalization, caller-supplied attribution/context/uncertainty preservation, stable-id derivation, and read-only/non-mutation posture.
4. **Does the artifact establish attribution, addressability, interpretation input, or another narrower standing?** Current implementation establishes at most a narrower representation/interpretation-input artifact preserving caller-supplied attribution-shaped material. It does not verify attribution or establish addressability on current evidence.
5. **What current non-test consumers, if any, receive a produced instance?** Not evidenced.
6. **What exact fields do those consumers rely upon?** No active non-test consumer reliance is evidenced. Possible callable consumers rely on `normalized_text`, `exact_text`, `expression_id`, `operator_ref`, `workspace_ref`, `session_ref`, `provenance`, `unknowns`, and/or full JSON snapshots as described above.
7. **Does any current consumer strengthen empty or missing fields?** Not evidenced.
8. **Is any true Fidelity crossing established?** No.
9. **Is any implementation repair warranted now?** No, not from this bounded recovery.
10. **Where must this operation stop?** It must stop at the report-only finding: constructible artifact, no non-test producer-to-consumer handoff, no stronger downstream reliance, no Fidelity crossing, and no repair warranted.
