# 03 Runtime Loop

This is a historical RuntimeLoop-era document retained only as deleted/former
architecture testimony. It is not an active architecture, command, invariant, or
operator instruction document.

PR 1918 removed the pure foreign planning/control shell. Current Seed must not
preserve compatibility obligations, identifiers, imports, architecture claims,
invariants, or instructions for the deleted planning, handoff, proposal,
authorization, pending-action, or builder-candidate roads.

For current architecture and invariants, see `docs/architecture.md` and
`docs/invariants.md`. This historical placeholder intentionally does not define
a current request pipeline, model-decision route, policy route, execution route,
or compatibility mechanism.
